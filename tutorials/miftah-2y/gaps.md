# Miftah 2-year run — gaps and defects

Run `wt-miftah2y-20260922-1a5b5e70` · production · build `0c110a2` · UI-driven.

Severity: **P0** multi-tenant leak / data loss / money wrong · **P1** blocks a real
landlord workflow · **P2** forces a painful workaround · **P3** polish.

Kind: **BUG** (behaves wrong) · **UI-GAP** (API can, screens cannot) · **MISSING**
(product has no concept of it) · **UX** (possible but hostile).

| # | Beat | Sev | Kind | What a real user hits | Evidence |
|---|---|---|---|---|---|
| 1 | S01 (pre-login) | P2 | MISSING | The web app is still **RentAxis** end to end — logo, "Property Management Portal", "New to RentAxis?" on the sign-in screen — while this is being shown as the *Miftah* demo. The mobile apps were rebranded (PRs #80/#81); web + backend emails/PDFs were the remaining phase and are still outstanding on the deployed build. A customer opening the demo link sees the old brand first. | `/en/auth/login` on `0c110a2` |
| 2 | S05 | P1 | MISSING | **No invite / set-password flow when provisioning a user.** Super Admin → Users → New User makes `Password` a *required* field, so the only way to onboard a tenant admin, manager or guard is for an operator to invent a plaintext password and send it to the person out of band. The app already ships an `/auth/set-password` route, so the destination exists — nothing triggers it from here. Real landlords onboarding staff will email passwords around. | `/en/superadmin/users` → New User |
| 3 | S05 | P1 | UI-GAP | **`ACCOUNTANT` cannot be created from the UI.** `UserRole` has 7 values; the New User role dropdown offers 5 — `SUPER_ADMIN, TENANT_ADMIN, PROPERTY_MANAGER, SECURITY_GUARD, TENANT_USER`. `ACCOUNTANT` is missing, yet it is the role accounting v2 gates its whole finance surface on (post/reverse journals, opening balances, period lock, penalty approval — see the `@PreAuthorize` on `PenaltyAssessmentController`, `JournalController`). A customer literally cannot staff their finance function through the product. (`RENTER` is also absent but has a legitimate path via Renters → portal account.) | `/en/superadmin/users` → New User vs `UserRole.java` |
| 4 | S04 | P2 | UX | **Domain terminology is inverted between the UI and the product.** The sidebar says *Properties*; the page it opens is titled *Projects*; the thing you add to a Project is called a *"Property (Unit)"* and asks for a unit number and rent. In the backend that outer object **is** `Property` (it carries `nameEn/emirate/makaniNumber/fixedExpenses`) and the inner one is `Unit`. So the entity a landlord calls "my building" is a Project in one place and a Property in another, and "Property" means "apartment" in the create dialog. Every doc, support conversation and training deck inherits this confusion. | `/dashboard/properties` vs `Property.java` |
| 5 | S04 | P2 | UX | **Rent amounts carry no period anywhere.** The Add Unit form labels the field *Expected Rent* with placeholder `e.g. 5000`; the Add Property dialog uses `50000`; the units table column is just *RENT* showing `AED 85,000`. Nothing says per month or per year. The two placeholders imply different periods, and in the UAE both conventions are in daily use. A manager entering 5,000 meaning monthly gets an 85,000-a-year unit priced at 5,000. | Units tab, Add Unit form |
| 6 | S04 | P3 | UX | **Unit list comes back in arbitrary order** — A-101, A-201, A-102, A-103, A-203, A-202, A-301, A-302 after creating them in sequence. No sort by unit number, floor or creation time, and the column headers do not sort. Unusable for a real tower of 100+ units. | Units tab after creating 8 units |
| 7 | S05 | P2 | BUG | **Renter portal credentials are shown once, in plaintext, and cannot be recovered.** Creating a renter with "Create Portal Account" pops *"Portal Account Created — share these credentials with the renter"* with the email and a generated password in clear text and a copy button. There is no invite link, no email delivery, and the advice *"The renter should change their password after first login"* is not enforced. Close that dialog and the password is gone: nothing on the Renters screen can re-issue or reset it. In practice every renter password gets pasted into WhatsApp, and a manager who mis-clicks has locked the tenant out with no self-service path. | Renters → Add Renter → portal dialog |
| 8 | S05 | P1 | BUG | **A renter cannot be opened — the only row action is dead.** "View" in the Renters table is a plain `<span class="text-xs font-semibold text-primary">` with no anchor, no button and no click handler; clicking it does nothing at all. There is no `/dashboard/renters/[id]` route in the app. So there is no renter detail anywhere: no leases, no ledger, no documents, no contact history, no credential reset. This is the tenant-facing half of a property-management product and it has no detail view. | Renters table; DOM inspection of the "View" cell |
| 9 | M03 | P1 | BUG | **A post-dated cheque can be banked before its own cheque date.** Collection → select → *Deposit Batch* accepts any deposit date. I deposited five cheques on **02/10/2024**, two of which were dated 01/11/2024 and one 01/12/2024 — all accepted silently, no warning, no rejection. A UAE bank refuses a PDC presented before its date, so the register now shows cash-in-transit that could not exist, and the PDC→Bank journal is dated before the instrument was payable. The validation exists nowhere: not on the row action, not on the batch dialog. | Collection → Deposit Batch, deposit date 2024-10-02 on cheques 300201 / 400302 / 400303 |
| ~~10~~ | M03 | P3 | UX (mitigated) | **Downgraded by the #9 fix.** Batch deposit still takes one date for the selection, but now that a post-dated cheque cannot be banked before its date (#9), a batch on date D simply *refuses* any row not yet due and names it — so the old hazard (sweeping a future cheque into today's batch) is gone. What remains is only the minor convenience of not being able to bank two months' cheques on their own dates in one pass. |
| 10 | M03 | P3 | UX | **Batch deposit forces one date across the whole selection.** The dialog takes a single *Deposit Date* for every cheque selected, so a manager banking October's and November's cheques in one pass has to either run two batches or stamp the wrong date on half of them. Combined with #9 (no date validation) the wrong date is the path of least resistance. | Collection → Deposit Batch (5 cheques, one date field) |
| 11 | M03 | P2 | BUG | **The Clear dialog's date field is labelled "Deposit Date" but sets the clearing date.** Clearing cheque 400301 with that field set to 05/10/2024 produced **CRT-24/1 dated 05/10/2024** — the bank-credit journal. The cheque had already been deposited on 02/10/2024. An accountant reading "Deposit Date" on a clear action reasonably thinks they are confirming or restating when it went to the bank, when they are in fact choosing the value date of the Bank Dr / PDC Cr entry. Wrong label on the field that decides which period the cash lands in. | Cheque Register → Clear → field label vs `CRT-24/1` doc date |
| 12 | M05 | P1 | UI-GAP | **A penalty can never be raised by a human — only by the auto-propose rule.** `/dashboard/finance/penalties` contains four status tabs (Proposed / Approved / Waived / Reversed) and *no create action of any kind*. The backend exposes `POST /api/v1/penalties` (propose) to `PROPERTY_MANAGER` and above, but nothing in the UI calls it. So a landlord cannot charge for the first bounce (below the configured threshold), cannot charge late payment outside the automatic grace rule, and cannot charge for damages, noise, unauthorised pets or parking abuse — the ordinary reasons fines get issued. The whole penalty module is reachable only when a cheque fails often enough. | `/dashboard/finance/penalties` — DOM shows only the 4 tab buttons |
| ~~13~~ | M10 | — | **PARTLY RETRACTED** | Investigation: approving a penalty posts `PEN` (Dr Rent Receivable / Cr Penalty income — correct) and then, **by design**, routes the amount through a *collection row* so the fine flows through deposit/clear like any collectible (`PenaltyAssessmentService.approve` → `chequeService.addCollectionRow`; the code says so: "the same door the settlement's balance-due row uses"). So the accounts are not misstated — the money genuinely is a receivable in the collection pipeline. My "overstates both lines" framing was wrong. The real residue is naming (see #14). |
| 13 | M10 | P2 | BUG | **Approving a penalty books it into "PDC Receivable" and invents an instrument for it.** Approving Fatima's 750 signature-mismatch fine posted `PEN-25/1` (Dr Rent Receivable / Cr Cheque Return Penalty — correct) and then `PDR-25/3` (Dr **PDC Receivable** / Cr Rent Receivable), and created a row in the Cheque Register: **`#7`, 10/04/2025, 750.00, mode Cash, REGISTERED**. No such instrument exists — the renter never handed over anything for the fine. So "PDC Receivable" (post-dated cheques *held from tenants*) is overstated by every unpaid penalty, ordinary receivables are understated by the same amount, and the register shows a mystery line with no cheque number that a manager cannot reconcile to any physical cheque. | Fatima's lease journals + Cheque Register row `#7` |
| 14 | M10 | P3 | UX | **The auto-created penalty instrument is labelled `#7`** — a bare sequence number in the *Cheque No* column, mode "Cash" in a register of cheques, with nothing tying it visually to the penalty that created it. Alongside #13 this makes the register impossible to tick back to a cheque book. | Cheque Register, row `#7` |
| 15 | M07 | **P1** | MISSING | **A live contract cannot be changed once any cheque has cleared — so "tenant buys parking mid-year" is impossible.** Ahmed asked for a bay in Feb 2025. *Amend Lines* opens read-only with: *"Cheque 200101 is Cleared. Amend is only possible while every cheque is still Registered."* Every charge row is disabled and there is **no "Add line" button at all**. Since the first cheque of any real tenancy clears in month one, this means no lease can ever be varied after it starts: no mid-term parking or storage, no added chiller/DEWA recharge, no negotiated reduction, no correction of a mis-keyed charge. The only routes left are terminating and re-issuing the contract, or a hand-written journal that leaves the lease document lying. This is the single biggest functional gap found so far. | Ahmed's lease → Amend Lines, warning banner + all inputs `disabled` |
| ~~16~~ | M07 | — | **RETRACTED** | I reported that the blocked AMEND LINES button stayed enabled and did nothing. That was a measurement error: my DOM query matched the page-header *Amend Lines* trigger, not the dialog's confirm button. `LeaseDialog` renders `disabled={confirmDisabled || busy}` natively and `AmendLinesDialog` passes `confirmDisabled={!!blocker || …}`, so the button *is* properly disabled — and `AmendLinesDialog.test.tsx:93` already asserts exactly that. No defect. | `AmendLinesDialog.tsx:95`, `LeaseDialog.tsx:83` |
| 17 | M07 | **P1** | MISSING | **Nothing can ever be *added* to a posted contract — amendment only redistributes.** Even on Sara's lease, where no cheque had cleared and *Add line* was available, adding a 6,000 Parking Fee produced: *"Cheques total 132,000.00 but contract value is 138,000.00 — An amendment redistributes the contract value between the lines; it cannot re-price the contract. **Change the cheque grid first**, or keep the total as it is."* But the cheque grid on a posted lease offers only **Deposit / Details / Cancel** per row and *Bulk upload cheques* — there is no add-cheque or edit-grid action anywhere. So the instruction points at something the UI cannot do. Net effect: the contract value of a posted lease is frozen for life. Selling a tenant a parking bay is only possible by taking 6,000 *away* from rent, which is not what happened commercially. Together with #15 this means mid-term upsell — parking, storage, an extra chiller line — has no path at all. | Sara's lease → Amend Lines (error banner) + posted cheque-grid actions |
| 18 | M04 | P2 | MISSING | **A maintenance ticket cannot be dated — it is always "now".** The Create Ticket form has Title, Description, Property, Unit, On-behalf-of, Category, Priority and attachments, but **no received/reported date**. The ticket I raised for Rajesh's December-2024 AC fault was stamped 23/09/2026. So complaints taken by phone or WhatsApp and logged later, and any history migrated from another system, all collapse onto the day they were typed — destroying SLA measurement and the maintenance history a landlord needs at renewal or in a dispute. | Tickets → Create Ticket; resulting row created `23/09/2026` |
| 19 | M04 | P2 | UX | **"On behalf of" is free text, not a renter link.** The field is captioned *"Renter name if reporting on their behalf"* and accepts any string — I typed "Rajesh Kumar" and it was stored as a label. It is not tied to the renter record, so a phoned-in complaint never reaches that renter's history, cannot notify them, and is vulnerable to spelling variants creating invisible duplicates. Most complaints in a UAE building arrive by phone or WhatsApp to the manager, so this is the *normal* path, not the exception. | Tickets → Create Ticket → `on behalf of: Rajesh Kumar` rendered as plain text |
| 20 | M04 | P3 | UX | **Tickets have no human reference number** — the list shows the raw UUID prefix `82dd7a2e`. Staff and tenants need a quotable reference (TKT-2025-014); a hex fragment is unreadable over the phone and not obviously a ticket number. | Tickets list, ID column |
| 21 | M04 | P2 | BUG | **The same instant is displayed as two different dates — some screens render UTC, others Gulf time.** The ticket's Activity History reads `23/09/2026, 03:44:21` (correct local time), while the lease header says *"Posted 22/09/2026"* and the penalty list says *"Proposed at 22/09/2026"* for actions taken in the same session. Dubai is UTC+4, so anything done between midnight and 04:00 Gulf time is stamped a day earlier on the screens that render UTC. Every night-shift action in a UAE building lands on the wrong day, and two screens disagree about the same record. | Ticket activity history vs lease header vs penalties list, same session |
| 22 | M04 | P2 | UX | **A resolved ticket can only be closed with an OTP the renter receives** — the sole action is *"CLOSE WITH OTP"* + a 6-digit box, with no manager override and no alternative evidence path. When the tenant has moved out, is unreachable, will not answer, or simply never replies, the ticket cannot be closed at all; the only escape is switching the org-wide *Ticket Closure OTP* setting off for everyone. A per-ticket "close without OTP, with reason" is the normal escape hatch and is missing. | Ticket → Resolved → Actions |
| 23 | M14 | P2 | UX | **Renewal silently copies one-off charges into the new term.** *Renew* offers only "Copy the charge lines from this contract", which brought Ahmed's **Admin Fee 1,500** — a one-time contract-administration charge — into year two. Nothing flags it as non-recurring. A landlord who does not notice re-bills the signing fee every single year, and the tenant is charged for administering a contract that was administered once. | Ahmed's renewal draft, lines copied = Rent + Admin Fee |
| 24 | M14 | P2 | MISSING | **No rent escalation on renewal.** The Renew dialog has contract/start/end dates and two checkboxes — there is no percentage uplift, no new-rent field, and no reference to the RERA rental index that governs permitted increases in Dubai. Getting Ahmed to +8% meant opening the saved draft, expanding *Edit draft*, retyping 91,800 by hand and regenerating the cheque grid. Renewal *with an increase* is the normal case in the UAE, and it is the one the screen does not help with. The new **Ejari #** is also left blank with no prompt, though a renewal legally needs a fresh registration. | Renew dialog fields vs the manual edit required |
| 25 | M16 | P2 | BUG | **The finalized settlement names a UUID, not a person.** The locked move-out statement reads *"Finalized 15/10/2025 by `09944330-ac54-48f8-8ab0-c9087af06b52`."* This is the audit line on a legal financial document that gets shown to a departing tenant and may be produced in a rental-dispute case; it must name the user. | Settlement page after Finalize, `STL-25/1` |
| 26 | M15 | P2 | UX | **The termination screen says returning a cheque stops the debt — it does the opposite.** Help text on *Uncleared instruments*: *"Returned rows have their registration reversed and **the money stops being owed**."* Flipping cheque 300203 (15,500) to *Return* moved **Receivable after** from 17,734.25 **up** to 33,234.25 — because reversing the PDR moves the debt from PDC Receivable back to plain Rent Receivable. The behaviour is right; the sentence tells the operator the opposite of what the number does, on the screen where they decide what to hand back. | Terminate screen, Return/Keep toggle on 300203 |
| 27 | M15 | P2 | MISSING | **"Give notice" records no date and no party.** The dialog explains it marks the contract `NOTICE_GIVEN`, but there is no notice-date field and no distinction between the *renter* giving notice and the *landlord* serving one. In the UAE these are different instruments with different legal weight — a landlord's eviction notice must be notarised and served 12 months ahead, and notice periods are counted from a date. The product stores neither, so it cannot evidence that notice was validly served. | Lease → Give notice dialog |
| 28 | X01 | P3 | UX | **The renter portal opens for non-renters in a broken state.** `/dashboard/renter-portal` loaded as SUPER_ADMIN renders *"Welcome to your Renter Portal, System Admin"*, a red *"Couldn't load this. Please try again."* banner and *"NO LEASES FOUND"*. No other tenant's data leaks, but the route should redirect or refuse rather than render a personal portal for a user who has no tenancy. | `/en/dashboard/renter-portal` as SUPER_ADMIN |
| 29 | X02 | P3 | UX | **Pagination is untranslated in Arabic.** `/ar/dashboard/leases` mirrors correctly and translates headings, actions and lease statuses (ساري / منتهي / مجدد), but the footer still reads *"Showing 1-5 of 5"* and *"per page 25"* in English and in LTR word order. It appears on every list screen in the product. | `/ar/dashboard/leases` footer |
| ~~30~~ | M16 | — | **PARTLY RETRACTED** | Same mechanism as #13: the settlement balance-due row is deliberately created via `addCollectionRow` (`SettlementService:666`) so the amount is collectible through the register. Not a balance bug; the concern is the account name (#14). |
| 30 | M16 | P2 | BUG | **Settlement balances are also parked in "PDC Receivable", leaving Rent Receivable at nil while a tenant still owes money.** Finalising Fatima's statement posted `PDR-25/12` *"Settlement balance due" 5,234.25*, moving the debt out of Rent Receivable into PDC Receivable — the same pattern as the penalty in #13. The closing trial balance shows **Rent Receivable 505,550.00 Dr / 505,550.00 Cr = nil** even though 5,234.25 + 750 is genuinely outstanding, while PDC Receivable (*post-dated cheques held from tenants*) carries 345,034.25 including amounts backed by no cheque at all. Any balance sheet or aging report drawn off these two accounts misstates both lines, and an auditor reconciling PDC Receivable to the physical cheque book will not tie. | Closing trial balance + `PDR-25/12` |
| 31 | M09 | **P1** | MISSING | **The seeded chart has no usable expense accounts, so no supplier invoice can be coded.** Filtering the chart to EXPENSE returns **6 rows, of which only 3 are postable**: `D-02-001 Rounding Off`, `D-02-002 Discount Allowed`, `D-02-003 Bank Charges`. `D-01 Direct Expense` describes itself as *"Building running costs, one leaf per property per category"* and contains **no leaves whatsoever**. There is no account for maintenance, repairs, cleaning, security, chiller/DEWA, insurance, service charge or management fees. On the Purchase/Service Invoice screen the account lookup returns "—" for *Repairs* and for *Maint*, so the AC repair for A-103 cannot be entered at all. Income leaves per property *are* generated (Rental Income, Admin Fee, Parking, Maintenance Charges, three penalty accounts) — the expense side of the same template was never built. Consequence: every NOI, P&L and expense report shows income only, and the whole accounts-payable module is unreachable on a fresh tenant until someone hand-builds a chart of accounts. | Chart of Accounts → EXPENSE filter (6 accounts); Purchase Invoice account lookup |
| ~~32~~ | — | P3 | BUG (fixed) | **A flaky web test — root-caused to an order-dependent mock, not load.** `preview.test.tsx > ignores a pricing that the user's edit has already superseded` timed out on `terminate-receivable-after` under full-suite parallelism and passed in isolation. It was not slow rendering: the mock queued two responses by *call order* (`mockImplementationOnce`/`mockResolvedValueOnce`), and under the suite the mount re-fired `price(today)` a variable number of times, so an extra call consumed the queued answer and left `preview` null — the receivable row never rendered. Fixed by keying the mock on the requested *date* instead of call order, and flushing the late superseded answer inside `act`. Also capped the worker pool (`maxWorkers: 4`), which separately fixed real worker-spawn exhaustion at 10 forks. Verified 6× full-suite: 982 passed every run, 0 spawn failures. | `preview.test.tsx`, `web/vitest.config.ts`, `web/src/test/setup.ts` |
| ~~33~~ | — | P2 | infra (root-caused + fix proven) | **Diagnosed and fixed by pattern.** 117 IT classes each start their own Postgres container with no reuse, so the container starts, the migrations run and the Spring context loads 117 times (~3 min/class = the ~6 h). A shared-container base (`SharedPostgres` + `AbstractPostgresIT`) collapses that to one container, one migration and one cached context; proven on three converted classes (1 container, 1 `HikariPool`, ~16 s/class). Full recipe and measurements in `FIX-33-slow-suite.md`; the 114-file rollout is a reviewable follow-up (can't verify a 6 h run in-session). |
| 33 | — | P2 | BUG | **The full backend suite is pathologically slow on this machine** — killed at 6 hours against a 19-minute baseline for the same 2,301 tests, with the Gradle worker alive and inching forward. The 15 cheque/account suites run in 3m23s. Whatever stalls the full run is independent of this branch's changes and will hurt CI. | `./gradlew test` vs `--tests` subset |
| 34 | S05 | **P0** | BUG | **Every renter portal password was derived from the renter's id and therefore guessable.** `RenterService.createRenter` generated `"Renter@" + renterId.substring(0, 6)`. I noticed it because Ahmed's dialog showed `Renter@69e7b1` and his renter id is `69e7b1cf-…`. A renter id is **not a secret** — it comes back from `GET /api/v1/renters`, populates the tenant-ledger picker, and travels in listing payloads — so anyone who can see a renter can compute that renter's portal password and sign in as them, reading their ledger, payment history and personal details. There is no forced rotation (#7), so **every portal account ever created by this product is still guessable until its password is changed by hand** — including the Miftah Demo and Al Ashram demo renters on production. | `RenterService.java:67` (before fix); `Renter@69e7b1` vs renter id `69e7b1cf-1556-…` |

## Fixed on `fix/miftah-2y-simulation-gaps`

| # | What changed | Tests |
|---|---|---|
| 3 | The staff-provisioning role list is now **derived from `ROLE_RANK`** (`PROVISIONABLE_ROLES` in `web/src/lib/rbac.ts`) instead of being hand-copied into the page, so `ACCOUNTANT` appears and no future role can go missing from the form again. `assignableRoles` still keeps it out of a PROPERTY_MANAGER's reach. | `web/src/lib/__tests__/provisionable-roles.test.ts` — 11 tests |
| 9 | **A post-dated cheque can no longer be banked before its date.** `requireNotPresentedEarly` guards both the single deposit and the batch; the batch names the offending row and banks nothing. A cut-over `Replay` is exempt, because history has to stay reproducible. | 3 new tests in `ChequeServiceIT`; 5 existing tests updated — they had encoded the old behaviour by banking the whole grid on one early date |
| 31 | **Per-property expense leaves are now generated** under `D-01 Direct Expense` — Repairs & Maintenance, Cleaning, Security, Utilities, Insurance, Management Fees — so a supplier invoice can be coded on a fresh tenant. Idempotent, and skipped quietly for a tenant whose chart has no `D-01`. | 2 new tests in `PropertyAccountServiceIT` |

| 11 | **The Clear dialog's date is labelled "Clearing Date"** instead of "Deposit Date", and a cash receipt says "Receipt Date" (`ReceiveCashDialog` had the same mislabel). That field sets the value date of the CRT, i.e. which period the cash lands in. | en + ar locale keys; web typecheck + 259 component tests |
| 26 | **The termination help text now matches the arithmetic** — "Returning a row reverses its registration and hands the instrument back — the amount goes back to rent receivable and is still owed." Previously it claimed the debt disappeared while the figure on screen rose by the cheque amount. | en + ar locale keys |

Committed as `a52eda2f` on `fix/miftah-2y-simulation-gaps`. Verification run:
**320 tests** across the 15 cheque/account suites, the **golden ledger gate
(16 tests, 0 failures — both PACT replays still tie row by row)**, web typecheck
clean, **259** component tests.

| 6 | **Units are ordered by unit number** — `UnitRepository.findByPropertyId` carried no `ORDER BY`, so creating A-101…A-302 in sequence listed back as A-101, A-201, A-102, A-103, A-203, A-202, A-301, A-302. | `UnitOrderingIT` — creates them shuffled, asserts the read-back order |
| 29 | **The pagination footer is translated.** `Showing {start}-{end} of {total}` and `{count} per page` now come from the `Common` namespace in both locales; they were the last English text on an Arabic page and appear under every list in the product. | web suite (977 passing); the notifications test's `next-intl` mock had to answer `useTranslations` and interpolate like the real one |

| 34 | **Portal passwords now come from `SecureRandom`** — 12 characters of a 55-character alphabet (~58 bits), with look-alike glyphs removed because these still get read out over the phone. | `RenterPortalPasswordTest` — 4 tests; 2 of them fail against the old scheme, verified by reverting |

| 5 | **Rent fields now carry a period** — the unit and property forms' *Expected Rent* / *Actual Rent* labels read "(AED/year)" in both locales, and the misleading `e.g. 5000` placeholder (which reads as a monthly figure) is now `e.g. 85000`. The model stores rent annually — `revenueAtCapacity` sums `expectedRent` directly and showed AED 85,000 against Ahmed's annual line — so "per year" is the correct label, not a guess. | web typecheck clean; no test asserted the old label |

| 25 | **The finalized settlement now names the settler**, not a UUID. The mapping resolved the name with the *tenant-filtered* `findById`, which cannot see a SUPER_ADMIN acting inside a pivoted tenant (`tenant_id = NULL`) — so a legal move-out document showed a raw id. Swapped to the native `findDisplayNameById`, purpose-built for cross-tenant audit attribution. | `SettlementServiceIT.finalizeCarriesTheSettlersNameOnTheStatement` — guards that the name is populated; see its scope note on why the harness cannot reproduce the tenant-filter half |

| 21 | **Timestamps now render as their local date everywhere.** `fmtIsoDate` sliced the first ten characters — the UTC calendar date — off every value, so a lease's `postedAt` (an `Instant`) showed 22/09 on the header while the ticket history showed 23/09 for the same action. It now converts a full timestamp (one containing `T`) to the viewer's local date, and keeps the parts-based path for bare `yyyy-MM-dd` values so pure dates still can't shift a day. One function, so all 45 call sites are corrected at once. | 4 new `fmtIsoDate` tests in `leaseMath.test.ts`; full web suite 981 passing |

| 14 | **Cash-mode collection rows no longer show a fake cheque number.** The register printed `#7` (the sequence) in the *Cheque No* column for the collection row a penalty or settlement balance creates — reading as "cheque number 7", an instrument no cheque book holds. A non-PDC row now shows a dash there; the Mode and narration columns already say what it is. A numberless *PDC* row still shows `#seqNo` as a legitimate "awaiting its number" placeholder. | register tests (16 passing); typecheck clean |

**On #13 / #30 — a correction after investigation:** routing penalties and settlement balances through the
collection register (and thus PDC Receivable) is intentional — they are amounts to be collected from the
tenant, so they enter the same deposit/clear pipeline as cheques. I originally logged this as a
balance-misstatement bug; that was a misread. The genuine, narrow issue was the account's **name** ("PDC
Receivable / post-dated cheques held from tenants" understates what it holds) and the `#N` instrument
label (#14, now fixed). Renaming the seeded account is a data/naming decision left for the owner.

| 18 | **A ticket can now be dated.** `maintenance_tickets` gains a `reported_date` (migration 90, backfilled from `created_at`, NOT NULL), the Create Ticket form has a *Reported On* date field (defaulting to today, capped at today), and the service refuses a future date. So a complaint taken by phone and logged days later keeps the day it was actually raised, and SLA/history measure from the right date. Shown on the ticket detail page too. | 3 new service tests (given date / default today / future refused); migration applies clean under the ticket ITs |

**Remediation still required (operational, not code):** existing renter accounts keep their old derived
passwords. Every portal password created before this change should be rotated — on the Miftah Demo tenant,
the Al Ashram demo tenant, and any live customer. The fix stops new accounts being guessable; it does not
retro-fix the ones already out there.

**Note on the expense categories:** the six names above are a starting set, not a
decision. They are plain leaves carrying no `AccountRole`, so nothing posts to them
automatically and the list can be changed freely — but it is the chart every new
property will inherit, so it is worth a minute of your time.
