# SDD ledger — plan: docs/superpowers/plans/2026-09-17-accounting-v2-plan3-recognition-termination-settlement.md
Spec: docs/superpowers/specs/2026-09-17-accounting-v2-design.md (§8, §9.1, §9.2, §11). Depends on plans 1+2 (stacked, not merged).
Ruling: branch feat/accounting-v2-recognition-settlement is cut from feat/accounting-v2-lease-posting AFTER plan 2's final fix wave (plan's "from main after Plan 2 merges" superseded by Kunal's "stack them" directive 2026-09-18); PR base = plan 2 branch — cost if wrong: rebase onto main after plan 2 merges.

## Pre-flight scan (2026-09-21, Mac)
| Pair / task | Produces vs consumes | Finding | Ruling |
|---|---|---|---|
| Plan 2 → T3 | LeasePostedEvent(tenantId,leaseId,contractDate), LeaseAmendedEvent(tenantId,leaseId,reversedJournalId,newJournalId), LeaseExtendedEvent | all three records exist in core/service/lease | T3 brief: verify the events are published INSIDE the posting transaction before relying on a same-tx @EventListener |
| Plan 2 → T5 | ChequeService.returnToTenant(UUID, LocalDate, String), cancel(UUID, ChequeActionRequest), revertOnlinePending(UUID) | exist with those signatures (ChequeService:512/533/674) | — |
| Plan 2 → T6 | ChequeService.addRowToPostedLease(UUID, ChequeRowInput); PenaltyAssessmentService.outstandingForLease | exist; addRowToPostedLease may refuse a TERMINATED lease | T6 brief: check the status guard; if it refuses TERMINATED, widen it for the settlement collection row only (internal overload), never from the user-facing path |
| Plan 1 → all | JournalDocType CIL/TCR/STL, JournalSourceType RECOGNITION/SETTLEMENT, LeaseStatus CLOSED, ClockConfig, ShedLockConfig, LeaseTestFixtures | all exist | — |
| T1 changeset 85 | addUniqueConstraint columnNames quoted | OK (the 81/83 truncation bug does not recur) | — |
| T2 self-consistency | interface lists truncate(List<Slice>, LocalDate); Step 2 corrects to truncate(List<Slice>, BigDecimal dayRate, LocalDate) | plan corrects itself | Step 2's 3-arg signature is the one; the test calls it with the segment's 6dp rate |
| T2 numbers | 114 d × 139.726027 = 15,928.77; Σ first four = 13,832.88; slice = 2,095.89 | re-derived by controller: correct | — |
| T5 numbers | "Feb 1–15 entry of 2,095.89 (20,260.27 − 18,164.39)" | WRONG by one fil: 20,260.27 − 18,164.39 = 2,095.88 (earnedThrough − Σ previous, the very rule T2 mandates) | implementer asserts 2,095.88 and re-derives every Task 5/6 figure with ProrationEngine, as the plan's own self-review instructs |
| T6 numbers | "received 28,500" vs receivedTotal = Σ CLEARED cheques (2,000 admin + 2×12,750 + 3,000 SD = 30,500) | inconsistent with its own definition | definition wins: Σ CLEARED cheques; expected value re-derived from the fixture. netRefund 8,239.73 and −1,760.27 re-derived: correct |
| T3 ↔ T5 | T5 adds RecognitionService.truncateForTermination | consistent (T3 creates, T5 extends) | — |
| T3 runTo | per-entry REQUIRES_NEW inside runTo | self-invocation bypasses the proxy | separate bean (RecognitionPoster) or TransactionTemplate; every service method @Transactional (plan 2 T3 ruling: TenantAspect enables the filter only inside a tx) |
| T4 | "pattern: PenaltyService.calculateDailyPenalties" | v1 cron was disabled in plan 2 T9 and the class retired in T12 | use the surviving per-tenant job pattern (cheque reminder / LeaseExpirationJob) |
| T6 ↔ T7 | both describe "CLOSED when the collection row later clears" hook in ChequeService.clear | duplicate | T7 owns the hook (clear + receive + clearOnline); T6 sets CLOSED only at finalize when netRefund ≥ 0 |
| T6 ↔ plan 2 T12 | SettlementDepositLedgerTest "replaced"; SettlementService already reads ledger deposits (plan 2 T12) | test may already be gone | delete-if-present; T6 is a rewrite of the T12 state, not of v1 |
| T6 ↔ plan 2 T9 ruling | penalty approve on a non-live lease says "charge this penalty through settlement" | T6 rejects category PENALTIES (already in receivable) | consistent: a post-termination charge is an EARLY_TERMINATION_FEE/OTHER deduction line; the 400 text for PENALTIES must say so |
| T7 | POST /{id}/notice "exists? if not add" | implementer checks | — |
| T8 | leasing.ts, MvpSidebar, rbac shared with plan 2 web | additive | UI standard: table-first, pagination, createdAt asc; the "UI offers what the server refuses" discipline (plan 2 ledger) is a review lens for T8 |
| T9 | plan predates Kunal's walkthrough directive | — | T9 adds web/walkthrough/accounting-v2-plan3.{config,spec}.ts, one video per scenario, run on the local stack (stub gateway flag on), recording pass at default pacing; PR opened by controller after final review, base = plan 2 branch |
Carried in from plan 2: unit.current_lease_id (spec §6.4 note) → decide in T5/T7; settlement arrears register-based → T6 replaces; REFUNDED is a pinned contract with no flow (not plan 3 scope unless spec §9.3 says so); deposit held = ledger balance (done in plan 2 T12, T6 keeps).
Branch feat/accounting-v2-recognition-settlement cut from feat/accounting-v2-lease-posting @ ea4a205c (PR #288 open). Task 1: BASE ea4a205c — dispatching sonnet implementer (plan text carries the complete changeset).
Ruling: Tasks 1 and 2 are dispatched as ONE sonnet implementer doing them sequentially (two commits) and reviewed as one unit — both are transcription from complete plan text on disjoint files — cost if wrong: a finding in one delays the other's completion line. truncate() binding signature = (List<Slice>, BigDecimal dayRate, LocalDate) per the plan's own Step 2 correction.
Tasks 1+2: implemented (sonnet) commits 498621d3 (changeset 85 + RecognitionSchemaIT 6), eb3b6069 (ProrationEngine + 8 tests); targeted run 27/27. Implementer found the brief's truncate fixture cannot discriminate the right rule from the wrong one (both 2,095.89) and added a 2027-06-10 case (1,397.25 vs 1,397.26), mutation-checked. Concern: truncate() edge branches (cutoff before start / at-or-after end) untested → carried into T5's brief. Review dispatched (sonnet) ea4a205c..eb3b6069.
Ruling (T3): LeaseAmendedEvent is widened to carry List<UUID> reversedJournalIds (plan 2's fix wave made amend reverse the original TCO AND extension TCOs; no consumer exists yet) — T3 owns the change since it writes the first consumer — cost if wrong: one record signature.
Task 3: BASE eb3b6069 — dispatching opus implementer (parallel with the T1+2 review; disjoint files).
Tasks 1+2: review (sonnet) — Task 1 spec ✅; Task 2 spec ❌ Needs fixes. C1 ProrationEngine.truncate() throws IllegalArgumentException when lastDay == an intermediate slice's periodEnd (a month-end termination) — reproduced by the reviewer with a standalone javac run. I1 truncate boundary coverage thin (cutoff before start, at/after end, on a slice boundary). ⚠️ resolved by controller: (1) test counts are the implementer's XML-read counts, re-confirmed by T3's full-suite run; (2) FK constraints unexercised → confirmed a real (small) gap, joins the fix round as one negative insert per new FK; (3) fixture helper built empirically → accepted.
Tasks 1+2: minor (deferred): RecognitionSchemaIT.chargeType() inserts role "RENT_INCOME" (real enum: RENTAL_INCOME) — folded into fix round 1 since the file is open.
Tasks 1+2: fix round 1 QUEUED (resume the sonnet implementer) until Task 3's implementer returns — one Gradle at a time, sequential implementers; T3 uses slice(), not truncate(), so it does not build on the defect; T5 does and must not start before this lands.
Task 3: implemented (opus) commit 0d52c7c9; backend 1686/0 (181 classes; +9 RecognitionServiceIT); three guards mutation-checked. NEW changeset 86 (rent_segments.lease_line_id nullable, ON DELETE SET NULL) because amendLines→applyLines deletes+flushes lines before LeaseAmendedEvent fires and fk_rs_line killed every amend — the plan reserved 86 for exactly such a fix-up. Extension fixture re-derived: 99 days → 4 entries (plan's "3" wrong, as pre-flight suspected). Fixed stale DTOs out of runTo (REQUIRES_NEW poster). 
Ruling: 86's SET NULL is accepted over cascade-deleting recognition history or rewriting applyLines — history must survive an amend; the residual risk (a caller deleting a posted lease's lines without cancelling segments) is carried into T5/T6 briefs as "cancel/truncate segments first" — cost if wrong: an orphaned ACTIVE segment with a null line, detectable by query.
Ruling: rebuildAfterAmend reversing at today and refusing the amend when today is locked stands (a lock on the current day is an explicit accountant act); the amend dry-run not predicting it → deferred minor for the final review.
Task 3: minor (deferred): RecognitionRunResult has no separate "skipped (locked)" list — T4 decides when it builds the run page contract. Task 3: minor (deferred): amend ITs adjust cheque amounts with raw JDBC — T5/T6 must not copy.
Task 3: review dispatched (opus) eb3b6069..0d52c7c9. Tasks 1+2: fix round 1 dispatched (resume sonnet implementer) FIX_BASE 0d52c7c9.
Task 3: review (opus) spec ✅ / quality Needs fixes — C-1 RecognitionPoster re-checks status with NO row lock → nightly job + manual run can double-post one CIL (journal source index is non-unique); I-1 runTo holds an outer tx + a REQUIRES_NEW connection per entry (pool starvation); I-2 failure-isolation path untested (collapsing the poster into the service stays green); I-3 "debit follows the line's creditAccountId" untested; I-4 changeset 86's rollback DELETEs the very history it preserves. 7 minors in task-3-review.md. ⚠️ resolved by controller: 1-4 rest on the implementer's XML-read 1686/0 (accepted; the next full-suite run re-confirms); 5-6 → the fix round adds a schema assertion for 86 and an amend IT that loads the segment before the delete.
Ruling: changeset 86 is REWRITTEN (unreleased, hours old) to the reviewer's smaller alternative — drop fk_rs_line, keep lease_line_id NOT NULL as a plain UUID (entity maps a UUID column, not a @ManyToOne) — strictly more audit trail (which line a cancelled segment came from survives), no null-line ACTIVE segment is possible, rollback = re-add the FK as NOT VALID (no deletes) — cost if wrong: no referential check on lease_line_id; segments are only ever written by RecognitionService.
Ruling: C-1 fix = PESSIMISTIC_WRITE lockById in the poster's new transaction + status re-check under the lock, PLUS a partial unique index on journal_entries(source_type, source_id) WHERE source_type='RECOGNITION' AND reversal_of_id IS NULL — in changeset 86 (same fix-up changeset) if the journal columns allow it; else lock only and say so.
Task 3: fix round 1 QUEUED (resume opus implementer a810…) behind the Tasks 1+2 fix round (one Gradle at a time).
Tasks 1+2: fix round 1 done (sonnet) commits 008dbd97 (truncate month-end), f43a5b2d (FK negative inserts + role value); targeted 31/31 (ProrationEngineTest 15, RecognitionSchemaIT 7, RecognitionServiceIT 9). CARRY to T5/T6: a cutoff on a slice boundary can give a cut-slice amount one fil off the untruncated slice (2027-01-31: 4,331.50 vs 4,331.51) — earnedThrough is the source of truth; termination must reverse+repost a POSTED entry containing T even when T == periodEnd if the amounts differ, and settlement derives earned rent from POSTED entries AFTER truncation, never from an assumed match. Re-review dispatched (sonnet) 0d52c7c9..f43a5b2d. Task 3: fix round 1 dispatched (resume opus) FIX_BASE f43a5b2d.
Tasks 1+2: fix round 1/5 (3 addressed, 0 open Critical/Important; commits 0d52c7c9..f43a5b2d) — re-review (sonnet) probed 368 cutoffs standalone: no throws, Σ == earnedThrough everywhere, the one-fil boundary drift confirmed as the contract's consequence. Residual: fk_re_lease (recognition_entries.lease_id) missing from foreignKeysRejectDanglingIds — the controller's fix message omitted it from the list; handed to the T3 fix round, which is editing that test anyway.
Tasks 1+2: complete (commits ea4a205c..f43a5b2d, review clean after 1 fix round; fk_re_lease case rides with T3's fix round).
Task 3: fix round 1 done (opus) commit cf46f296; backend 1699/0 (181 classes); 4 new guards mutation-checked. C-1 lock only, NO partial unique index: a reversal carries source_type='REVERSAL' + the original journal's id, so the reviewer's predicate is a no-op that would block T5's reverse+repost; a status predicate misfires on Hibernate's insert-before-update flush order.
Ruling: C-1 ships lock-only for now; T5 is told to create a NEW recognition_entries row for a truncated replacement (never reuse the reversed row's id as source_id) — then UNIQUE(source_id) WHERE source_type='RECOGNITION' becomes a safe backstop, added by T5 in changeset 87?? NO — plan 4 owns 87/88 → T5 adds it to changeset 86 in place (unreleased fix-up changeset) — cost if wrong: the row lock remains the only guard against a double CIL.
CARRY to T4: runTo is NOT transactional — job/controller must not wrap it in @Transactional; RecognitionPoster.post returns RecognitionEntryDTO; T4 owns deferred minors M-5 (preview counted as posted) and M-6 (locked vs failed split in RecognitionRunResult). CARRY to T5/T6: RentSegment.getLeaseLineId() (plain UUID).
Task 3: re-review dispatched (sonnet) f43a5b2d..cf46f296. Task 4: BASE cf46f296 — dispatching opus implementer (parallel with the re-review; T3 fixes, if any, applied after T4 returns).
Task 3: fix round 1/5 (5 addressed + 3 asks, 0 open; commits f43a5b2d..cf46f296) — re-review (sonnet) clean; the lock-only C-1 decision independently verified against PostingService.reverse.
Task 3: complete (commits eb3b6069..cf46f296 [0d52c7c9, cf46f296], review clean after 1 fix round). Deferred minors: M-1 (first consumer of reversedJournalIds), amend dry-run does not predict a locked "today"; M-5/M-6 owned by T4.
Task 4: implemented (opus) commit 01b32124; backend 1716/0 (183 classes; +12 RecognitionControllerIT, +5 RevenueRecognitionJobIT); 4 mutants killed (future-date rejection, PM gate, per-tenant context, LeaseAccessPolicy on the lease schedule). Run contract: preview, posted (0 on preview), wouldPost, amount, entries[], skippedLocked, skippedLockedEntries[], booksLockedThrough, failed, errors[]. GET /pending also rejects a future `to` (stricter than brief — Ruling: accepted; the month-end page never needs a forward list, the lease tab shows the future schedule). runFor(LocalDate) is the unlocked entry point; run() is ShedLocked even on direct calls.
Ruling: the nightly job gets a kill switch like its siblings — `rentaxis.recognition.job.enabled` (default true) — a job that writes journals unattended must be stoppable without a deploy; rides with T4's fix round if the review opens one, else a one-line resume — cost if wrong: one unused property.
Task 4: review dispatched (sonnet) cf46f296..01b32124. Task 5: BASE 01b32124 — dispatching opus implementer (parallel with the T4 review).
Task 4: review (sonnet) spec ✅ / quality Approved with 1 Important — SUPER_ADMIN calling /run or /pending with no tenant context hits requireTenant() → IllegalStateException → opaque 500 (fails safe, not a leak; untested). Minors: runTenant's catch(Exception) swallows InterruptedException without restoring the flag; JVM-zone "today" on the interactive endpoints (#269, platform). ⚠️ resolved by controller: lockById semantics were verified in T3's re-review; counts are the implementer's XML-read 1716/0; `amount` ambiguity → T8's web test pins it; SA-500 is the Important itself.
Task 4: fix round 1 QUEUED behind Task 5's implementer (one Gradle at a time): (1) SA without a tenant → 400 "Select an organisation first" + IT; (2) kill switch rentaxis.recognition.job.enabled (ruled earlier) + test; (3) restore the interrupt flag.
Task 5: implemented (opus) commit a312fd1d; backend 1727/0 (184 classes; +9 LeaseTerminationServiceIT, +2 RecognitionSchemaIT); 9 mutants killed (one assertion tightened first). 2,095.88 confirmed; month-end POSTED re-cut 4,331.51→4,331.50 covered. 86 edited in place: uq_re_segment_period partial over PLANNED/POSTED + partial unique index journal_entries(source_id) WHERE source_type='RECOGNITION' (the C-1 backstop, now safe). Concerns: mobile lease_service.dart posts {notes} with no date to /terminate → 400 (mobile is frozen → CARRY to plan 5's MOBILE_FINANCE flag task); settlement/finalize no longer terminates (T6's end state, landed early; web flow inconsistent until T6+T8); LeaseService.markTerminated is public (cross-package); a TRUNCATED segment keeps original amount/day_rate while to_date/days move (spec §8.1 identity no longer holds for it) → reviewer to judge; implementation preceded the IT (mutation table is the evidence).
Task 5: review dispatched (opus) 01b32124..a312fd1d. Task 4: fix round 1 dispatched (resume opus T4 implementer) FIX_BASE a312fd1d.
Task 5: review (opus) spec ✅ (Anil's scenario hand-verified: RR −5,239.73, PDC 0, Advance Rent 2,095.88→0, income 18,164.39) / quality Needs fixes — C1 truncation reads/writes entries without lockById and RecognitionEntry has no @Version → terminate racing the nightly run leaves a CANCELLED/re-cut row whose CIL stays POSTED; I1 a TRUNCATED segment keeps amount/day_rate while to_date/days move → ProrationEngine.earnedThrough on it returns 51,000.00 instead of 20,260.27 (landmine for T6); I2 recognition reversals dated T, BEFORE the entries they reverse → as-of-T income reads 12,016.43 instead of 20,260.27; I3 settlement can be FINALIZED on a still-ACTIVE lease; I4 no endpoint tests for terminate/preview role gates (and PM→404 not 403 on the service path). 8 minors in the review file.
Ruling (I1): on truncation the segment becomes self-consistent — amount := earnedThrough(T), to_date := T, days := daysInclusive(from,T); the stored 6-dp day_rate stays the ORIGINAL contract rate and is documented as authoritative (never re-derived from a truncated row); the original contract figures are preserved in two new nullable columns original_amount / original_to_date (changeset 86 in place) — cost if wrong: two columns nobody reads.
Ruling (I2): each recognition reversal is dated max(T, original.entryDate) — a reversal never precedes what it reverses, so any as-of read is right — cost if wrong: reversals of future-dated CILs sit in future periods until those dates arrive (correct: they net to zero on the same day).
Ruling (I3): finalizeSettlement requires the lease to be TERMINATED or EXPIRED (T6/T7 own the expiry path) → 400 "Terminate the lease before settling it" — cost if wrong: T6 widens one status set.
⚠️ resolved by controller: (1)(2) implementer's XML-read 1727/0 accepted; (3) ONLINE_PENDING return path, (4) two-RENT-segment lease terminated mid-term, (5) BOUNCED row in bouncedOutstanding → each becomes an IT in the fix round; (6) C1 gets a concurrency IT; (7) 86 in-place edits are fine on this unreleased stack (dev DBs recreated) → PR deploy note.
Task 5: fix round 1 QUEUED behind Task 4's fix round (one Gradle at a time).
Task 4: fix round 1 done (opus) commit 44bd3081; targeted 22/0 (RecognitionControllerIT 13, RevenueRecognitionJobIT 7, RevenueRecognitionJobDisabledIT 2); 4 mutants killed. Out of scope → issue https://github.com/kunal-ak23/rentaxis/issues/289 (three requireTenant copies). Re-review dispatched (haiku) a312fd1d..44bd3081. Task 5: fix round 1 dispatched (resume opus) FIX_BASE 44bd3081.
Task 4: fix round 1/5 (3 addressed, 0 open; commits a312fd1d..44bd3081) — re-review (haiku) clean.
Task 4: complete (commits 01b32124, 44bd3081; review clean after 1 fix round). Deferred: JVM-zone "today" on the interactive endpoints (#269); shared tenant-less guard (#289).
Task 5: fix round 1 done (opus) commit 5581a0ad; backend 1748/0 (186 classes; +16); test-first with red lines recorded; 9 mutations, 8 killed, 1 honestly reported unreachable (up-front lock check on later reversal dates — books_locked_through is a single high-water date). KEY FINDING: a @Lock query returns the first-level-cache instance when the row is already loaded → lock taken, STALE status returned; fixed with refresh(entry, PESSIMISTIC_WRITE). RecognitionPoster still uses lockById (safe only because it always enters with a fresh persistence context).
Ruling: RecognitionPoster gets the same refresh-under-lock treatment as a rider on Task 6's dispatch (T6's implementer is the next one in the recognition package's neighbourhood; one method + one test) — cost if wrong: a redundant refresh.
Ruling (M2): terminate preview's receivableAfter stays computed from the DEFAULT split; the web page (T8) recomputes it client-side as finance flips rows (receivableAfter ± the row's amount — every row's amount is in the response) and the POST re-validates — cost if wrong: a displayed figure drifts from the server's until submit.
Deadlock edge (EntryNumberService sequence lock vs entry locks) → issue https://github.com/kunal-ak23/rentaxis/issues/290, final-review triage. Mobile {notes}-only terminate contract → plan 5 mobile-flag task (carried). SETTLEABLE includes CLOSED (accepted).
Task 5: re-review dispatched (sonnet) 44bd3081..5581a0ad. Task 6: BASE 5581a0ad — dispatching opus implementer (parallel with the re-review).
Task 5: fix round 1/5 (5 addressed + required coverage, 0 open; commits 44bd3081..5581a0ad) — re-review (sonnet) clean; two-segment arithmetic (8,030.30 earned / 6,969.70 unearned at 53 days) independently recomputed. Task 5: minor (deferred): I3 refusal message text deviates cosmetically from the ruled string.
Task 5: complete (commits a312fd1d, 5581a0ad; review clean after 1 fix round). Deferred: #290 sequence-lock cycle; mobile terminate contract → plan 5.
Task 6: implemented (opus) commits 84ece65e (rider: RecognitionPoster refresh-under-lock), c51f39ee (settlement statement + STL + balance-due row); backend 1763/0 (188 classes); 6 mutants killed. received = 30,500 confirmed. Concerns: ChequeService.requirePosted excludes TERMINATED → the balance-due collection row (and T5's KEPT cheques on a terminated lease!) can never be cleared → T7; mobile settlement screen reads fields that no longer exist → plan 5 mobile-flag carry; web rbac.ts misdescribes three settlement endpoints → T8.
Ruling (T7): collection transitions (deposit, clear, receive, bounce, replace, registerOnlinePending/clearOnline, cashReceipt) are allowed on leases in ACTIVE, NOTICE_GIVEN, EXPIRED, RENEWED and TERMINATED — a kept cheque or a settlement balance-due row on a terminated lease is money owed and must be collectable (spec §9.1 keep-list, §9.2 balance due); CLOSED refuses everything; grid-shaping actions (addRowToPostedLease from the user path, generate, saveRows) stay live-lease only — cost if wrong: a status set in one guard.
Task 6: review dispatched (opus) 5581a0ad..c51f39ee. Task 7: BASE c51f39ee — dispatching opus implementer (parallel with the review).
Task 6: review (opus) spec ✅ / quality Approved with 2 Important. Accounting hand-verified: refund STL Dr SD 3,000 / Dr RR 5,239.73 / Cr Bank 8,239.73 → both lease balances 0; damage-10,000 case leaves exactly 1,760.27 on the collection row, PDR credits RR once, NO double count. I1 netRefund ignores PDC_RECEIVABLE: an approved penalty's PEN debits RR and its collection row's PDR credits it straight back, so outstanding penalties and cheques KEPT at termination sit in PDC_RECEIVABLE, not RR — the full deposit can be refunded while instruments are outstanding, and the 400 text "already in the receivable balance" is false for penalties; I2 no IT for the multi-deposit STL, EXPIRED-lease settle, netRefund == 0. 8 minors in the review file.
Ruling (I1): netRefund's formula stays as the spec/plan define it (uncleared instruments are collected through the register, not netted silently), BUT the statement gains `instrumentsOutstanding` (amount = lease-dimension PDC_RECEIVABLE balance, plus the uncleared rows) and finalising a REFUND (netRefund > 0) while it is > 0 requires `acknowledgeOutstanding: true` on FinalizeSettlementRequest → else 400 naming the amount; the false 400 text is corrected ("approved penalties are collected through their own register row; add a post-termination charge as EARLY_TERMINATION_FEE or OTHER") — the accountant decides with the fact in front of them — cost if wrong: one extra checkbox for a legitimate refund.
⚠️ resolved: W1 counts accepted (T7's full run re-confirms); W2 NOWAIT lock correct by construction → deferred minor; W3 is T7's job (dispatched); W4 → T8; W5 mobile → plan 5 carry.
Task 6: fix round 1 QUEUED behind Task 7's implementer (one Gradle at a time).
Task 7: implemented (opus) commit 2ca6caad; backend 1779/0 (190 classes; +16); 6 mutations, 5 killed, 4b honestly reported as masked by a second correct filter. CHANGED Task 6 behaviour (accepted): finalise no longer closes on netRefund ≥ 0 alone — new LeaseClosureService (ended + settlement FINALIZED + nothing outstanding, BOUNCED counts) is called by finalise AND every clearing path; a lease with kept cheques stays TERMINATED after finalise. CARRY to T8: the UI must not say finalise closes the contract. clearOnline's idempotent branch stays above the lease guard (gateway retry after CLOSED must not 400 forever). EXPIRED stays grid-shapeable because penalty approval on an EXPIRED lease raises its collection row through addRowToPostedLease (Ruling: accepted — the implementer's reading of CHARGEABLE is right; my parenthetical was wrong). Lock order meets both ways (termination lease→cheque, close hook cheque→lease) but every lock is NOWAIT → "try again", not a deadlock → noted on #290.
Task 7: review dispatched (opus) c51f39ee..2ca6caad. Task 6: fix round 1 dispatched (resume opus) FIX_BASE 2ca6caad. Task 8 (web): BASE 2ca6caad — dispatching opus implementer in parallel (web/ only, no Gradle; settlement contract per the I1 ruling: statement.instrumentsOutstanding + FinalizeSettlementRequest.acknowledgeOutstanding).
Task 7: review (opus) spec ❌ (grid-shaping still open on EXPIRED) / quality Needs fixes — all 15 requirePosted call sites verified classified. I1 returnToTenant and cancel take a row out of OUTSTANDING without calling the close hook → a lease that should be CLOSED is stuck (last kept cheque handed back; PenaltyAssessmentService.reverse cancelling the last collection row); I2 POSTED still contains EXPIRED so the public cash-receipt endpoint can add an arbitrary row + PDR to an ended contract; I3 NOTICE_GIVEN is reachable for the first time but claimUnitForLease / releaseUnitIfNoOtherActiveLease / heldOnlyBy test ACTIVE only → a unit can be double-let or vacated under a tenancy on notice. 9 minors in the review file.
Ruling (I2): the reviewer is right and my acceptance of "EXPIRED stays grid-shapeable" is WITHDRAWN — user-facing grid-shaping is ACTIVE/NOTICE_GIVEN/RENEWED-as-today only; penalty approval on an EXPIRED lease raises its collection row through the INTERNAL door Task 6 added for settlement (same door, second caller) — cost if wrong: none for users; one more internal caller.
Ruling (I1): every transition that removes a row from OUTSTANDING (clear, receive, clearOnline, returnToTenant, cancel, and replace when it nets to nothing outstanding) ends by calling LeaseClosureService — one call site list, asserted by a parameterised IT.
Ruling (I3): "live tenancy" for unit occupancy = ACTIVE or NOTICE_GIVEN, defined ONCE (a LeaseStatus helper / single set) and used by claimUnitForLease, releaseUnitIfNoOtherActiveLease, heldOnlyBy and the one-active-lease-per-unit check; verify the partial unique index ux_leases_one_active_per_unit (changeset 80) covers NOTICE_GIVEN — if its predicate is status='ACTIVE' only, say so in the report (a new changeset would be needed; 86 is the fix-up changeset on this stack, the index lives in released 80 → add the wider index in 86, leave 80 alone).
⚠️ resolved: counts accepted pending the next full run; clearOnline idempotent branch harmless (verified writes nothing); front-end assumption → T8 brief already forbids promising closure.
Task 7: fix round 1 QUEUED behind Task 6's fix round (one Gradle at a time).
Task 8: implemented (opus) commit d390d8eb (web only); tsc clean; vitest 567/567 in 92 files (+50); lint 206 (−5); 15 mutants killed (one test found passing for the wrong reason and fixed). Concerns: acknowledgement refusal not yet in the backend at its commit time (T6 fix round in flight — controller verifies when it lands); month-end page groups by CONTRACT because RecognitionEntryDTO carries no property → Ruling: RecognitionEntryDTO gains propertyId, propertyName, unitName (rider on T7's fix round, backend) and T8's fix round regroups by property — spec §11 says by property — cost if wrong: three DTO fields; rbac canTerminateLeases was wrong both ways, corrected and pinned; mobile still on dead contracts → plan 5.
Task 8: review dispatched (sonnet) web paths 2ca6caad..d390d8eb.
Task 8: review (sonnet) spec ✅ / quality Approved — 0 Critical, 0 Important; reviewer re-ran tsc (clean) and vitest 567/567; the acknowledgement gate matches SettlementService.requireOutstandingAcknowledged on disk. Task 8: minor (deferred): LineTable inputs lack aria-label/scope; disabled-reason text not wired via aria-describedby to Finalize; 404-as-"no draft" depends on LeaseController catching NotFoundException.
Task 8: complete (commit d390d8eb, review clean, no fix round) — EXCEPT the ruled regroup-by-property follow-up, which waits for the RecognitionEntryDTO property fields (rider on T7's fix round) and is dispatched as "Task 8b" with the two a11y minors folded in.
Task 6: fix round 1 done (opus) commit 4a72bcd5; backend 1786/0 (190 classes; +7); mutations A–D killed. Field names exactly as ruled and verified against web d390d8eb. acknowledgeOutstanding is a boxed Boolean (Jackson FAIL_ON_NULL_FOR_PRIMITIVES turned an omitted primitive into a 400 for every finalise) — accepted. Deferred → issue https://github.com/kunal-ak23/rentaxis/issues/291 (forfeiture category, VAT on settlement charges, queryable acknowledged figure, the two "outstanding" definitions). Re-review dispatched (sonnet) backend 2ca6caad..4a72bcd5. Task 7: fix round 1 dispatched (resume opus) FIX_BASE 4a72bcd5, with the RecognitionEntryDTO property-fields rider.
Task 6: fix round 1/5 (2 addressed, 0 open; commits 2ca6caad..4a72bcd5 backend) — re-review (sonnet) Approved: instrumentsOutstanding and Σ outstandingInstruments cannot disagree by construction; BOUNCED exclusion verified against ChequeService.bounce's CBR; guard runs before posting under the lease lock.
Task 6: complete (commits 84ece65e, c51f39ee, 4a72bcd5; review clean after 1 fix round). Deferred: #291 (forfeiture category, VAT on settlement charges, queryable acknowledged figure); concurrency of two simultaneous finalises is correct by construction (NOWAIT) but untested.
Task 7: fix round 1 INTERIM (opus) — fixes written, full suite running, not yet committed: I1 returnToTenant/cancel/replace evaluate LeaseClosureService (penalty reverse inherits via cancel); I2 POSTED = {ACTIVE, NOTICE_GIVEN, RENEWED}, internal addCollectionRow door with penalty approval as second caller; I3 LeaseService.LIVE = {ACTIVE, NOTICE_GIVEN}; changeset 80's index WAS status='ACTIVE' only → widened in 86 in place; latent ordering bug fixed (markActiveOnPosting flipped status before claiming the unit → the widened index would have turned a double-let into a 500); rider: RecognitionEntryDTO propertyId/propertyName/unitName.
Task 8b dispatched (resume opus web implementer; web only) against the final DTO names: regroup month-end by property + two a11y minors.
Task 8b: done (opus) commit ffb07c8d (web only); tsc clean; vitest 572/572; lint 206; 9 mutants killed. testid change: recognition-group-{propertyId} (+ -unassigned) replaces recognition-group-{leaseId} → T9 walkthrough uses the new ids. Re-review dispatched (haiku) d390d8eb..ffb07c8d.
Task 8b: review (haiku) Approved — all requirements MET, 0 Critical/Important. Task 8: complete (commits d390d8eb, ffb07c8d; reviews clean).
Task 7: fix round 1 done (opus) commits d2bdcd0b (I1+I2+I3+minors), b85a52fa (rider: property/unit on RecognitionEntryDTO); backend 1799/0 (190 classes; +13); 6 mutants killed. Changeset 80's index confirmed ACTIVE-only → widened in 86 in place (rollback restores 80's form; operator note for a DB already holding ACTIVE+NOTICE_GIVEN on one unit → PR deploy note). Latent ordering bug fixed in markActiveOnPosting (claim the unit BEFORE dirtying the lease). Deferred → issue https://github.com/kunal-ak23/rentaxis/issues/292 (gate pass/booking/listing ACTIVE-only lookups; expiry listing sync; penalty on terminated lease only waivable). addCollectionRow public with two callers (same shape as markTerminated/markExpired) → final-review triage.
Task 7: re-review dispatched (sonnet) backend 4a72bcd5..b85a52fa.
Task 9: local stack up (jar built at b85a52fa; rentaxis_v2 recreated empty — changesets 80…86 applied clean on a fresh DB; stub gateway on; web :3001). Task 9 implementer dispatched (opus): plan 3 walkthrough spec (13 scenarios, one video each, hard assertions), e2e extension, seed script recognition run, stale spec clean-up. Ruling: a BACKEND bug the walkthrough finds is reproduced in the report and its scenario marked test.fixme — the walkthrough agent does not touch backend/; the controller dispatches the fix through the normal loop and re-runs — cost if wrong: one extra round trip per backend bug.
Task 7: fix round 1/5 (3 addressed, 0 open; commits 4a72bcd5..b85a52fa backend) — re-review (sonnet) PASS; 80/85 byte-identical, no 87. Task 7: minor (deferred): addCollectionRow guard wider than either caller's gate (unenforced, like markExpired/markTerminated); zero-replacement replace branch untested; LeaseExpirationJobIT counts globally scoped.
Task 7: complete (commits 2ca6caad, d2bdcd0b, b85a52fa; review clean after 1 fix round). Tasks 1–8 ALL COMPLETE; Task 9 (walkthrough) in flight.
Ruling: the final whole-branch review starts NOW, in parallel with Task 9's walkthrough (reviewers are read-only; production code is frozen unless the walkthrough finds a bug, in which case that fix joins the single fix wave) — two seats as in plan 2 (backend fable, web opus) over ea4a205c..b85a52fa — cost if wrong: a walkthrough-driven fix lands unreviewed by the final seat; mitigated by its own scoped re-review.
Final review (backend seat, fable): With fixes — 1 Critical / 5 Important / 12 Minor; report final-review-backend.md. C-1 a lease settled over an unreplaced BOUNCED cheque can only reach CLOSED by collecting the bounced amount a SECOND time (the STL already netted it via RR; the IT asserts the flow and never checks RR, which ends −12,750). I-1 a RENEWED predecessor whose deposit was NOT carried forward can never be settled (spec §6.6). I-2 closure checks the register, never the ledger → returning/cancelling a kept cheque after finalise closes the lease still owing 12,750. I-3 rebuildAfterAmend cancels/reverses entries without the row lock (T5's fix never carried back to T3's method). I-4 saveDraft takes no lease lock and LeaseSettlement has no @Version → a save racing finalise rewrites DRAFT after the STL → double finalise. I-5 TCR reverses only unearned NET rent; VAT on unearned stays in RR and OUTPUT_VAT. No tenant-isolation P0; 85 untouched; 86 coherent; no convention-only method reachable over HTTP.
Ruling (C-1 + I-2): closure is LEDGER-based — CLOSED iff lease ended + settlement FINALIZED + lease-dimension RENT_RECEIVABLE == 0 AND PDC_RECEIVABLE == 0 AND no ONLINE_PENDING row; a BOUNCED row whose debt the STL absorbed no longer blocks closing and can no longer be replaced/collected once the settlement is FINALIZED and RR ≤ 0 ("already settled through the settlement"); returnToTenant/cancel of a KEPT cheque after the settlement is FINALIZED is refused ("the settlement was finalised counting on this cheque — replace it instead"); replace stays allowed — cost if wrong: finance must re-open nothing; a genuinely uncollectable kept cheque needs a write-off flow (issue, plan 4+).
Ruling (I-1): RENEWED joins SETTLEABLE (a predecessor whose deposit stayed behind is settled like an expired lease; closure rule as above); terminate stays refused for RENEWED.
Ruling (I-5): credit-note treatment — the TCR also reverses VAT on the unearned amount per VAT-bearing RENT line (Dr OUTPUT_VAT / Cr RENT_RECEIVABLE, LeaseVat helper), preview shows it; client confirmation tracked in https://github.com/kunal-ak23/rentaxis/issues/293 — cost if wrong: one journal line.
Fix wave (backend) = C-1, I-1..I-5 + minors M-1 (close hook stale lease status), M-3 (CLOSED in SETTLEABLE), M-6 (giveNotice lock contention → 500), the untested zero-replacement replace branch; M-10 → PR deploy note (two pre-flight queries for 86's unique indexes). Dispatched now (opus); web seat's findings go to a web fixer in the same wave.
Final review (web seat, opus): With fixes — 0 Critical / 5 Important / 11 Minor; tsc clean, vitest 572/572, en/ar parity 1972 keys. I-1 cheque grid / finance register / New cash receipt ignore LEASE status (transitions offered on CLOSED; cash receipt offered on EXPIRED — both always refused); I-2 Recognition tab footer sums REVERSED/CANCELLED rows (51,000 amend → 111,000) and its fixture cannot catch it; I-3 NOTICE_GIVEN unreachable — terminationApi.notice has no caller; I-4 Renew gated on ACTIVE only while RENEWABLE = {ACTIVE, EXPIRED, NOTICE_GIVEN}; I-5 lease LIST Terminate buttons use canManageLeases (table) / no role gate (cards) and ACTIVE only.
Ruling: web fix wave = I-1…I-5 + M-1 (unused keys), M-3 (render skippedLockedEntries), M-4/M-5 (translate enums in sentences/tables), M-6 (snapshot instrumentsOutstanding after FINALIZED — needs the backend to expose the acknowledged/snapshot figure; if absent, freeze the tile to the value at finalise from the settlement response or hide it after FINALIZED), M-7 (no control to satisfy the acknowledgement 400) + mirrors for the backend wave's new rules (RENEWED settleable; bounced-after-settlement not replaceable; kept cheque not returnable/cancellable after FINALIZED; preview.unearnedVat). Deferred minors M-2, M-8, M-9, M-10, M-11 → issue https://github.com/kunal-ak23/rentaxis/issues/294.
Web fix wave is HELD until the Task 9 walkthrough agent returns (both edit web/; sequential implementers per tree) and until the backend wave reports its final JSON/messages.
Task 9: DONE (opus) commits a2bbd01f (3 web defects fixed test-first — incl. a settlement-page double-load race that silently erased an unsaved deduction), e099d93a (walkthrough spec), 2c445a5e (e2e + dead-route specs), 7465dfbd (seed runs month-end). Recording pass at default pacing: `12 passed, 1 skipped (1.3m)`; scenario 11 (expiry) skipped deliberately — the job has no HTTP entry point and faking EXPIRED in SQL proves nothing (Ruling: accepted; LeaseExpirationJobIT covers it). No backend defect found by the live run. vitest 576/576, tsc clean, lint 206. The takes describe the b85a52fa build → the walkthrough is RE-RUN after the final fix wave; those takes are the ones delivered.
Final fix wave (web): dispatched (opus) now that web/ is free.
Final fix wave (web): DONE (opus) commit f7abba71; tsc clean; vitest 632/632 in 96 files (+56); lint 206; red lines + mutations per finding. Residual: the finance register (and renter Pay now) cannot gate on lease status — ChequeDTO has no leaseStatus → Ruling: parked as issue https://github.com/kunal-ak23/rentaxis/issues/295 (no second fix wave); the still-running backend wave adds ChequeDTO/RenterChequeDTO.leaseStatus as a rider so the follow-up is web-only. Rule (b) mirrored as "withhold cancel/return, keep replace" because the Java guards are balance-dependent (accepted: the 400 text surfaces). CLOSED withholds details/receipt too (client-stricter, accepted — read-only views remain). One walkthrough assertion corrected (footer Σ live rows). Scoped re-review dispatched (sonnet) web 7465dfbd..f7abba71.
Final fix wave (web): scoped re-review (sonnet) — all findings + the three backend mirrors ADDRESSED, verified member-by-member against the working-tree Java; tsc clean, vitest 632/632 self-run; no new Critical/Important. Non-blocking: receipt download hidden on a CLOSED lease's grid (server has no such guard) → added to #295. Web half COMPLETE; waiting on the backend half.
Final fix wave (backend): DONE (opus) commits 08acd850 (I-1, I-4, M-3), 253fb5b9 (C-1, I-2, M-1 + SETTLEABLE rename), 26797e6f (I-3), b22a381b (I-5, M-6), c1fbe8ce (rider: leaseStatus on ChequeDTO/RenterChequeDTO); backend 1812/0; 9 ITs red-first, 13 guards mutation-checked. Decisions: the two post-finalise refusals are MEASURED (FINALIZED + what the receivable says), not flat — a flat rule would kill "bounce after finalise, debt is real" and the penalty-reversal closure (Ruling: accepted; the measured rule is the ledger-based principle applied consistently). ENDED gained RENEWED so a settled predecessor reaches CLOSED (Ruling: accepted per my I-1 ruling). Found: a locking finder cannot refresh a @Version entity (ObjectOptimisticLockingFailure) and EntityManager.refresh leaks JPA LockTimeoutException past Spring's translation — fixed here; three other lockLease copies carry the same latent trap → issue. Zero-replacement replace does not exist (validateNewRows refuses an empty list; pinned). Scoped re-review dispatched (opus) backend f7abba71..c1fbe8ce. Controller: rebuilding the jar at c1fbe8ce and re-running the walkthrough on the final code in parallel (reviewer is read-only).
Issue https://github.com/kunal-ak23/rentaxis/issues/296 filed (single lockLease helper; DepositCarryForward.carry unlocked). Stack rebuilt at c1fbe8ce on a fresh DB; plan 3 recording pass + plan 2 walkthrough regression running.
Recording pass on the FINAL code (jar c1fbe8ce, fresh DB, web at HEAD via hot reload): plan 3 walkthrough `12 passed, 1 skipped (1.3m)` at default pacing — 12 takes in web/walkthrough/takes/accounting-v2-plan3/; plan 2 walkthrough regression `17 passed (1.7m)`. Controller checked end frames of 02 (second run posts 0: "Nothing to post up to this date") and 09 (register after closure: both rent cheques CLEARED). Note: the on-screen version tag reads b85a52f because the Next dev server captured the sha at start-up; the code it served is HEAD. The register still offers Bounce on a CLOSED lease's cleared row — the known #295 gap, now web-only since leaseStatus is on the DTO.
Final fix wave (backend): scoped re-review (opus) — all ten items ADDRESSED with ledger-level evidence; the two MEASURED rulings judged correct (they keep "bounce after finalise" and the penalty-reversal closure working). NEW Important (residual corner of C-1): requireNotAlreadySettled measures the LEASE's receivable, so a post-finalise bounce on cheque B re-opens replace on cheque A whose debt the STL absorbed → A collected twice.
Ruling: the residual is REAL and parked as issue https://github.com/kunal-ak23/rentaxis/issues/297 marked fix-before-prod (per-cheque "absorbed by settlement" marker), not fixed here — the skill allows no second fix wave, Kunal's standing instruction is to file what the wave turns up, and nothing in plans 4–5 builds on the replace guard; it needs a settlement finalised over a bounced cheque AND a second bounce afterwards — cost if wrong: one double collection in that corner until the issue lands; surfaced in the PR body. Minors + out-of-scope notes ride the same issue.
Plan 3 final review: COMPLETE. Tasks 1–9 complete. Pushing and opening the stacked PR (base feat/accounting-v2-lease-posting).
