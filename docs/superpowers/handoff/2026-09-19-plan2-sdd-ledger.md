# SDD ledger — plan: docs/superpowers/plans/2026-09-17-accounting-v2-plan2-lease-posting-pdc.md
Spec: docs/superpowers/specs/2026-09-17-accounting-v2-design.md (§6, §7, §9.3, §11). Depends on plan 1 branch feat/accounting-v2-ledger-core (stacked; NOT merged to main).
Ruling: branch feat/accounting-v2-lease-posting is cut from feat/accounting-v2-ledger-core (plan's "from main after merge" superseded by Kunal's "stack them" directive 2026-09-18); PR base = plan 1 branch — cost if wrong: rebase onto main after plan 1 merges.

## Pre-flight scan (2026-09-18)
| Pair / task | Produces vs consumes | Finding | Ruling |
|---|---|---|---|
| T1 changeset 83 | `addUniqueConstraint columnNames: tenant_id, code` unquoted in a flow mapping | truncates to tenant_id (same bug as 81) | fixed in plan doc (quoted); T1 brief says so |
| T1 ↔ T6 | T6 says "add leases.renter_accepted_at to 83 now" | T1 doesn't know | T1 adds `renter_accepted_at timestamptz` up front |
| T1 ↔ T8 | T8 says add `leases.carry_deposit_forward boolean default false` to 83 | T1 doesn't know | T1 adds it up front; T1 schema IT asserts both columns |
| T1 ↔ T3 | cheques columns / Cheque entity | consistent | — |
| T2 seed → T4 test | `Advance Rent - <property>` name | matches plan 1 template `"Advance Rent - {property}"` (PropertyAccountService:84) | — |
| T4 ↔ plan 1 PostingRequest | `dr(UUID)/cr(UUID)`, `withNarration`, `withDims`, `ofPairs`, `pair` | all exist (PostingRequest.java:58-104) | — |
| T5 ↔ T11 | `GET /api/v1/leases/{id}/cheques` defined in T5; T11 says "moved here" | duplicate route | stays on LeaseController (T5); T11 does not move it; leasing.ts `leaseApi.cheques(id)` hits /leases/{id}/cheques |
| T6 ↔ T14 | T14 needs `POST /{id}/post?dryRun=true` "add to Task 6" | T6 doesn't know | T6 implements `dryRun` (returns validation errors, writes nothing) |
| T7 ↔ T10 | T10 calls `chequeService.clearOnline(chequeId, capturedOn)` | not in T7's interface | T7 adds `clearOnline` (CRT Dr settlement/BANK / Cr PDC_RECEIVABLE; ONLINE_PENDING→CLEARED) alongside register/revertOnlinePending |
| T3 JPQL | `c.lease.status not in ('DRAFT','PENDING_SIGNATURE')` string literals vs enum | Hibernate 7 may reject | T3 uses enum literals / parameters; brief notes it |
| T9 ↔ T16 | both modify `settings/fines/page.tsx` | duplicate web work | T9 backend + DTO only; T16 does the web fields |
| T10 ↔ plan 1 T15 carry | `OnlinePaymentServiceClearIdempotencyIT` near-vacuous after v1 removal | — | T10's replacement IT asserts exactly one CRT after duplicate webhook delivery |
| T12 deletions | lists `ChequeFailurePenaltyIT`, `PenaltyClearanceIT`, `PenaltyPaymentServiceTest`, `SettlementDepositLedgerTest` | some already deleted/reduced in plan 1 T15 | delete-if-present; `ChequeFailureReason` enum must SURVIVE (used by Cheque) |
| T12 ↔ T11 | `MeetingDetail.payment_schedule_ids` old column ignored until 84 | consistent | — |
| T13 leasing.ts ↔ T11 routes | names match the table | consistent | — |
| T14 deletes `PaymentScheduleEditor`, `CollectChequeDialog`, `MarkChequeFailedDialog`; T15 deletes `components/payments/*` | overlapping deletions | T14 deletes editor + stops using the dialogs; T15 deletes the components dir |
| T17 walkthrough | plan predates Kunal's directive | — | T17 adds `web/walkthrough/accounting-v2-plan2.{config,spec}.ts`, one video per scenario, run on the local stack; PR opened by controller after final review |
| Each task self-consistency | tests vs code | T4/T6 test bodies are outlines — implementer writes them in full | — |
Changeset numbering: 83 (T1), 84 (T12). Rollback YAML in 84 already quoted (313c678c).
Branch feat/accounting-v2-lease-posting cut from feat/accounting-v2-ledger-core @ a627f9dd (PR #263 open). Task 1: BASE a627f9dd — dispatching opus implementer
Task 1: implemented (opus ae56a429335b38bfd) commit ecf65b4d (trailer amended); backend 1331/0; LeaseChequeSchemaIT 7/7 mutation-checked. Concerns: rollback untested (no harness, same as 81); properties.code nullable → Task 2/4 naming must handle NULL; cheques.mode/status no CHECK (Java-side enums). Review dispatched (sonnet).
Task 1: review (sonnet afeced71e3f6bb0b3) Approved, 0 issues (minor: splitStatements:true explicit no-op; rollback unexercised as with 81).
Task 1: complete (commit ecf65b4d, review clean, no fix round)
Ruling: Task 3 creates `ChequeDueRules` (static due/overdue/daysOverdue per T3 Step 2 formulas) so `ChequeMapper` compiles now; Task 7 reuses it — cost if wrong: T7 edits one small class.
Task 2: BASE ecf65b4d — dispatching opus implementer
Task 2: implemented (opus a75e1d18de5014913) commit a243e105; backend 1337/0; ChargeTypeServiceIT 6/6 mutation-checked. Decisions by implementer: PUT is full replacement (primitive active/displayOrder — web T13/T16 must send the whole DTO); code immutable (400 on change); role→type map is a switch; asset-side role on FEE passes ("all others → INCOME" as briefed) — reviewer to judge. AccountRequestBindingTest ctor updated.
Task 2: review dispatched (sonnet) BASE ecf65b4d..a243e105
Task 3: BASE a243e105 — dispatching opus implementer (parallel with T2 review; disjoint files)
Task 2: review (sonnet acdf68f3cb490f42f) Approved; 1 Important needing ruling: "all others → INCOME" lets FEE/RENT types credit BANK/CASH/receivables/OUTPUT_VAT/etc.
Ruling: charge-type credit roles are an ALLOW-LIST — LIABILITY: ADVANCE_RENT, SECURITY_DEPOSIT, PARKING_DEPOSIT; INCOME: RENTAL_INCOME, ADMIN_FEE, PARKING_INCOME, COOLING_CHARGES, MAINTENANCE_CHARGES, RENT_PENALTY, CHEQUE_RETURN_PENALTY, OTHER_INCOME, FORFEITED_INCOME; every other role → 400 "Role X cannot be credited by a charge type" — spec §6.1 (lines credit the type's role) makes asset/VAT credits nonsensical — cost if wrong: an admin cannot create an exotic charge type until the list is extended.
Task 2: fix round 1 (resume a75e1d18de5014913) FIX_BASE a243e105 — after T3 implementer finished (sequential implementers).
Task 3: implemented (opus ae5f551ef57d714e6) commit c850bb0f; backend 1388/0; ChequeDueRulesTest 39, ChequeRepositoryIT 12; mutation-checked. Concerns: TenantAspect-outside-transaction gap bit 6 assertions (wrapped in TransactionTemplate) → Ruling: every plan-2 service method and controller path that touches repositories is @Transactional (readOnly for reads); carried into T6/T7/T9/T10/T11 briefs; platform audit stays with the auth/tenant hardening item. search by property name unreachable (asserted unit/renter/cheque-number). isUncleared includes ONLINE_PENDING while findDue excludes it → Ruling: correct — a pending online payment must not be re-collected; T7 keeps both. ChequeMapper needs the loading transaction; T11 fetch-joins.
Task 3: review dispatched (opus) BASE a243e105..c850bb0f
Task 3: review (opus a0e09841a51e752e6) Approved; Important: I1 search inner-joins nullable c.unit (unit-less cheques vanish) → fix (left join + fixture); I2 findDue/findToDeposit propertyId never exercised → add one assertion each; I3 grace anchor for BOUNCED → Ruling: stays chequeDate per brief (cheque-return penalties are priced by bounce count in T9; late-payment amount uses clearedOn vs chequeDate+grace) — cost if wrong: T9 changes one anchor. Minors to include in the round: mapper emits daysOverdue 0 when !overdue; ChequeMapper test; amount precision/scale. Deferred: default sort on Page queries (T11), ChequeImageRetentionJob migration (T11), ChequeDueRules unchanged.
Task 3: fix round 1 queued (resume ae5f551ef57d714e6) after T2 fix round lands. FIX_BASE c850bb0f
Task 2: fix-round agent a75e1d18de5014913 STALLED (watchdog) after writing the allow-list fix + tests and mutation-checking; controller verified the diff, running the full suite itself, will commit on green.
Task 2: fix round 1 committed by controller (e607c01b); backend 1391/0 (Gradle UP-TO-DATE against current sources). Re-review dispatched (haiku). Task 3 fix round dispatched (resume ae5f551ef57d714e6).
Task 2: re-review clean (haiku ad8889073c81b37f6). Task 2: complete (commits a243e105, e607c01b; review clean after 1 fix round)
Task 3: fix round 1 done (2b66926c); backend 1397/0 (+6 cheque tests, +3 are T2's allow-list tests); left join mutation-checked. Re-review dispatched (haiku).
Task 4: BASE 2b66926c — dispatching opus implementer
Task 3: re-review clean (haiku a9e852037d428897b). Task 3: complete (commits c850bb0f, 2b66926c; review clean after 1 fix round)
Task 4: implemented (opus a0a8d1688a0809bb5) commit f4da6818 (trailer amended); backend 1387/0 (tests deleted per brief); guards mutation-checked. Deviation: brief's try/catch around AccountResolver.resolve marks the caller tx rollback-only (proxy is @Transactional) → added AccountResolver.resolveOrNull — CARRY to T5/T6/T7 briefs. Concerns: import now drops Cheques-sheet data (T5/T11 must build cheque rows from it); per-method payment field validation has no successor until T5; extendLease still calls PaymentScheduleService (dies T12); aLineWhoseRoleHasNoMappedAccountIsSavedUnmapped doesn't exercise null path (move to T6).
Task 4: review dispatched (opus) BASE 2b66926c..f4da6818
Ruling: the import's Cheques-sheet data stays unconsumed until T11 (plan assigns "cheques sheet → ChequeRowInputs on the draft" to T11); T5 only provides saveRows — cost if wrong: imports between T4 and T11 lose cheque detail on this unreleased branch only.
Task 5: BASE f4da6818 — dispatching opus implementer (parallel with T4 review; T4 fixes, if any, applied after T5 returns)
Task 4: review (opus a5eb563c083e61006) Needs fixes — C1 explicit creditAccountId unvalidated (group/inactive/wrong-type passes; T6 guard only sees nulls); I2 contract PDF Section 3 + grand total still read LeaseCharge (nothing writes it) and ensureScheduleExists now creates schedules as a side effect; I3 import drops Cheques-sheet/booking-deposit instrument data with no warning; I4 unmapped-role test cannot fail. Minors: LeaseLineRepository @Modifying comment wrong; dead UpdatePaymentScheduleDTO/LeaseChargeDTO; 404 vs 400 for bad creditAccountId; deleteDraftLease with non-DRAFT cheque → opaque FK 500; extendLease now adds 0 installments (accepted until T8).
Ruling: explicit creditAccountId must be an active leaf whose accountType equals ChargeTypeService.CREDITABLE_ROLES.get(chargeType.role) → 400 otherwise — the allow-list already defines the expected type — cost if wrong: an accountant cannot point a line at an unusual account without changing the charge type.
Ruling: contract Section 3 + totals read lease_lines NOW (money on a legal document); ensureScheduleExists side effect removed; Section 4 stays on whatever schedules exist until T11 moves it to cheques.
Task 4: fix round 1 (resume a0a8d1688a0809bb5) FIX_BASE c5c3c64b (after T5)
Task 5: implemented (opus ace99ded507c854e4) commit c5c3c64b; backend 1409/0; mutation-checked. Decisions: distribution defaults FIRST_LARGER ignoring lease.installmentDistribution (Ruling: accepted per brief/PACT; web sends it explicitly — note for T13/T14); 3-arg distribute keeps LEGACY_STEPS (dies T12); generateNumbers/saveRows require a DRAFT lease (accepted; registered rows are edited via T11 `details`); fold flag is Boolean (accepted); no controller test for the four endpoints.
Task 5: review dispatched (sonnet) BASE f4da6818..c5c3c64b
Task 5: review (sonnet a277d4dcdcb944a96) Approved; Important: I1 generate() ignores VAT (grid short by VAT on VAT-applicable lines; T6 guard would reject every such default grid); I2 no controller test for the 4 endpoints' roles. Minor: foreign-lease row id untested; inline floor(i×months/n) (4th copy; v1 copies die in T12).
Ruling: generate() grosses up VAT-applicable lines — rent total + VAT(rent) is what gets distributed; each DEPOSIT/FEE extra carries its own VAT — via ONE shared helper `core/service/lease/LeaseVat` (RATE 0.05, vatOf(net) HALF_UP 2dp) that T6's TCO posting also uses, so Σ cheques = contract value + VAT by construction — cost if wrong: VAT rounding per line vs on total differs by fils; T6's guard uses the same helper so they cannot disagree.
Task 5: fix round 1 queued (resume ace99ded507c854e4) after T4 fix round: VAT gross-up + LeaseVat helper, @WebMvcTest roles for the 4 endpoints, foreign-lease id test. FIX_BASE = T4 fix commit.
Task 4: fix round 1 done (1656f028); backend 1416/0; leaf check mutation-checked. Concerns: contract Section 4 empty until T11 moves it to cheques (Ruling: accepted on the unreleased branch; T11 brief must restore it from cheques and its IT must assert a non-empty Section 4); web import summary may not render warnings (check in T14/T17 walkthrough); expectedTypeFor null = "no constraint" → tighten to refuse (folded into T5's fix round, 2 lines); line messages use the charge-type code (accepted).
Task 4: re-review dispatched (sonnet) c5c3c64b..1656f028. Task 5: fix round 1 dispatched (resume ace99ded507c854e4) FIX_BASE 1656f028
Task 4: re-review clean (sonnet aeb8766c8909f63e8). Task 4: complete (commits f4da6818, 1656f028; review clean after 1 fix round). Note for T6: contract totals exclude VAT (pre-existing semantics); LeaseVat helper arrives with T5's fix round.
Task 5: fix round 1 done (7809c5af); backend 1427/0; LeaseVat helper (RATE public, vatOf/grossOf package-private in core.service.lease); VAT gross-up mutation-checked; LeaseControllerChequeEndpointsIT 3; expectedTypeFor null refuses. Beyond brief: LeaseAccessPolicy now treats ROLE_ACCOUNTANT as tenant-wide (plan T11 mandates exactly this — Ruling: accepted early; T11 skips it). 
Ruling: deposits never carry VAT — LeaseVat.vatOf(LeaseLine) returns ZERO for DEPOSIT behaviour regardless of the flag, matching ContractGenerationService.vatOn; applied in T6 (one definition) — cost if wrong: a VAT-able deposit type needs the rule relaxed.
Task 5: re-review dispatched (sonnet) 1656f028..7809c5af
Task 6: BASE 7809c5af — dispatching opus implementer
Task 5: re-review clean (sonnet aa9ed897089e574e2; LeaseAccessPolicy change judged Safe). Task 5: complete (commits c5c3c64b, 7809c5af; review clean after 1 fix round)
Task 6: implemented (opus a70e11a8bf68d6c4b) commit 4beffc80; backend 1451/0; Σ-cheques guard + lease lock mutation-checked (lock failure shows as exception type: @Version + unit lock already prevent a double TCO). Found+fixed: renewal flush-ordering race vs ux_leases_one_active_per_unit (predecessor flushed first). Concerns: mobile + web e2e helpers call deleted PUT /activate (web → T14/T17; mobile frozen → plan 5); requiredRoles demands a role mapping even when the line has an explicit credit account (Ruling: relax — explicit account satisfies the role — goes into T6's fix round); dryRun takes no lock (accepted: post re-validates under lock).
Task 6: review dispatched (opus) BASE 7809c5af..4beffc80
Ruling (T7): clearOnline(chequeId, capturedOn, settlementAccountId|null→BANK) added now for T10; PenaltyRuleEngine.onBounce call is added by T9 (T7 leaves no dangling reference); email events use cheque-based payload factories added alongside the schedule ones (T12 deletes the old).
Task 7: BASE 4beffc80 — dispatching opus implementer (parallel with T6 review)
Task 6: review (opus a21f79466ddb099ab) Approved — journals hand-verified. Fix round items: I1 shared DecimalFormat not thread-safe; I2 PDR entryDate=cheque.postingDate untestable today (add divergent-date test); I3 no mid-write rollback test (lock a later cheque's postingDate); I4 markActiveOnPosting lost the status precondition; I5 receivableAccountId override unvalidated (tenant/leaf/active) + untested; I6 requiredRoles relaxation (ruled); minors: validate cheque amount>0, dead requiredRoles(Lease), checkStatus flag doing two jobs. Deferred: unit.current_lease_id (no column; spec §6.4 note → plan 3/5), amend has no dry run.
Task 7: implemented (opus ad86a9341ae48f9c9) commit d382a1fa; backend 1474/0; both mutants killed. New `LeaseChequeRegistrar` duplicates LeasePostingService's PDR block (file was locked for review) → T6 fix round delegates to it. Concerns: clearOnline from a BOUNCED-origin row would drive PDC negative → Ruling: registerOnlinePending accepts ONLY REGISTERED rows (T10 replaces a bounced cheque with an ONLINE row first, per plan) — T7 fix round; addRowToPostedLease can duplicate seqNo under concurrency → lock the lease row first — T7 fix round; concurrentClearAndBounce replaced by concurrentBounces (accepted); PENALTY_INCURRED notification left to T9; ChequePayload.paymentScheduleId carries a cheque id until T12 (accepted).
Task 6: fix round 1 (resume a70e11a8bf68d6c4b) FIX_BASE d382a1fa. Task 7: review dispatched (opus) BASE 4beffc80..d382a1fa
Task 7: review (opus aa5cd5b8ca592ee0c) Needs fixes — all 11 transitions journal-correct; C1 registerOnlinePending admits BOUNCED → clearOnline drives PDC negative, and revertOnlinePending launders BOUNCED→REGISTERED; C2 the ruled remedy (ONLINE replacement row) is blocked by ChequeRowRules refusing ONLINE; I3 no lease lock in addRowToPostedLease/replace (seqNo dup); I4 debitAccountId override untyped; I5 writes guarded by requireReadable (renter passes object check; T11 must role-gate; add a manage-level floor); I6 untested: TRANSFER receive, non-PDC deposit, non-PDC clear→bounce refusal, revert from wrong status, batch contention, cancel with null pdr, and NO cross-tenant test; I7 PDC leg of the 750 case unasserted. Minors: notes overwritten, clearOnline idempotency swallows non-ONLINE CLEARED rows, static DecimalFormat, double register load.
Ruling: gateway door = `ChequeService.replaceForOnlinePayment(bouncedChequeId, date)` → BOUNCED→REPLACED + ONE ONLINE-mode row (same amount, REGISTERED, own PDR) via the registrar; ChequeRowRules keeps refusing ONLINE from user-facing paths (flag/overload for the internal path); registerOnlinePending accepts REGISTERED only; revertOnlinePending accepts ONLINE_PENDING only; clearOnline idempotent only for mode ONLINE rows, other CLEARED rows → 400 — cost if wrong: T10 adjusts one call.
Ruling: debitAccountId override must be an active ASSET leaf that is a bank/cash account (by AccountSubType if the enum distinguishes them; else ASSET leaf that is not the lease's RENT_RECEIVABLE/PDC_RECEIVABLE account).
Task 7: fix round 1 queued (resume ad86a9341ae48f9c9) after T6's fix round lands.
Task 6: fix round 1 done (4771c169); backend 1484/0; PDR-date, per-cheque lock and override guards mutation-checked; PDRs now via LeaseChequeRegistrar; public requiredRoles(Lease) deleted (Ruling: accepted; T14 uses dryRun); BANK demanded only when a row lacks a debit account (accepted: clear-time validates). Cross-tenant override test asserts the tenant filter (applyToLoadByKey), not the scoped query — accepted, noted for the hardening item.
Task 6: re-review dispatched (sonnet) d382a1fa..4771c169. Task 7: fix round 1 dispatched (resume ad86a9341ae48f9c9) FIX_BASE 4771c169
Task 6: re-review clean (sonnet a14f76bfbd3619961). Task 6: complete (commits 4beffc80, 4771c169; review clean after 1 fix round)
Task 7: fix round 1 done (5ed5342d); backend 1504/0; both mutants killed. Gateway auth: manage-level via canManage, renter via requireReadable (own lease), null Authentication = webhook (signature check is the guard — T10 review must treat it as load-bearing). Concerns carried: renter can trigger replaceForOnlinePayment on own bounced cheque (intended; confirm with client at walkthrough); clearOnline fallback BANK vs gateway settlement mapping (T10: TenantGatewayConfig.settlementAccount); seq_no has no DB uniqueness (final-review triage: partial unique index in 84?); no LeaseEvent audit rows for cheque transitions (T11); notes is now a dated log (T15 UI).
Task 7: re-review dispatched (sonnet) 4771c169..5ed5342d. Task 8: BASE 5ed5342d — dispatching opus implementer
Task 7: re-review clean (sonnet a147c00e6917cfc06). Task 7: complete (commits d382a1fa, 5ed5342d; review clean after 1 fix round). CARRY to T10: webhook must live under /api/webhooks/** and verify the signature before any ChequeService gateway call (null Authentication = trusted there); add a ChequeServiceIT case as RENTER calling clear/bounce → refused.
Task 8: implemented (opus a80b4e6ad929f5701) commit 1fe63368; backend 1520/0; extend Σ guard + carry-by-ledger-balance mutation-checked. Deviations: extend validates in two stages (shape/Σ/lock first; then lines+rows written and account/role plan checked before the first journal; rollback proven by 3 ITs); markPredecessorRenewed also retires EXPIRED/NOTICE_GIVEN predecessors; "non-cancelled successor" = any successor (no CANCELLED status; a mistaken DRAFT is deleted). Concerns: web lease page + mobile POST the old extend body → 400 (web → T14; mobile → plan 5); wide package-private seam on LeasePostingService; extend has no dry run.
Ruling: unit.actualRent is NOT refreshed by an extension — it is the annual contracted rent; an extension lengthens the term, it does not re-rate the unit — cost if wrong: one line in extend.
Task 8: review dispatched (opus) BASE 5ed5342d..1fe63368. Task 9: BASE 1fe63368 — dispatching opus implementer
Task 8: review (opus a63f8ba576ef3bf4e) Needs fixes — single-hop journals hand-verified. C1 chained carry-forward strands the deposit (a flagged renewal has no DEPOSIT line, so the next carry finds no account). I2 EXPIRED/NOTICE_GIVEN predecessor retirement untested; I3 vacancy-exemption negative case untested; I4 markRenewedIfOpen at post untested; I5 DepositCarryForward public methods not @Transactional; I6 settlement still reads nominal depositAmount after a carry (wrong refund figure) ; minors: renew doesn't lock the predecessor, null tenant → silent zero carry, report stage description.
Ruling: carry-forward candidate accounts = DEPOSIT-line credit accounts collected by walking the renewal chain back (renewedFromLeaseId) from the predecessor; the balance is always measured on the IMMEDIATE predecessor's lease dimension — cost if wrong: a deposit parked on an account no ancestor line names is not carried (would need a by-balance query).
Ruling: I6 → T12 (it already edits SettlementService preview): deposit held = ledger credit balance of the deposit accounts on the lease dimension, not lease.depositAmount; a RENEWED lease with zero balance previews 0.
Task 8: fix round 1 (resume a80b4e6ad929f5701) FIX_BASE 7b504116
Task 9: implemented (opus a0b9d38e97dacd905) commit 7b504116; backend 1566/0; both mutants killed. Deviations: controller at /api/v1/penalty-assessments (v1 PenaltyController owns /penalties until T12 — Ruling: T12 renames to /api/v1/penalties; T13's penaltyApi targets the final path and T13 runs after T12); OTHER_INCOME template row added (accepted; AccountSeedNonPactChartIT pin 13→14); @Lazy cycle ChequeService↔PenaltyRuleEngine (reviewer to judge); LeaseAccessPolicy.visiblePropertyIds(); fine-settings PUT treats the 3 new fields as optional.
Ruling: onLateClear also fires from receive() and clearOnline() — late is late in any mode — T9 fix round.
Task 9: review dispatched (opus) BASE 1fe63368..7b504116
Task 9: review (opus afbe3125883dcdac0) Needs fixes, 0 Critical — approval gate airtight, PEN asserted whole. I1 R1 not done (onLateClear only in clear()); I2 onLateClear reads RentCollectionSettings.gracePeriodDays while the register uses lease.gracePeriodDays (comment claims same); I3 approve on a non-posted-status lease (e.g. TERMINATED) fails late with a cheque-grid message after posting the PEN (rolled back); I4 v1 nightly PenaltyService cron + markFailed still create renter-visible PaymentPenalty rows. Minors: redundant threshold re-resolution; FineConfig.Source semantics; no write path for property bouncesBeforePenalty (T16); CRT on a received collection row unasserted; MANDATORY propagation on the hooks.
Ruling: late-payment grace = lease.gracePeriodDays everywhere (the v2 lease carries it; the register already uses it) — cost if wrong: a property-level grace needs copying onto leases at draft time.
Ruling: approve is refused up front, before any posting, when the lease is not in a posted-and-live status (ACTIVE/NOTICE_GIVEN/EXPIRED/RENEWED) with "Lease is <STATUS>; charge this penalty through settlement" — plan 3 settlement owns post-termination charges.
Ruling: the v1 nightly penalty cron is disabled NOW (remove @Scheduled; class stays until T12) so no un-approved penalty row can reach a renter.
Task 9: fix round 1 queued (resume a0b9d38e97dacd905) after T8's fix round.
Task 8: fix round 1 done (7e12940b); backend 1575/0; chain walk mutation-checked (A→B→C). Notes: MAX_CHAIN_DEPTH 50 silent cap (accepted); concurrent renew surfaces the lock message (accepted).
Task 8: re-review dispatched (sonnet) 7b504116..7e12940b. Task 9: fix round 1 dispatched (resume a0b9d38e97dacd905) FIX_BASE 7e12940b
Task 8: re-review clean (sonnet a7ab80211d49832a5). Task 8: complete (commits 1fe63368, 7e12940b; review clean after 1 fix round). Deferred (final review): no IT with two live deposit accounts in one carry (correct by construction).
Task 9: fix round 1 done (3b5f7e43); backend 1585/0; lease-grace + requireChargeable mutants killed; v1 nightly cron disabled. Notes: MANDATORY inert in the `new`-built unit test (ITs cover it); replacement cheque lateness judged on its own date (Ruling: correct — the replacement is a new instrument with its own date); rent_collection_settings.grace_period_days loses its last consumer at T12 (final-review triage).
Task 9: re-review dispatched (sonnet) 7e12940b..3b5f7e43. Task 10: BASE 3b5f7e43 — dispatching opus implementer
Task 9: re-review clean (sonnet a2151ea2c73844ca7). Task 9: complete (commits 7b504116, 3b5f7e43; review clean after 1 fix round)
Task 10: implemented (opus a2a661dc8caac5b1a) commit 8337335d; backend 1603/0; OnlinePaymentServiceIT 20; both mutants killed. Deviations: NEW changeset 83b drops NOT NULL on online_payments.payment_schedule_id (NOT NULL since changeset 13); clearOnline idempotency only forgives ONLINE-mode rows so a PDC instalment paid online is compensated in OnlinePaymentService.capture (payment CAPTURED && cheque CLEARED → noop); webhook now fails closed on a missing webhook secret; payment.failed webhook handled; requireGatewayAccess duplicates gatewayLeaseOf's rule. Web/e2e still on old shapes (T13–15/17).
Ruling: fold 83b into changeset 83 in place (83 is unreleased on this stacked branch; one file per plan step keeps the numbering 83/84) — T10 fix round — cost if wrong: local DBs on this branch are recreated (already required).
Task 10: review dispatched (opus) BASE 3b5f7e43..8337335d
Ruling (T11): LeaseExpirationJob does NOT cancel cheques on expiry unless spec §7/§9 says so (a held, undeposited cheque still represents money owed; plan 3 termination owns returns); ChequeReceiptController folds into ChequeController; contract Section 4 from cheques with a non-empty IT; import Cheques sheet → saveRows on the draft (replaces the T4 warning); LeaseEvent audit rows on bounce/replace/cancel/return; default sort on paged register queries; PENALTY notification fires on APPROVE only.
Task 11: BASE 8337335d — dispatching opus implementer (parallel with T10 review; T10 fixes after T11 returns)
Task 10: review (opus a150024cb4f3ddd75) Needs fixes — webhook trust boundary verified sound (tenant from order id, that tenant's secret, fails closed). C1 second capture on a stale order for an ONLINE-mode CLEARED row is absorbed silently + success email (renter charged twice, one CRT); C2 captured amount/currency never validated against the cheque/order; I3 tenant→secret binding untested (stub matches any secret); I4 penalty half of getMyPayments cannot fail; I5 verifyPayment lacks requireGatewayAccess before release(); I6 capture for a row a clerk moved leaves only a webhook_log error. Minors: INR currency fallback (pre-existing), unordered activeConfig get(0), gateway call inside the tx, controller RENTER-only vs counter payments, X-User-Id identity (known P0), NPE on malformed payload.
Ruling: an unappliable capture (row already CLEARED by another payment, or moved by a clerk, or amount/currency mismatch) puts the OnlinePayment in a distinct terminal state CAPTURED_UNAPPLIED with a reason, log.error, webhook_log processed=true with the reason, NO success email/notification, no CRT — so reconciliation can find money that needs a refund — cost if wrong: one more status for the UI to label.
Task 10: fix round 1 (resume a2a661dc8caac5b1a) FIX_BASE 4e13d926 — also folds changeset 83b into 83.
Task 11: implemented (opus a997384b8b36eefcd) commits c4b18c94, 4e13d926; backend 1627/0; both mutants killed; final PaymentSchedule grep = T12 delete list + SettlementService. Concerns: imported ACTIVE leases carry DRAFT cheque rows visible on the register with no PDR; import booking-deposit row can push Σ cheques above contract value; findDue now includes BOUNCED of any date (matches ChequeDueRules); Section 4 ordered by seqNo; summary/aging compute overdue in Java over unpaged due rows; image retention job keeps v1's try/catch-in-tx shape.
Ruling: DRAFT cheque rows are grid rows, never register rows — every register list/summary/aging/post-dated/search excludes status DRAFT (T11 fix round); "imported leases are posted" is owned by plan 4 (cut-over import = drafts + bulk post), incl. the booking-deposit Σ question.
Task 11: review dispatched (opus) BASE 8337335d..4e13d926
Task 11: review (opus a01c3c83b82c64566) Needs fixes, 0 Critical — endpoint table complete, PM scoping in-query, jobs' tenant handling correct, rule-equivalence test real. DRAFT ruling gap is wider than reported: search returns DRAFT rows; statsByLeases has neither lease-status nor cheque-status filter; findRecentlyChanged/totalsByStatus unfiltered. I1 public generateFor/saveRowsFor(Lease) skip LeaseAccessPolicy; I2 sendPenaltyIncurred swallows an exception from a notify that joined approve's tx (use notifyInAppInNewTx); I3 five bulk-attach behaviours (dup schedule id, dup number in request, number used on lease, lease not found, CROSS-TENANT) ported but tests deleted; I4 DashboardService unscoped for PM (v1 parity) now disagrees with the scoped register; I5 BOUNCED-of-any-date makes the overdue reminder daily forever. Minors: statsByLeases unbounded IN; cashReceipt PDR dated today vs CRT dated row date; dead findImagesOlderThan; meeting cheque_ids no backfill (note for T12); details cannot set image (accepted: bulk attach is the image path); missing PM-foreign GET/{id} and cashReceipt-rollback tests; PortfolioImportIT asserts DRAFT rows.
Ruling: overdue reminders are sent on days-overdue 1, 3, 7, 14 and then every 30th day, capped at 180 days — bounded noise, still chasing — cost if wrong: cadence constants.
Ruling: dashboard money tiles/feed are scoped by LeaseAccessPolicy.visiblePropertyIds() exactly like the register (a PM sees only assigned properties).
Task 11: fix round 1 queued (resume a997384b8b36eefcd) after T10's fix round.
Task 10: fix round 1 done (3ba14ca9); backend 1638/0; amount + two-orders mutants killed; 83b folded into 83 (checksum changed — local DBs on this branch re-seed). Carried: CAPTURED_UNAPPLIED needs an operator surface (T15: register filter/tile "Online payments to refund" — add to T15 dispatch); client callback cannot verify the gateway amount (provider has no order fetch; webhook is the check) → final-review note; two active configs for one gateway resolve positionally (needs online_payments.tenant_gateway_config_id) → final-review triage; INR fallback → final review.
Task 10: re-review dispatched (sonnet) 4e13d926..3ba14ca9. Task 11: fix round 1 dispatched (resume a997384b8b36eefcd) FIX_BASE 3ba14ca9
Task 10: re-review clean (sonnet aacef4b6d41a1180c). Task 10: complete (commits 8337335d, 3ba14ca9; review clean after 1 fix round). Note: the legacy null-cheque OnlinePayment access-check gap disappears at T12 (online_payments.cheque_id NOT NULL; wipe-and-reseed).
Task 11: fix-round agent a997384b8b36eefcd was cut off by the account usage limit AFTER staging the complete fix and writing its report section; controller verified each ruled item in the staged diff, ran the full suite fresh (1682/0, 194 classes) and committed c6ed4e19. Mutation checks for items 1 and 4 were not confirmed by the agent's final message — the re-reviewer is asked to judge whether the new tests can fail.
Task 11: re-review dispatched (sonnet) 3ba14ca9..c6ed4e19. Task 12: BASE c6ed4e19 — dispatching opus implementer
Task 11: re-review (sonnet a831c2b15bb4c6035) Needs another round — items 1-4,6,7 Fixed (guards judged load-bearing); item 5 PARTIAL: DashboardService.getSummary portfolio block (totalProperties/units/occupancy/active-draft-expiring leases/totalRentRevenue) still org-wide for a PM. Minor: ChequeDetailsService.updateDetails lock() keeps the weaker null-tenant shape.
Task 11: fix round 2 (resume a997384b8b36eefcd) FIX_BASE 66716f7c
Task 12: implemented (opus a988bbd3cff7152f9) commits 214206b3 (settlement on ledger/register), 66716f7c (retire v1); backend 1578/0 (was 1682; −104 v1 cases). Found+fixed: TenantArtifactCleanupService raw SQL still read payment_schedules.cheque_image_* (would have broken tenant purge). Deviations: SettlementService.saveDraft also moved to the ledger deposit; penalty collection rows excluded from arrears (else double-deducted); NotificationServicePenaltyIncurredTest re-homes two live cases. Concerns: sendPenaltyCleared/Waived have no v2 successor (Ruling: accepted — a paid fine yields the normal receipt; a waived PROPOSAL was never visible to the renter; dead EmailEventTypes → final-review triage); settlement arrears still register-based (plan 3 replaces); MeetingDTO paymentScheduleIds JSON aliases kept (meetings screen not in T13–16).
Task 12: review dispatched (opus) BASE c6ed4e19..66716f7c
Ruling: web Task 13 (sonnet) runs in parallel with T11's backend round 2 — disjoint trees (web/ vs backend/), different build tools, explicit staging — cost if wrong: an interleaved commit order, no file conflicts.
Task 13: BASE 66716f7c — dispatching sonnet implementer
Task 12: review (opus acee54cb457f76d73) Approved — CASCADE blast radius and settlement arithmetic hand-verified; raw-SQL audit re-run clean. Follow-ups (fix round after T11 r2): I1 findDueForLease is a hand copy of findDue with no pinning test (no BOUNCED / draft-lease case); I2 replacing a bounced PENALTY collection row drops penaltyAssessmentId → the fine is counted in arrears AND penalties. Minors: null-tenant 500 vs tolerant guard in SettlementService; dead PENALTY_CLEARED/WAIVED wiring (final-review triage); vacuous V1FinanceRemovedIT case; weaker never() assertion.
Ruling: a replacement of a penalty collection row inherits penaltyAssessmentId and the assessment's collectionChequeId is re-pointed to the (first) replacement — one fine, one collection story.
Task 12: fix round 1 queued (resume a988bbd3cff7152f9) after T11 round 2.
Task 13: implemented (sonnet a66197cbccf43c74a) commit 637d2bc7; tsc clean; vitest 407 (57 new); lint 133 baseline. canManageLeases NOT widened (draft CRUD is SA/TA on the API); asOf added to summary/aging; bulkAttach/settlement/contract endpoints not in the client (T14/T15 add what they need).
Task 13: review dispatched (sonnet) BASE 66716f7c..637d2bc7 (web paths only)
Task 13: review (sonnet a353af3b608ab23aa) Approved — contract check against the Java clean. I1 (backend, not T13's): LeaseController read endpoints (list/paged/by-property/by-id/lines/events) lack ACCOUNTANT, so the role that may POST a lease cannot LOAD one → goes into the queued backend fix round (with T12's follow-ups). I2 DraftLeaseInput.paymentMethod typed wider than CHEQUE|ONLINE → narrowed in T14. Minors: LeaseLine.behaviour stricter than the wire type; recognitionSchedule Arabic wording.
Task 13: complete (commit 637d2bc7, review clean, no fix round)
Task 11: fix round 2 commit 5c83b72a observed on the branch (agent report pending).
Task 14: BASE = HEAD at dispatch — dispatching opus implementer (web only; backend fix rounds run alongside)
Task 11: fix round 2 done (5c83b72a); backend 1583/0; scope mutant killed (3 failures). Re-review dispatched (haiku).
Task 14: implemented (opus a64b65e6ac0e72299) commits d629d31a, 5ed1ad85, 44e12933, 2767065e; tsc clean; vitest 414; lint 131 (baseline 133); build ok. Concerns: 5 stale walkthrough specs reference v1 payments (T17 re-records); lease list status filter is client-side because GET /leases/paged has no status param (→ backend fix round adds it; T14 fix wires it); ACCOUNTANT lease reads pending backend; receipt download not wired (T15); replace dialog submits one row (T15's register dialog takes many); components/payments/*, BulkChequeUploadFlow, ChequeScanner, RecordPenaltyPaymentDialog now unreferenced (T15/T16 delete).
Task 14: review dispatched (sonnet) BASE 637d2bc7 (web paths)
Task 12: fix round 1 (resume a988bbd3cff7152f9): I1 findDueForLease pinning test, I2 penalty row replacement inherits penaltyAssessmentId, + ACCOUNTANT on LeaseController reads, + status param on /leases/paged.
Task 15: dispatching sonnet implementer (web only, parallel with the backend fix round)
Task 11: re-review r2 clean (haiku a646b28e68f40e93f). Task 11: complete (commits c4b18c94, 4e13d926, c6ed4e19, 5c83b72a; review clean after 2 fix rounds)
Task 12: fix round 1 done (5a1e4207, 5a... b70f770b); backend 1595/0; inheritance mutant killed. Notes: the penalty-row-bounce defect is latent (collection rows are CASH and cannot bounce) — fix kept as a guard; a fine split over several replacements reads collected when the first clears (accepted; link table if "collect fine by cheque" becomes real); paged leases still finish in memory for PMs/free-text (final-review triage). Re-review dispatched (haiku).
Task 14: review (sonnet a791e918353ae543c) Needs fixes, 0 Critical — post gate, VAT math, bodies, i18n, dates all verified. I1 ChequeGrid.actionsFor offers Receive on ONLINE_PENDING (server: REGISTERED+CASH/TRANSFER) and Bounce only on DEPOSITED (server also allows CLEARED PDC); I2 Terminate gated on canManageLeases (SA/TA) — PM lost it though the settlement API admits PM; I3 ACCOUNTANT reads (now fixed backend-side b70f770b); I4 client-side status filter (server param now exists → wire it); I5 discount>amount not blocking submit in Amend/Renew/Extend dialogs. Minors: unused chequeTotal prop; wizard refresh orphans a draft.
Task 14: fix round 1 (resume a64b65e6ac0e72299) — also deletes the orphaned components/leases/ChequeActionDialog.tsx left by T15.
Task 15: implemented (sonnet ac34d37ce8aa1bb85) commit 21fbebb9; tsc clean; vitest 428; lint 129; build ok. Gaps: no API lists CAPTURED_UNAPPLIED payments (tile not built); orphaned components/leases/ChequeActionDialog.tsx (sandbox refused git rm); bulk-upload copy says "Installment". Review dispatched (sonnet).
Ruling: add GET /api/v1/online-payments/unapplied (SA/TA/ACCOUNTANT; CAPTURED_UNAPPLIED rows with reason, amount, renter, cheque, gateway payment id) — small backend addition via the T10 implementer; T16 adds the "Online payments to refund" tile — cost if wrong: one read endpoint.
Task 12: re-review clean (haiku aca62bda6dd356e53). Task 12: complete (commits 214206b3, 66716f7c, 5a1e4207, b70f770b; review clean after 1 fix round). Backend tasks 1-12 all complete.
