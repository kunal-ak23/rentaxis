# SDD ledger — plan: docs/superpowers/plans/2026-09-17-accounting-v2-plan5-seed-golden-walkthrough.md
Spec: docs/superpowers/specs/2026-09-17-accounting-v2-design.md (§12–§13). Stacked on plan 4's branch (Kunal's "stack them" directive); branch feat/accounting-v2-seed-golden cut from the plan 4 branch head; PR base = plan 4 branch. Next free changeset after plan 4 = 89 (plan 4 owns 87/88).
## Pre-flight scan (2026-09-21) — full tables in preflight.md beside this file (opus scanner, against BUILT plans 1–3; plan 4 in flight)
Both PACT GL exports found and readable in ~/Downloads ("General Ledger- tenant1.xlsx" REPORT TOTAL 248,150; "General Ledger tenant 2.xlsx" REPORT TOTAL 157,000) — never copied into the repo; fixtures are hand-transcribed per the plan.
GOOD NEWS: all 26 hard-coded recognition figures re-derived under the built ProrationEngine reproduce exactly (rates 150.684932 / 139.726027; Σ 55,000.00 / 51,000.00; both report totals).
Headline findings needing rulings when plan 5 starts:
1. T1's two "product gaps" are ALREADY closed (ledgerRows ordering; per-line particular via contra_account_id) → T1 Steps 8–9 are DROPPED (they would replace explicit pairing with an amount heuristic).
2. Golden fixtures expect BLANK narration on TCO rows; LeasePostingService.narrationOf falls back to the charge type's English name → undeclared deviation: decide blank-vs-name (PACT's Remarks is blank).
3. LeaseService.createDraft/post do not exist (createDraftLease(CreateLeaseDTO) + LeasePostingService.post); CreateLeaseDTO drops cheques[] AND contractNumber; the grid goes through PUT /leases/{id}/cheques.
5. POST /finance/recognition/run rejects to > today (golden tests call the SERVICE runTo under a fixed Clock instead); runTo is 2-arg; scheduleFor returns records.
6. ChequeService signatures (ChequeActionRequest / ReplaceChequeRequest → ChequeDTO) and HTTP verbs are PUT, not POST.
7. Seed Tasks 4–5 target text plans 2/3 already removed; real routes /finance/fiscal-settings, /properties/{id}/accounts; 8 manifest keys never written.
8. Charge-type codes are COOLING / MAINTENANCE; /finance/vendor-ledger and /settings/charge-types do not exist; verifiers hardcode 33 in 3 and 7 places.
9. web/e2e/finance/payments-pdc.spec.ts does not exist; prod-client.ts already ships the cheque helpers with different signatures; the plan never references web/walkthrough/accounting-v2-plan{1,2,3}.spec.ts (the per-phase proof precedent).
10. Mobile: every 404 surface confirmed; terminate posts {notes} with no date; settlement screen reads removed fields; NO Dart feature-flag infrastructure exists today; GET /api/v1/tenant/features already returns the flat map.
Deferred-into-plan-5 items ABSENT from the plan text: unit.current_lease_id (spec §6.4), zero-line-entry assertion, trial-balance missing-account assertion, REFUNDED contract, the expiry walkthrough scenario plan 3 skipped.

## Rulings on the pre-flight (written 2026-09-21 while plan 4 is in flight; binding when plan 5 starts; "built code wins over plan text")
Ruling P5-R1 (2.1): Task 1 keeps ONLY the fixture format, the readable comparator (LedgerDiff) and the Gradle tag wiring. Steps 6–9 (re-ordering ledgerRows; replacing contra-account pairing with an amount heuristic; deleting counterAccounts) are DROPPED — the built ledger already orders by entry_date, created_at, entry_number, line_no and carries an explicit per-line contra account — cost if wrong: none; the golden ITs prove the order.
Ruling P5-R2 (G-1): TCO narration — PACT's Remarks column is blank on contract rows, but a blank narration on our own ledger rows is worse for the accountant than the charge type's name, and the lease editor lets the user type one. The PRODUCT keeps narrationOf's fallback (charge type name); the golden COMPARATOR treats narration as "equal, or the fixture is blank" for doc type TCO only, and the deviation is added to the plan's "Deviations from PACT" table — cost if wrong: one comparator branch. CIL narration ("Advance rent adjustment – Sep 2025", en dash) is compared exactly.
Ruling P5-R3 (1.2, G-11): golden ITs and the seed build leases the BUILT way — LeaseService.createDraftLease(CreateLeaseDTO) → ChequeGenerationService.saveRows(leaseId, rows) → LeasePostingService.post(leaseId); over HTTP: POST /leases → PUT /leases/{id}/cheques → POST /leases/{id}/post. CreateLeaseDTO silently drops cheques[] and contractNumber (@JsonIgnoreProperties) — never rely on either. PACT's contract string uses plan 4's leases.external_contract_ref.
Ruling P5-R4 (1.3, G-15): golden ITs drive recognition through RecognitionService.runTo(to, false) under a fixed Clock (the HTTP endpoint refuses to > today by design); future-dated journals are allowed by PostingService; scheduleFor returns records (periodStart()).
Ruling P5-R5 (1.2): cheque transitions use the built signatures (ChequeActionRequest / ReplaceChequeRequest → ChequeDTO) and HTTP verbs PUT; prod-client.ts already ships createLease/postLeaseFlow/getLeaseCheques/runRecognition/deposit/clear/bounce/replaceCheque — REUSE them; do not add colliding helpers.
Ruling P5-R6 (§4): the seed tasks are re-based on the CURRENT scripts/seed_demo_tenant.py (plans 2–3 patched it): real routes /finance/fiscal-settings and /properties/{id}/accounts; charge-type codes COOLING / MAINTENANCE; every manifest key the verifiers read (accounts, fiscal, leaseStatus, cheques, recognitionRunTo, vouchers, renterIds, adminUserId) is written; prod demo-tenant gotchas from memory apply (accounts seed, deposit cap, Caddy routing).
Ruling P5-R7 (§5): the per-phase proof precedent is web/walkthrough/accounting-v2-plan{1,2,3,4}.spec.ts; plan 5's dev e2e spec and tutorials 34–37 build on those helpers and testids; routes that do not exist (/finance/vendor-ledger, /settings/charge-types) are replaced by the real ones (find them in MvpSidebar); web/e2e/finance/payments-pdc.spec.ts does not exist (nothing to git rm); the verifiers' hard-coded "33" is updated in ALL places (3 and 7).
Ruling P5-R8 (§6): MOBILE_FINANCE flag as designed (default OFF → finance surfaces hidden/redirected), built on a NEW small Dart feature-flag provider reading GET /api/v1/tenant/features (no such infrastructure exists today; precedent file app_version_provider.dart); it MUST cover the screens plans 3 broke: lease terminate ({notes} with no date) and lease settlement (removed preview fields) — covered transitively by the /leases redirect on the manager app; /penalties (renter) and /queue are NOT gated (their endpoints exist — over-reach dropped); /vendors/:id checked for a report_service call. Mobile stays frozen otherwise. adb install -r when testing.
Ruling P5-R9 (§7, items absent from the plan text): add to Task group A — (a) a posting with zero lines is refused (PostingService IT if not already present — check first); (b) trial balance IT asserting EVERY account with a posted line appears (missing-account assertion); (c) the REFUNDED online-payment status stays a pinned contract (no flow) — note only; (d) unit.current_lease_id (spec §6.4) is NOT built and not needed: occupancy is derived from LeaseService.LIVE + the partial unique index — recorded as a spec deviation in the PR, no column; (e) the expiry walkthrough scenario plan 3 skipped stays covered by LeaseExpirationJobIT — no HTTP entry point is added for a nightly job.
Ruling P5-R10 (§9): branch feat/accounting-v2-seed-golden stacked on plan 4's branch; PR base = plan 4 branch; next free changeset 89 (none expected); implementers use their own Co-Authored-By trailer; final deliverable includes the plan 5 walkthrough/tutorial recordings sent to Kunal and the stacked PR.
Ruling P5-R11 (privacy, added 2026-09-22): the golden-ledger fixtures replay two REAL tenants' PACT ledgers; the plan text names them (renter names, property names, contract numbers). Fixtures, test class names, seed data, tutorials and recordings MUST use anonymised names (e.g. GoldenLedgerTenantOneIT / "Sample Renter One" / "Sample Tower"); only the AMOUNTS, DATES and DOCUMENT SEQUENCE come from the real exports. The real files stay in ~/Downloads and are never committed. The spec/plan docs already in git name them — raise with Kunal whether to scrub history-free (edit the docs going forward).
- 2026-09-22 pre-start check: both PACT exports present (~/Downloads/General Ledger- tenant1.xlsx 16,625 B; ~/Downloads/General Ledger tenant 2.xlsx 15,273 B); plan 4 final fix waves in flight; plan 5 branch not yet cut.
- 2026-09-22 13:58 branch `feat/accounting-v2-seed-golden` cut from plan 4 head d9aea28a (PR #319 open, base plan 3 branch). BASE for Task 1 = d9aea28a.
- Task 1 dispatched (opus) 2026-09-22 14:02 from BASE d9aea28a; brief `task-1-brief.md` + rulings P5-R1/R2/R11 + G-3/G-7 in the dispatch.
- Task 4 dispatched (opus) 2026-09-22 14:06 in PARALLEL with Task 1 (disjoint: scripts/ vs backend/; Gradle-free; runs against the local :8081 stack). Ruling P5-R12: parallel dispatch allowed only for tasks with disjoint file trees and no shared build tool — cost if wrong: an index.lock retry. Task 5 (same script) waits for Task 4.
- Task 4 returned DONE 1b28c4f3 (seed: chart/fiscal/account sets/vendors/vouchers; idempotent run proven; manifest keys accounts/fiscal/vouchers/adminUserId). Concerns: contractDate pinned to 1 Jan inside the lease body (carry to Task 5); vendors+vouchers moved before the listings section; DEMO_GUARD_PHONE for a 2nd local tenant. Task 4 review dispatched (sonnet) on review-task-4.diff.
- Task 1 returned DONE_WITH_CONCERNS: e540cdba (GoldenLedgerFixture, LedgerDiff, 5 anonymised sample CSVs, 28 unit tests), 666cfabd (build.gradle tag filter, verified by probe). Rulings: **P5-R13 naming convention (binding for Tasks 2–3):** tenant one → property "Sample Tower", renter "Sample Renter One", contract ref "SAMPLE-25/001", class `GoldenLedgerTenantOneIT`, fixtures `tenant-one-*.csv`; tenant two → "Sample Residences", "Sample Renter Two", "SAMPLE-26/001", `GoldenLedgerTenantTwoIT`, `tenant-two-*.csv`. Each golden task transcribes its own fixtures from the real export in ~/Downloads (amounts/dates/sequence only) — cost if wrong: renaming 77 rows once. Task 2 adds one direct assertion of the TCO narration fallback (charge-type name). `failOnNoDiscoveredTests` no-op noted. Trailer per P5-R10. Task 1 review dispatched (opus) on review-task-1.diff.
- Task 4 review (sonnet): Spec ✅ · Quality Approved (2 minors: size=200 lookup cap; contractDate pin sits in make_lease — subsumed by Task 5). **Task 4: complete.** Task 5 dispatched to the same implementer (resume) from BASE 666cfabd.
- Task 1 review (opus): Spec ✅ · Quality Approved; three Importants that are requirements on Tasks 2–3, not defects in Task 1: I-1 the golden tag gate passes vacuously until a `@Tag("golden")` test exists; I-2 no account-set comparison; I-3 TCO narration tolerance needs a direct assertion. **Task 1: complete** (no fix round). Ruling P5-R14 (binding for Tasks 2–3): each golden IT is `@Tag("golden")`; asserts `actual.keySet()` equals the fixture's account set exactly (extra product accounts such as OUTPUT_VAT or penalties fail the test — pre-flight G-5/P-2); asserts `reportTotal == debitTotal == creditTotal == the export's REPORT TOTAL`; Task 2 asserts the TCO narration fallback (charge-type name) directly once. Cost if wrong: one over-strict assertion to loosen. Task 2 dispatched (opus) from BASE 666cfabd (Task 5 runs in parallel on scripts/).
- Task 5 returned DONE fedf1aff (typed lease lines + explicit cheque grid, lifecycle deposit/clear/bounce/replace, recognition to 2026-08-31 → 24 CILs; TB 1,033,156.14 both sides; manifest leaseStatus/cheques/renterIds/recognitionRunTo; run 2 idempotent). Ruling P5-R15: the PRODUCTION demo tenant is NOT re-seeded by this plan — the new grids only materialise on a tenant without leases, and `--reset` would delete the App Store reviewer's tenant; Task 17's "demo re-seed" runs against the LOCAL stack only, and the prod re-seed (fresh demo tenant vs reset) is Kunal's deploy-time decision, stated in the PR. Cost if wrong: none — nothing irreversible happens without Kunal. Task 5 review dispatched (sonnet) on review-task-5.diff.
- Task 6 dispatched (opus) in parallel (web/e2e only; Gradle-free) from fedf1aff; Task 5 review in flight; Task 2 in flight.
- Task 5 review (sonnet): Spec ✅ · Quality Approved; minor round() half-to-even fixed by the controller (36c6c90c). **Task 5: complete.**
- Task 8 dispatched (sonnet) in parallel (tutorials/ only) from 36c6c90c. Task 7 held: it is the PRODUCTION Playwright suite — ruling pending on how it is verified without touching prod.
- Ruling P5-R16: Task 7 (production Playwright suite) is implemented and verified against the LOCAL stack only, via `PROD_BASE_URL=http://localhost:3001` (the config honours it; the seed used the same switch); it is NEVER run against production by this session — Kunal runs it at deploy time. Dispatch after Task 6 finishes so two provisioning suites do not share the local backend at once (unique guard phone etc.). Cost if wrong: one local re-run.
- Task 8 returned DONE afa75c3e (adapter; route map re-pointed, 154 routes mapped 01–37; verifiers' ceiling 37 in all 10 places; library verifier fails only on the 34–37 storyboards/matrix rows Task 9 writes). Review (sonnet) + Task 9 (sonnet) dispatched in parallel.
- Task 2 returned DONE 97daf6c3 — **the first golden replay matched PACT's export on the first run** (64 rows / 7 accounts / report total 248,150.00; 13 CILs Σ 55,000.00; 8 tests, 2 mutations red). No product defect. Carry to Task 3: template account names are HYPHENATED ("Rent Receivable - Sample Residences"); pre-flight §2.2 row counts (41/36) are wrong — the plan's fixtures are the authority; bounce-return auto-propose threshold 2 → a PROPOSED assessment with no journal (assert, don't silence); `runTo` takes no Clock (dates explicit). Concern to review: same-day ordering rests on created_at microsecond ties (entry_number sorts PDR- before TCO-). Task 2 review dispatched (fable) on review-task-2.diff.
- Task 8 review (sonnet): Spec ✅ · Quality Approved (3 minors: forward-referenced evidence now committed as 97daf6c3; adapter seed-contract list not exhaustive — fine). **Task 8: complete.**
- Task 9 returned DONE c8341b7b (storyboards 34–37, narration generated, matrix rows; both verifiers pass; extract-narrations.mjs ceiling also bumped). Review (sonnet) dispatched. Ruling P5-R17: Tasks 10–13 (record tutorials 34–37) are same-shape work → ONE batched dispatch after the Task 9 review, against the local stack's seeded demo tenant.
- Task 6 returned DONE da35550d (`web/e2e/finance/accounting-v2.spec.ts`, 7 serial scenarios; spec alone `13 passed (36.6s)`). Folder run `3 failed / 18 passed`: the three failures (`cheques.spec.ts:72` late return reopens 0 not 12,750; `cheques.spec.ts:210` settlement-status never renders after finalize; `ledger.spec.ts:66` chart collapsed for an already-seeded tenant) are PRE-EXISTING on this branch (proven by a byte-identical baseline run) and may be product regressions from plan 4. Ruling P5-R18: triage them NOW as plan 5 work (the plan's Iron Law: no capture without a passing proof; Task 17 needs green suites) — test drift is fixed in the test; a product defect is reported with root cause and NOT patched in the backend without a ruling. Task 6 review (sonnet) + e2e triage (opus) dispatched; Task 7 follows the triage so two provisioning suites do not share the stack.
- Task 2 review (fable): Spec ✅ · Quality Approved; fixture verified against the brief AND the real xlsx on every non-CIL row; 8/8 re-run. **Task 2: complete** (no fix round). Rulings: **P5-R19** (I-1) the spec-D2 manual-remapping proof moves to Task 3 — tenant two remaps two roles to manually named accounts (anonymised) before posting, so one replay proves a remapped set; P5-R11 never required template names, only anonymised ones. M-2: Task 3 also adds `hasSize(5)` to its TB assertion and `hasSize(7)` to Task 2's. M-1 (same-day ordering rests on created_at µs; durable fix = engine posting_seq / doc-type rank) → issue. M-4: CIL narration recorded as the fourth fixture deviation in the PR's deviation table. Task 3 dispatched (opus) from BASE $(git rev-parse --short HEAD).
- Task 9 review (sonnet): Spec ✅ · Needs fixes — one Important (storyboard 35 said "Mark returned"; the app's action is "Bounce") fixed by the controller (8f372362, narration regenerated, verifier passes). **Task 9: complete.** Task 6 review (sonnet): Spec ✅ · Approved (xpath Sub-Total locator noted; needs a testid later). **Task 6: complete.** Tasks 10–13 batched dispatch (opus) from BASE 8f372362 per P5-R17; e2e triage still running on the stack — the recorder seeds and validates first, and checks no other Playwright run is active before each capture.
- Task 15 dispatched (opus) in parallel (mobile/ only; flutter, no Gradle) from 8f372362 — it needs only the flag NAME  (Task 14 adds the enum constant; absent key = default OFF). Task 16 follows Task 15 (shared rentaxis_core provider).
- Task 3 returned DONE 3cb061e3 — **the second golden replay matched on the first run** (40 rows / 5 accounts / 157,000.00; 13 CILs Σ 51,000.00; two roles hand-remapped per P5-R19; 16 golden tests green; 2 mutations red). Concerns accepted: tenant-default fall-through (clearMapping + setTenantDefault) is not golden-covered → noted for the PR (unit-tested in plan 1); property-filtered TB guarded by hasSize + containsOnlyKeys. Task 3 review (opus) dispatched; Task 14 (sonnet, mechanical brief with complete code) dispatched in parallel — Gradle lock waits are tolerated.
- e2e triage returned: folder `25 passed (51.6s)` (baseline 3 failed/18 passed). 0e2efbfb fix(web) chart auto-expand for a seeded tenant (UI defect); ac6ea3cf fix(web) stale termination-pricing response could end a contract on the wrong date (UI defect, vitest-guarded); 0f4c75d4 test(web) cheque suite drift (renter ledger sum included the landlord's bank leaf; settlement needs Save draft before Finalize; penalty testids). Issues: #321 shared-tenant flakiness, #322 Finalize before draft, #323 collapse-all. Triage review (opus) dispatched; Task 7 dispatched (stack free) per P5-R16.
- Task 7 dispatched (opus) from 7cb3129e — local stack only (P5-R16); coordinates with the recording batch via a one-shot process check.
- Task 14 returned DONE 7cb3129e (MOBILE_FINANCE default false; IT 3/3; no migration; prod-client union widened). Review (haiku — mechanical diff) dispatched.
- Task 14 review (haiku): Spec ✅ · Approved. **Task 14: complete.** (Trailer per P5-R10 — implementers use their own.)
- Task 3 review (opus): Spec ✅ · Approved; I-1 (row-count assertion on a property-filtered TB cannot see a dimension-less stray) fixed by the controller in both classes → tenant-wide TB, golden gate 8/8 + 8/8 (4891e8ed). **Task 3: complete.**
- Task 15 returned DONE 7c86c690 + 4cc68df3 (tenant-features provider failing closed; manager finance/lease/cheque screens hidden + redirected; core 144/manager 147/renter 229 tests green; route verifier 44/44). Deviations accepted pending review: provider keyed on session identity; MiftahNavBar accepts 3 items; Vendors kept visible (P5-R8). Review (opus) + Task 16 (resume the same implementer) dispatched in parallel.
- Task 17 plan: controller runs the full backend suite in its own background Bash; steps 3–6 (web/mobile/verifiers/dev Playwright) go to one sonnet verifier when the stack is free; step 7 LOCAL re-seed only (P5-R15); step 8 local only (P5-R16); step 9 plan boxes + CLAUDE.md (next changeset 89, plan status); step 10 PR stacked on #319 — body drafted at pr-body-draft.md.
- e2e triage review (opus): Needs fixes — 1 Important (auto-expand ref-guard vitest cannot see the refetch; mutation would pass) + 2 minors (vacuous replace assertion; waitForResponse on the 15th). Fix round 1 → resumed the triage implementer. CLAUDE.md refreshed (b4e378a5, Task 17 step 9). Full backend suite started in the controller's background at 4891e8ed (/tmp/p5-full-backend-test.log).
- Task 15 review (opus): Spec ❌ · Needs fixes — I-1 Account Mappings tile still shown (hide it), I-2 MiftahNavBar centre mis-placed for 3 items (hits Task 16's renter shell). Fix round 1 folded into the Task 16 implementer's current run (same agent) before its Task 16 commits.
- Triage fix round 1 returned: 4d425a90 (ref-guard vitest waits for the refetch; mutation red at the assertion), 1cb72fa8 (cheque balance = role allow-list, anchored at −12,250, waits for the CRT — also explains the original 0/−12,750 baseline inconsistency). cheques.spec 8/8, vitest 6/6. Scoped re-review (sonnet) dispatched.
- Triage fix round re-review (sonnet): accepted. e2e triage closed.
- Task 15 fix round + Task 16 returned: af05ccff (tile hidden; 3-tab centre via an empty trailing column), 09ceee88 (renter Wallet hidden + redirected; /penalties ungated per P5-R8). core 148 / manager 148 / renter 240 green; renter 26/26. Issues: #324 flaky core suite, #325 renter hero when OFF. Brief Step 6 (seed flag explicit + App Store review notes) is outside mobile/ → controller does it. Review (opus) dispatched.
- Task 16 Step 6 done by the controller (2a916cd6): seed sets MOBILE_FINANCE=false explicitly; App Store review notes (Resident/Manager steps + 5.6 §2) now disclose the sixth switch and its OFF state — the earlier 'no screen is hidden by a flag' claim would have been false.
- Task 7 returned DONE 2e0a9fed (`13h-accounting-v2.spec.ts` + 01-provision seeds the books before the property, lease wizard/cheque grid/extendLease moved to v2). Local run (filtered to numbered specs): `32 passed, 2 failed` — both backend-side and reproduced twice: `99-cleanup` tenant delete 500 with a posted lease (REAL defect → #326), `10a` Azurite URL not recognised (local-only → #328); tutorial-state specs abort unfiltered collection → #327. W-19: the journal API refuses to reverse a TCO, so 13h asserts the refusal and reverses a manual JV. Ruling P5-R20: #326 is fix-before-prod-run (the review tenant would leak on every deploy) but NOT in this plan's scope (backend TenantDeletionService; plan 5 adds no backend production code) — stated in the PR. Task 7 review (opus) dispatched.
- Task 7 review (opus): Spec ✅ · Approved; M-1 README done by the controller; M-2/M-3 (vacuous Σbalance assertion; real Gmail alias) → fix round with the Task 7 implementer. **Task 7: complete pending that commit.**
- Tasks 10–13 returned: **34 approved** (84c7f96b: 129 s, 5/5 QA frames), 35–37 recorded but REJECTED (876a5819: 10–15 of ~63 sampled frames show the loading shell — machine load 15–28 from the concurrent prod suite + full backend suite + flutter runs; measured, not assumed). Ruling P5-R21: re-record 35–37 on an idle machine — after the full backend suite and the Task 7 fix round finish and before the Task 17 verifier runs anything heavy; 35/36 need a fresh tenant. Product findings: Sara's draft has no admin-fee line though storyboard 34 names one (storyboard or seed to align); recognition pending rows all read "Open contract" (no reference) → issue. Throwaway tenants left on local rentaxis_v2 (fine). TTS = Azure (as tutorial 02).
- Task 16 review (opus): Spec ✅ · Approved; Task 15 fixes verified; one Important (renter home guards untested) → fix round 1 with the mobile implementer. Flaky core suite did not reproduce in 5 runs (#324 stays open). README 13h + filtered command committed (283ff5f3); #329 recognition list labels.
- 15:55 controller stopped two self-matching `until pgrep -f 'playwright test'` wait loops left by the recording agent (the memory lesson recurs: tell recorders no wait loops). Task 7 fix round committed 8750c3df; mobile fix round active; full suite ~45 min in.
- Task 7 fix round 1: 8750c3df (reversal proven on the two accounts across three states; synthetic renter email, no portal invite). tsc clean; NOT re-run locally (stack treated as busy) → the Task 17 verifier's local prod-suite run is its proof. **Task 7: complete pending that run.**
- **SECURITY (2026-09-22 15:56): a crypto-miner is running on this Mac** — `xmrig` pid 54962, user kunalsharma, ~280–440% CPU, started 10:03:14 today, parent launchd (orphaned), binary `/private/tmp/rigupdater/xmrig-6.26.0/xmrig` (staging dir born 2026-09-02, binary dated 2026-03-28), mining to `pool.hashvault.sh:443`, holds `/Library/Preferences/Logging/.plist-cache.2ADMDOFU`. Verified read-only by the controller; NO session transcript contains a launch — the only mentions are the recorder's `ps` observations, so none of this session's agents started it. No LaunchAgent/Daemon/crontab/shell-rc reference found; persistence unknown. This is the real cause of the rejected takes 35–37 (load 16 with Playwright idle). Nothing killed or removed — Kunal's decision (security-sensitive). Re-record and the Task 17 verifier are blocked on an idle machine.
- Task 16 fix round 1: 3a299b7c (renter home guards pinned; 2 mutations red). The implementer then showed pre-flight §6.1's renter row is WRONG: the Wallet reads `/api/v1/online-payments/my-payments` (v2 cheques, `OnlinePaymentServiceIT` green), `my-leases`, penalties — nothing broken. **Ruling P5-R22:** un-gate the renter app (revert the renter side of 09ceee88 + 3a299b7c; keep the shared provider and the manager gating; MOBILE_FINANCE gates the Manager app only) — hiding a working pay-rent path from tenants would be a product regression; cost if wrong: re-applying a reviewed diff. App Store notes corrected (Resident step 3 restored; 5.6 §2 says Manager only). Fix round 2 → same implementer.
- Task 16 fix round 2 / P5-R22: 29fd8269 reverts the renter gating (renter app byte-identical to pre-plan-5, 229 tests; core 148; manager 148; routes 44/26). Controller verified `git diff 09ceee88^ HEAD -- mobile/apps/renter` is empty. **Task 15: complete. Task 16: complete as "no renter surface is broken; nothing to gate" (P5-R22).** Pre-flight §6.1/§6.2 renter rows and the Task 16 brief describe the withdrawn design — historical documents, left as-is; the PR states the outcome.
- Task 17 verifier dispatched (sonnet): web/mobile/verifiers/local seed proof now; dev + prod Playwright after the backend suite's EXIT=; re-record 35–37 still blocked on the miner (Kunal's decision).
- Full backend suite at 4891e8ed (no backend commits since): BUILD SUCCESSFUL in 56m 31s — 225 classes, **2284 / 0 / 0 / 0**; golden 16 ITs + 11 harness unit tests. (56 min vs 30 — the miner.)
- Task 17 verifier returned: web PASS (tsc clean, lint 206 = baseline, vitest 958/958); mobile PASS (148/148/229); verifiers PASS (154 routes, 37 tutorials); prod suite (local) PASS modulo the documented #326/#328 and two load one-offs that passed on re-run; dev Playwright all-projects **75 failed / 77 passed** — 73 are `networkidle` 30 s timeouts (miner load + 47 accumulated test tenants on rentaxis_v2), plan 5's own spec green, `e2e/auth/login.spec.ts` pre-existing (also on main) → #330; local seed proof FAILED because the RUNNING JAR predates 7cb3129e (MOBILE_FINANCE) → controller's omission: rebuild + recreate rentaxis_v2 + restart now (also empties the 47 tenants), then re-run the seed proof and, once the miner is gone, the dev suite + retakes.
- 16:45 stack rebuilt (jar from HEAD with MOBILE_FINANCE; rentaxis_v2 recreated → 129 changesets; UP). **Local seed proof PASSED** on a fresh tenant: MOBILE_FINANCE explicitly off, books open 2026-01-01 locked through 2025-12-31, 15+15 account-set roles, 3 leases posted (5/5/13 instruments), lifecycle (3 cleared … 1 replaced … overdue), PISR-26/1 + BPV-26/1, recognition to 2026-08-31 → 24 CILs 177,756.14; manifest leaseStatus ahmed/fatima/rajesh ACTIVE, sara DRAFT (P5-R15: local only).
- **Task 17 status:** steps 1–5, 7, 8, 9 PASS; step 6 (dev Playwright all projects) environmental FAIL (miner load; plan's specs green; `e2e/finance` 25/25 earlier) → re-run when the machine is idle; tutorials 35–37 retakes likewise. Ruling P5-R23: open the stacked PR NOW with those two items stated as pending-on-an-idle-machine rather than hold the plan on a compromised host; retakes + a clean dev-suite run follow as commits on the same branch. Ledger snapshot → docs/superpowers/handoff/2026-09-22-plan5-sdd-ledger.md.
- **PR #331 opened** (base feat/accounting-v2-vouchers-cutover): plan 5 = 17/17 tasks through review; pending on this branch: retakes 35–37 and a clean all-projects dev Playwright run on an idle machine (P5-R23). Tutorial 34 sent to Kunal.
- 16:55 Stop-hook push-back: plan 5 is not wrapped until retakes 35–37 and a clean all-projects dev run exist. Load now ~12 on 10 cores (miner ~4.4 cores, nothing else heavy). Recorder resumed: probe retake of 37 on a fresh seeded tenant against the rebuilt stack; if clean → 35, 36 on fresh tenants; if rejected → stop after one take. The dev suite re-run follows the retakes (never concurrently).
- Retakes: **35, 36, 37 approved** (659395b5, 9a3fe132, 6ef1e6b5) at load 12.9–15.2 on the rebuilt stack; loading-shell scans 9/68, 4/64, 3/59 (34: 1/65). Findings: size-based frame scan missed a 235 KB half-loaded journals scene (fixed by waiting for network idle — the scan is a screen, not the test); 35 carries two documented residuals (scene seams trail narration ~4 s; busier register refreshes); narration-vs-seed mismatches (34 admin-fee line; 36 mid-month contract) → issue. All-projects dev Playwright re-run started in the controller's background (/tmp/p5-dev-e2e-2.log).
- Retake commits pushed; PR #331 body updated via REST (`gh pr edit` is broken by the Projects-classic GraphQL deprecation — use `gh api -X PATCH …/pulls/N --input`). Tutorials 35–37 sent to Kunal. #332 narration-vs-seed mismatches. Dev suite re-run in progress (42/400).
- Dev suite all-projects re-run at 6ef1e6b5: 76 failed / 73 passed — 62 `networkidle` timeouts, 52 in the `super-admin` project. ROOT CAUSE (verified with a request-level probe, curl through the proxy, and the backend log): `GET /api/v1/tenant/features` + `/tenant/info` 500 for a SUPER_ADMIN with no organisation selected (`TenantContextHolder.getTenantId()` null → NPE / `findById(null)`), and `useTenantFeatures` rejects non-ok responses without consuming the body, so Chromium never marks the two requests finished → `networkidle` never fires on any super-admin page. Pre-existing (controller and hook unchanged for months; no CI runs the dev suite), NOT the miner. Fix wave (sonnet): backend returns defaults / empty info for a tenant-less caller + MockMvc IT; hook cancels the error body + vitest. Then rebuild jar, restart, re-run the dev suite.
- Second triage (opus) dispatched for the 24 non-super-admin dev-suite failures (login heading #330, logout, stale rbac route lists incl. removed /finance/payments, users-crud under tenant-admin, lease wizard/lifecycle waits, cutover #321) — runs single specs only; whole-suite re-run by the controller after the super-admin fix + jar rebuild.
- Super-admin networkidle fix landed: 3a431bc9 (controller answers a tenant-less caller with defaults / empty info; MockMvc IT 3/3, mutation red) + 81539e9f (hook cancels error bodies; vitest 5/5). Jar rebuilt 17:38 and backend restarted (DB kept — it holds the retake tenants; the super-admin project uses its own auth state).
- 17:42 super-admin probe after the fix: `dashboard/home.spec.ts --project=super-admin` → 9 passed (was 3 failed). Full backend suite at 81539e9f started in the background (/tmp/p5-full-backend-test-2.log). Whole dev suite re-run waits for the second triage's commits.
- Second triage returned: 7043a6f5 (profile-menu/logout testids + vitest), 48f9e738 (login/logout/rbac/users-crud/leases specs realigned; bulk-post provisions its own draft). All 24 were TEST-DRIFT; no product defect; vitest 961/961; the seven specs across all projects 67 passed / 4 super-admin failures that the tenant-feature fix (already rebuilt) addresses. Class of failure "undrained non-2xx bodies keep requests in flight → networkidle unusable" → #333. Whole dev suite re-run #3 started (/tmp/p5-dev-e2e-3.log).
- Wrap-up fixes review (sonnet): Approved; one Important (users-crud parallel race) fixed by the controller (923d5f03, serial mode). Waiting on dev suite #3 and backend suite #2 for the final push.
- Dev suite #3 (with the two fixes; backend suite running concurrently): **115 passed / 28 failed** (was 73/76). Clusters: (A) renter-portal + anonymous login `page.goto`/`networkidle` timeouts → LOAD: the same specs alone pass 14/14; (B) super-admin finance specs `PUT /cheques/{id}/clear` 500 — the super-admin project's API calls carried no organisation → fixed by c8dd4fc2 (auth-setup selects the test tenant via `active_tenant_id`, as the org switcher does); (C) `login.spec.ts:33` RENTER login redirect fails alone too → reading it. Dev suite #4 after the backend suite ends.
- (C) login.spec.ts:33 renter login redirect fails alone (form re-rendered empty after Sign In, no redirect in 15 s) → sonnet diagnosis with a trace. Backend suite #2 still running.
- Full backend suite #2 at 81539e9f (controller change 3a431bc9): BUILD SUCCESSFUL — 226 classes, **2287 / 0 / 0 / 0** (wall time inflated by the miner). Diagnosis agent for login:33 stalled on the watchdog mid-trace → resumed with short-call instructions. Dev suite #4 started (/tmp/p5-dev-e2e-4.log) with c8dd4fc2's super-admin tenant selection.
- Dev suite #4: **143 passed / 8 failed**; six were load timeouts (the same specs alone: 44 passed), tenants.spec:21 was drift (fixed 66eec413: named input + search-based assertion, 8/8 alone), login:33/:43 under diagnosis.
- login:33/:43 diagnosis (sonnet, traced): ENVIRONMENT — NextAuth authorize()'s fetch to the backend occasionally never resolves because the `next dev` Node process itself stalls 75 s+ under the miner's load (trace: callback/credentials status −1; backend curl 81–110 ms ×8; 3/3 green alone). No code change. Dev suite #5 (final) started at 66eec413.
- **Dev suite #5 (final, at 66eec413): 149 passed / 2 failed / 249 skipped (4.0 m)** — from 73 passed / 76 failed at the start of the wrap-up. The two: `login.spec.ts:43` (environment — dev-server stall under the miner, 3/3 alone) and `accounting-v2.spec.ts:373` (shared-tenant pagination race on the journals list, #321 class; 13/13 alone). Root causes fixed on the way: tenant-less super-admin 500 + undrained error bodies (3a431bc9, 81539e9f), super-admin auth state selects the test org (c8dd4fc2), 26 stale specs realigned (48f9e738, 66eec413, 923d5f03), profile-menu testids (7043a6f5). Backend 2287/0. **Plan 5 Task 17: complete. Plan 5: complete** — PR #331. Open for Kunal: the miner; prod re-seed decision (P5-R15); prod Playwright run (P5-R16); #326 before the first prod-suite run.
- 22:11 On Kunal's instruction the miner was REMOVED: LaunchAgent com.xrtxmxnpizcvdkuq (created 2026-07-31, blockchain-resolved C2 via Polygon contract 0xA3a6…C2A0) booted out, xmrig killed, artefacts quarantined in ~/quarantine-2026-09-22 (README with IOCs), dead 'Electron' login item (→ ~/.Trash/stealth-electron-app) deleted; verified no process/label/connection remains. Credential rotation recommended.
- 2026-09-22 23:1x Kunal: 'continue all the test; work on the 5 open PRs'. Facts: deploy.yml deploys EVERY push to main (no gate); verify.yml is manual-only; main has 5 CI-only commits, merges clean. Ruling: one release PR (release/accounting-v2 = #331 tip + main) → main, so production gets one deploy; the five stacked PRs stay as reviewed history until Kunal decides; fix-before-prod #286/#297/#300/#326 are dispatched as a release fix wave on the release branch; the merge itself waits for Kunal's explicit go (production deploy + v1 finance tables dropped).
- Dev suite (idle machine, both servers restarted): **151 passed / 0 failed** (1.6 m). Prod suite local run #2 started. Release PR #334 opened (release/accounting-v2 → main); fix-before-prod wave (#286 #297 #300 #326) dispatched on it.
- CI verify on release/accounting-v2 could not start: GitHub annotation 'job was not started because recent account payments have failed or your spending limit needs to be increased' — Actions billing on the account (last successful CI run 2026-09-11). Kunal's side.
- Prod suite local #2 (release branch, pre-wave jar): 30 passed / 4 failed — 10a #328 (local), 99 #326 (in the wave), 01a+01b = feature inventory missing MOBILE_FINANCE → fixed 5a8ae12f. Re-run after the wave + jar rebuild.
- Walkthrough regression on the idle machine (release branch): plan 1 **15 passed** (after realigning two stale nav assertions — Payments→Cheque Register; PMs now legitimately see the operational finance screens, ledger pages still hidden), plan 2 **17 passed**, plan 3 **12 passed + 1 skipped**, plan 4 **13 passed**.
- Release fix wave returned: f18b1e80 #326 (NULL inbound journal pointers + delete ledger tables with user triggers disabled — needs table ownership; ACCESS EXCLUSIVE lock), 0fdf3f27 #286 (conditional write, no cheque lock), 0fca6a70 #297 (per-cheque settlementAbsorbed), ee52d215 #300 (only /serve/assets/** public; web fetches private attachments via proxy; authn not authz — remainder stays in #300). 174/0 across the touched classes; golden 16/0. Review (fable, no Gradle) + full backend suite (controller, background) dispatched in parallel.
- Release branch full backend suite (with the wave): BUILD SUCCESSFUL 19m 34s — 228 classes, **2295 / 0 / 0 / 0**. Jar rebuilt for the prod-suite re-run.
- Fix-wave review (fable): #286, #297 RESOLVED; #326 NEEDS CHANGE (ALTER TABLE DISABLE TRIGGER holds ACCESS EXCLUSIVE until commit → ruled: changeset 89 session-variable trigger exemption, SET LOCAL, no lock/ownership); #300 NEEDS CHANGE (proxy X-Frame-Options DENY breaks the settlement PDF iframe → SAMEORIGIN for the asset path). Fix round 1 → same implementer. Prod suite re-run in progress on the pre-round jar.
- Prod suite on the ee52d215 jar: 30 passed / 4 failed — 01a/01b: second MOBILE_FINANCE drift (all-enabled assertion) fixed 052e2017; 10a #328; **99-cleanup still 500: 'deleteTenant stalled after pass 4 with tables [users, properties, units, renters, accounts, leases, cheques, penalty_assessments]' — the #326 fix only covered a posted lease; the real disposable tenant does not converge → added to the fix round with a fuller IT.**
- Fix round 1 returned: c3b40808 (#326: changeset 89 session-variable exemption in the three append-only trigger functions + set_config in purgeLedger; cycle-breaker for cheques↔penalty_assessments; FK discovery from pg_catalog; IT with the production-shaped tenant + concurrent-post-not-blocked test; next changeset 90), 4a8eadf2 (#300: SAMEORIGIN only on /api/proxy/v1/assets/serve/; iframe verified rendering, control frame refused; note: next dev does not apply middleware headers to rewritten 200s). Re-review (fable) + full suite (background) dispatched.
- Fix round re-review (fable): **accepted — wave ready for release**; doc corrections applied (PR bodies: changeset 89 added / next 90; the 'next dev skips middleware headers' claim is NOT stated in the PR); low follow-up → issue. Waiting on the full suite, then jar rebuild + prod suite.
- Full backend suite with changeset 89 (release branch): BUILD SUCCESSFUL 20m 9s — 228 classes, **2297 / 0 / 0 / 0**. Jar rebuilt 00:38 (contains 89); backend cycled so Liquibase applies 89 to rentaxis_v2; prod suite re-run next.
- **Prod suite on the changeset-89 stack: 33 passed / 1 failed (10a, #328 local-only); `99-cleanup` 204 in 246 ms — #326 proven end to end.** Release PR #334 body final: backend 2297/0, golden 16/0, web 961/961, dev e2e 151/0, prod e2e 33/1 (local-only), walkthroughs 15/17/12+1/13, fix-before-prod wave accepted. Remaining for Kunal: merge (= prod deploy, DB backup first), Actions billing, close-or-keep the five stacked PRs, prod demo re-seed, prod Playwright run, prod migration role (changeset 89 needs no ownership).
