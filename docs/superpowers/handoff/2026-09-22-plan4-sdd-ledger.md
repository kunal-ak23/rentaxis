# SDD ledger — plan: docs/superpowers/plans/2026-09-17-accounting-v2-plan4-vouchers-cutover.md
Spec: docs/superpowers/specs/2026-09-17-accounting-v2-design.md (§10–§13). Stacked on plan 3's branch (Kunal's "stack them" directive); branch feat/accounting-v2-vouchers-cutover cut from the plan 3 branch head; PR base = plan 3 branch.
## Pre-flight scan (2026-09-21) — table in preflight.md beside this file (opus scanner, against real plan 1+2 code at 7f1ba452)
Headline findings needing rulings BEFORE the owning task is dispatched (rulings to be written here when plan 4 starts, after plan 3 lands, because several depend on plan 3's real signatures):
1. LeaseChequeRegistrar hard-codes importBatchId=null on PDRs → T11 must thread the batch id through the registrar (else imported PDRs escape batch reverse and hit the period lock).
2. T10 validator compares Σ cheques to NET; LeasePostingService demands GROSS (LeaseVat) → validator uses LeaseVat.
3. Lease has no property/contractValue; contractNumber is Long → PACT contract string needs a home (new nullable column external_contract_ref in changeset 88?).
4. LeaseService.syncDerivedTotals(Lease) not (UUID). 5. Equity root is F, "Opening Balance Difference" already exists as F-02 → T8/T9 resolve by role/existing leaf, never by code "E".
6. ChargeTypeRepository.findByCode only. 7. Cheque has @ManyToOne property/unit/renter; T10 must go through ChequeGenerationService.saveRowsForSystemImport + ChequeRowRules, not raw Cheque writes.
8. BPV with VAT lines silently loses the VAT (plan defect) → rule against spec §10 when T4 starts.
9. ContractImportValidator non-transactional repository reads (tenant filter off) + dropped validateDbConflicts (duplicate properties/renters on re-import).
10. PortfolioImportService cell helpers are instance methods; Cheque.importedStatus declared in T11 but used in T10 → declare in T10.
Deferred-in items unhandled by the plan text: batch reverse skipping reversalOfId!=null; saveTemplate upsert; duplicate property names vs generateMissing; sticky partial template seed.

## Started 2026-09-21 (Mac) — branch feat/accounting-v2-vouchers-cutover cut from feat/accounting-v2-recognition-settlement @ 722fdc93 (PR #298 open)
Note: the pre-flight was run against plan 1+2 code; plan 3 is now BUILT (its real signatures: task reports under .superpowers/sdd/2026-09-17-accounting-v2-plan3-…/ and the code) — Tasks 10–11 implementers verify against code, not against either plan text.
Rulings on the pre-flight (spec §10 binding; "the plan's code is a sketch where it disagrees with built code — the built code wins"):
Ruling R1 (1.3): TrialBalanceRowDTO / AccountLedgerDTO live in api/dto/ledger — import from there.
Ruling R2 (7b T8/T9): Equity root is `F`; "Opening Balance Difference" already exists as leaf F-02 and as role OPENING_BALANCE_DIFFERENCE — T8/T9 resolve it BY ROLE through AccountResolver, never by code; fixtures use the seeded chart.
Ruling R3 (1/2→4): a BPV (bank/cash payment voucher) carries NO VAT — VAT belongs to the purchase invoice (PISR) that the payment settles; VoucherService refuses vat_rate > 0 on a BPV line with 400 and the web hides the columns (plan already does) — spec §10.1 describes BPV as Dr payable-or-expense / Cr bank — cost if wrong: a direct-expense BPV with VAT needs the rule relaxed to Dr INPUT_VAT. T4 adds the refusal IT.
Ruling R4 (2.21): the import batch id is threaded through EVERY journal the bulk post writes — LeasePostingService.post AND LeaseChequeRegistrar (PDR) AND the ChequeService transitions T11 replays (deposit/clear/bounce) AND recognition catch-up CILs — via an explicit parameter/overload on the internal paths (never a thread-local); T11 owns it; its IT asserts EVERY journal of an imported lease carries the batch id and that batch reverse leaves the lease-dimension ledger at zero.
Ruling R5 (4.3): the contract-import validator compares Σ cheques with GROSS (Σ lines + VAT via the shared LeaseVat helper) exactly as LeasePostingService does — one definition; fixture gains a VAT-applicable contract.
Ruling R6 (4.4 booking deposit): a booking-deposit row is an ordinary cheque row of the contract (it is money against the contract value); Σ rule unchanged; if PACT's sheet carries it outside the contract value the row is imported as a DEPOSIT-behaviour LINE + its cheque — T10 documents which the client's sheet does (sample files in ~/Downloads) — cost if wrong: re-map one sheet column.
Ruling R7 (2.2–2.5, 2.7): Lease has no property / contractValue / lines collection; property via unit.getProperty(); totals via LeaseService.syncDerivedTotals(Lease); Cheque uses @ManyToOne property/unit/renter. PACT's alphanumeric contract number ("SAMPLE-25/001") gets a new nullable column leases.external_contract_ref (varchar 64, indexed per tenant) in changeset 88; Lease.contractNumber (Long) stays the system's own sequence.
Ruling R8 (2.6): add ChargeTypeRepository.findByCodeIgnoreCase (tenant-scoped) rather than upper-casing at call sites.
Ruling R9 (4.x): T10 writes lease lines and cheque rows THROUGH the existing services (LeaseService.applyLines / ChequeGenerationService.saveRowsForSystemImport + ChequeRowRules) so a duplicate cheque number becomes an ImportErrorDTO, not a rolled-back workbook; Cheque.importedStatus is DECLARED in T10 (changeset 88 column) since T10 sets it.
Ruling R10 (5c, 4.5): the contract import keeps validateDbConflicts — duplicate property/renter names are an import ERROR, not a silent merge; ContractImportValidator is a @Service with @Transactional(readOnly = true) validate() (TenantAspect only filters inside a transaction — an unscoped read is a P0).
Ruling R11 (5b, 5d): saveTemplate upsert semantics and the sticky partial template seed are OUT of plan 4's scope → issue; T8 must not depend on the template being complete (resolve roles; report unmapped roles as reconciliation errors).
Ruling R12 (7a 7→11): batch reverse must restore every cheque of the batch's leases to a clean DRAFT (status, all journal ids, cleared/bounced/deposited dates, replacedBy) — after plan 3 also: recognition segments/entries cancelled+CILs reversed, settlement absent, lease back to DRAFT, unit released; T11's IT asserts a reversed batch can be re-posted and yields identical balances.
Ruling R13 (plan 3 seam): bulk post of a contract that started before booksStart runs recognition catch-up through booksStart−1 via RecognitionService.runTo semantics for THAT lease only, journals carrying the batch id (exempt from the period lock like the rest of the batch).
Ruling R14 (§8): stacked branch; PR base = plan 3 branch; implementers use their own Co-Authored-By trailer; T17 = full verification + recorded walkthrough (web/walkthrough/accounting-v2-plan4.{config,spec}.ts) + final review + PR by the controller.
Ruling R15 (UI discipline from plans 2–3): every web gate mirrors its Java rule with a comment naming the class; en+ar parity; RTL-safe classes; table-first + pagination + createdAt asc.
Tasks 1+2: BASE 722fdc93 — dispatched as ONE sonnet implementer (changeset 87 + entities/VoucherMath/draft lifecycle; plan text carries complete code), two commits, reviewed as one unit.
Tasks 1+2: implemented (sonnet) commits 06ed8362 (changeset 87 + VoucherSchemaIT), 1794eb10 (entities, VoucherMath, draft lifecycle, BPV-no-VAT refusal, tenant-isolation IT); backend 1832/0 (+20). Concerns: updateDraft needs a saveAndFlush before rebuilding lines (Hibernate inserts before orphan deletes vs the new composite unique index); the "15.03" test's fixtures bypass VoucherMath.vat() so the rounding mutation is caught by vatRoundsHalfUpToTwoDecimals instead → reviewer to judge. Review dispatched (sonnet) 722fdc93..1794eb10.
Tasks 3+4: BASE 1794eb10 — dispatched as ONE opus implementer (PISR post, BPV post, amend — all in VoucherService; two commits), parallel with the T1+2 review.
Tasks 1+2: review (sonnet) spec ✅ / quality Approved — 0 Critical, 1 Important (no test pins per-line-VAT-then-sum through the real code: the 15.03 test uses pre-rounded fixtures), 3 minors in task-1-2-review.md. ⚠️ resolved: counts = implementer's XML-read 1832/0; the saveAndFlush mutation was asserted not run; seed account codes A-02-02 / D-01 exist in the seeded chart (AccountService seed) — accepted.
Ruling: the Important is a test gap in the file the T3+4 implementer has open — handed over as a rider (3 × 100.10 @ 5% → 15.03 / 315.33 at draft AND in the posted PISR; run the saveAndFlush mutation) instead of resuming the sonnet implementer into a busy tree — cost if wrong: none; T3+4's review verifies the rider.
Tasks 1+2: complete (commits 06ed8362, 1794eb10; review approved; the one Important rides with T3+4).
Tasks 3+4: implemented (opus) commits b8af18cc (PISR post + row lock + the T1–2 rider: 15.03/315.33 pinned at draft and in the posted journal; saveAndFlush mutation really run), 50528ff7 (BPV post + amend in one transaction); backend 1857/0 (+25); 8 mutations applied, all detected. The brief's 5,100 was right for ITS mixed-rate fixture (2,000 @5% + 3,000 @0%). ChequeService.isSettlementAccount extracted so vouchers reuse ONE bank/cash definition. amend's early assertOpen is defence-in-depth (not mutation-detectable; declared).
Ruling: the two post-time account rules (BPV payment account = bank/cash leaf; PISR line = EXPENSE/ASSET leaf) MOVE into draft validate() in Task 5 — a clerk must not be able to save a draft the server will always refuse at post (the plans 2–3 "UI offers what the server refuses" lesson, applied server-side); post re-asserts them — cost if wrong: a fixture in a later brief needs a valid account.
Tasks 3+4: review dispatched (opus) 1794eb10..50528ff7. Task 5: BASE 50528ff7 — dispatching sonnet implementer (parallel with the review).
Tasks 3+4: review (opus) spec ✅ / quality Approved with 3 Important — journals hand-verified for all four fixtures (5,100.00; 15.03/315.33; BPV → vendor ledger 0.00; amend → −3,780.00, INPUT_VAT 180.00 net); the BLOCKING post lock judged safe (business row → journal row → sequence row everywhere; no #290 cycle). I-1 a BPV can name vendor A and debit vendor B's payable (vendorLedger selects by payable account; the list filters by vendor_id); I-2 lockForWrite's javadoc credits the tenant filter but entityManager.find bypasses TenantAspect — the explicit tenant comparison is the ONLY guard there; I-3 POST /journals/{id}/reverse reverses a voucher's journal behind its back (voucher stays POSTED, amend dead-ends).
Ruling (I-3, WIDER than vouchers): the generic journal-reverse endpoint (JournalService.reverse over HTTP) may reverse ONLY journals whose sourceType is MANUAL; any document-sourced journal (LEASE, CHEQUE, RECOGNITION, PENALTY, SETTLEMENT, VOUCHER, OPENING_BALANCE, IMPORT) is refused with 400 "This journal belongs to a <document>; reverse it from there" — the same back-door exists for TCO/PDR/CIL/STL since plans 1–3; PostingService.reverse (internal) is untouched — cost if wrong: an accountant must use the document's own reversal path, which is the point.
Ruling (I-1): a BPV line whose account is ANY vendor's payable account must be the voucher's own vendor's payable account → 400 otherwise (draft validate + post); a BPV with payable lines requires a vendor.
Ruling (I-2): keep the explicit tenant comparison as the documented guard (javadoc corrected), and add an IT that runs post/amend for tenant B against tenant A's id and gets 404 — asserting no lock was taken and nothing changed.
Tasks 3+4: fix round 1 QUEUED behind Task 5's implementer (same files; sequential implementers, one Gradle).
Task 5: implementer (sonnet) landed d10f0c65 (draft-time account rules) then went IDLE at 17:55 "waiting for a notification" from a background run that had already ended — two hours lost. Resumed at 19:59 with explicit instructions (foreground full run or poll the log; no notification waits). LESSON for every later dispatch: tell implementers to run the full suite in the FOREGROUND with a long timeout.
Ruling: the voucher half of the web work (Task 12 voucher parts + shared plumbing, Tasks 13–14) runs IN PARALLEL with the backend (web/ only, typed from the working-tree Java, re-reconciled before its final commit); the cut-over half of Task 12 and Tasks 15–16 wait for Tasks 6–9's real DTOs — cost if wrong: a field rename in one client file. First web dispatch stalled on the stream watchdog before doing anything; resumed at 19:59.
Prepared while waiting: task-3-4-fix1-message.md (3 Important + an entityManager.find tenant-guard audit across plans 3–4), task-10-11-context.md (rulings R4–R13 for the contract import and bulk post), plan 5 rulings P5-R1…R10.
Task 5: implemented (sonnet) commits d10f0c65 (draft-time account rules), fd132279 (VoucherController, DTOs, attachments, VoucherControllerIT 18); backend 1879/0 (+22); 4 mutation checks (cross-tenant attachment refusal not independently detectable — the tenant filter already blocks it; declared). Found+fixed two Task 1–2 bugs the controller IT exposed: VoucherService.list() LazyInitializationException; VoucherRepository.search() null-check idiom rejected by Postgres for non-null date ranges. Flagged (pre-existing): DeductionAttachmentService.downloadAttachmentStream has no @Transactional → tenant filter OFF on that read → joins the T3+4 fix round's tenant-guard audit (P0 class).
Task 5: review dispatched (sonnet) backend 50528ff7..fd132279. Tasks 3+4: fix round 1 dispatched (resume opus) FIX_BASE = HEAD.
Tasks 12(voucher half)+13+14: implemented (opus, web only) commits 137b11cd, 9340b19b, 093ee492, a5c52556; tsc clean; vitest 715/715 in 102 files (+83); lint 206; 12 mutations. Server gaps it reported: journal-reverse refusal and the BPV vendor-payable rule not yet in Java (both are in the T3+4 fix round — the web mirrors run AHEAD of the server by design); no vendor DTO (entity returned — pre-existing); no voucher-by-journal lookup (link derived from sourceId + docType). Nothing ran in a browser → the T17 walkthrough is the proof; Arabic strings want a native read (PR note). Review dispatched (sonnet) web 50528ff7..a5c52556.
Task 5: review (sonnet) spec ✅ / quality Needs fixes — CRITICAL (pre-existing platform hole this task routes a new feature through): VoucherAttachmentDTO.fileUrl points at /api/v1/assets/serve/**, which SecurityConfig permitAll()s with no tenant/role check → a locally stored voucher attachment is fetchable unauthenticated (Azure mode unaffected). Important: content type trusted from the client header (no signature sniffing); declared 25 MB limit is dead (global multipart 10 MB wins and surfaces as a raw 500); role-matrix/tenant-guard ITs skip POST /{id}/amend and the upload endpoint.
Ruling (Critical): voucher attachments NEVER use the public asset route — fileUrl is removed from VoucherAttachmentDTO (download only through the authenticated, tenant-checked GET …/attachments/{aid} stream; the web already has that endpoint); local-disk keys for voucher attachments live under a "private/" prefix that AssetController.serveAsset refuses with 404 (one guard + IT: an unauthenticated GET of a voucher attachment's key → 404); the platform-wide hole (deduction attachments, lease documents, cheque images) is issue https://github.com/kunal-ak23/rentaxis/issues/300 — cost if wrong: none; a private document must not have a public URL.
Ruling (Important): accept only PDF / PNG / JPEG by FILE SIGNATURE (magic bytes; no new dependency), in addition to the declared type; limit aligned to the global 10 MB and MaxUploadSizeExceededException mapped to a clean 400 in GlobalExceptionHandler (helps every upload endpoint); role matrix + tenant guard extended to amend and upload.
Task 5: fix round 1 QUEUED behind the Tasks 3+4 fix round (same tree, one Gradle). NOTE for the web re-check: VoucherAttachment type loses fileUrl.
Tasks 12–14 (web): review (sonnet) spec ✅ / quality Needs fixes — VAT preview fuzzed against BigDecimal HALF_UP over 250k+ cases: no divergence; i18n parity 71/71. CRITICAL: Amend cannot amend — the form stays DRAFT-only-editable on a POSTED voucher, so amend() always resubmits the original figures. Important: isLineAccountAllowed / isPaymentAccountAllowed never called in production (picker filters are the only layer); the own-vendor-payable check runs for every voucher type instead of BPV only.
Ruling: Amend becomes an explicit AMEND MODE on a POSTED voucher (editable form + banner; "Post amendment" disabled until something changed → reversal date/reason dialog; "Cancel amendment" restores) — cost if wrong: none; without it the feature does nothing.
Tasks 12–14: fix round 1 dispatched (resume opus web implementer; web tree is free) — also mirrors the Task 5 security ruling (no fileUrl; authenticated download only; PDF/PNG/JPEG ≤ 10 MB).
Tasks 12–14: fix round 1 done (opus, web) commit 0847859a; tsc clean; vitest 732/732 in 102 files (+17); lint 206; 10 gates mutation-checked. Real amend mode (the original bug `editable = canEditVoucher(status)` fails the new API-level test). Notes: the download path is GET …/attachments/{attachmentId}/download (the controller's fix message had it wrong — the agent coded against the Java); UI attachment limits are already the RULED ones (PDF/PNG/JPEG ≤ 10 MB) ahead of the server — Task 5's backend fix round must land them as ruled; draftRefusal now returns { key, line? }. Re-review dispatched (haiku→sonnet: amend mode is stateful UI, use sonnet) web a5c52556..0847859a.
Tasks 12–14: fix round 1/5 (3 addressed + contract mirror, 0 open; commit 0847859a) — re-review (sonnet) "Ship": tsc clean, vitest 732/732 self-run, en/ar parity 80/80; BPV-only scoping matches VoucherService.java:379,468 exactly.
Tasks 12(voucher half)+13+14: complete (commits 137b11cd, 9340b19b, 093ee492, a5c52556, 0847859a; review clean after 1 fix round).
Tasks 12–14: minor (deferred → FINAL-REVIEW TRIAGE, likely must-fix): the voucher form drops per-line propertyId/unitId on any save (amend included) — line dimensions are what put an expense on a property's ledger. Tasks 12–14: minor (deferred): attachments uploaded in amend mode attach to the original (soon REVERSED) voucher with no on-screen hint; "failed amend keeps edits" correct by inspection but untested.
Tasks 3+4: fix round 1 done (opus) commits 0475732a (journal reverse over HTTP = MANUAL only; null sourceType refused), 60054245 (BPV own-vendor payable rule at draft/update/post; javadoc; minors M-1/2/3/5), 8aa6e21d (tenant-guard audit fixes); backend 1902/0 (199 classes); red phase 16 failures; 9 mutations killed.
AUDIT FOUND A LIVE P0 ON MAIN: MaintenanceTicketService.getTickets(userId, role, unitId) had no @Transactional → TENANT_ADMIN's GET /maintenance-tickets returned EVERY landlord's tickets. Controller verified it on origin/main, cherry-picked 8aa6e21d onto a hotfix branch off main, ran CrossTenantReadGuardIT (2/2) and mutation-proved it on main's code (fix removed → the other tenant's ticket id appears), opened PR #301 → main. NOT merged or deployed (Kunal's call). Memory saved.
Unexplained: twoSimultaneousPostsWriteOneJournal produced two winners ONCE (under an unrelated mutation), never reproduced in 11 re-runs.
Ruling: close it structurally — a partial unique index on journal_entries(source_id) WHERE source_type = 'VOUCHER' AND <not a reversal, same predicate shape as the RECOGNITION index in 86> goes into changeset 88 (Task 6, plan 4's cut-over changeset) — BUT an amended voucher's replacement is a NEW voucher row (new source_id), and the original's journal is REVERSED not deleted, so one live journal per voucher id holds; T6 verifies that before adding it — cost if wrong: an index to drop.
Ruling: the BPV payable rule stays ONE-WAY (an advance to a vendor booked to a prepayment leaf is legitimate) — accepted as implemented.
Tasks 3+4: re-review dispatched (sonnet) backend fd132279..8aa6e21d. Task 5: fix round 1 dispatched (resume sonnet) FIX_BASE 8aa6e21d.
Tasks 3+4: fix round 1 re-review (sonnet) — I-1, I-2, I-3 ADDRESSED; NOT approved as-is: NEW Important — JournalService.reverse's new entries.findById has no explicit tenant check (cross-tenant existence/document-category leak via the 400 text; mutation still blocked by PostingService's filtered lockById); Minor — requirePayableLinesMatchTheVendor refuses a legitimate BPV if two vendors share one payable account (no uniqueness on vendors.payable_account_id). AUDIT-SWEEP: the reviewer drove the project's spring-aop pointcut against a repository-shaped interface and found TenantAspect's `execution(* …domain.repository..*(..))` does NOT match inherited CrudRepository methods (findAll/findById…), only methods declared in our interfaces. Controller's reading: that is consistent with BOTH observed facts on main (IT passes with @Transactional, leaks without) — inside one transaction the shared session keeps the filter once ANY custom finder has enabled it; without a transaction each call gets its own session. So PR #301's fix is real for that path but by an incidental mechanism, and a transaction whose FIRST repository calls are inherited methods would run unfiltered. Also RecognitionPoster.lock / RecognitionService lock use bare findById first in their transaction.
Ruling: settle it by TEST, then fix the mechanism — fix round 2 (resume the opus T3+4 implementer after Task 5 frees Gradle): (1) a definitive TenantAspectIT — fresh transaction, tenant context A, call ONLY an inherited method (findAll, findById, count, existsById) on two repositories → must see only A; (2) if it fails, widen the pointcut to advise every Spring Data method on our repository types (e.g. `execution(* org.springframework.data.repository.Repository+.*(..)) && target(com.datagami.rentaxis.domain.repository..*)` — verify with the IT, incl. applyToLoadByKey for findById) — this is a platform P0-class fix and is ALSO cherry-picked onto the hotfix branch behind PR #301; (3) explicit tenant check on JournalService.reverse's load and the two recognition lock helpers; (4) the shared-payable edge: compare the line's account to the voucher's OWN vendor's payable account id (equal → fine, regardless of who else shares it). Caveat posted on PR #301.
Task 5: fix round 1 done (sonnet) commit a98d0398; backend 1908/0 (+6); VoucherControllerIT 24/24; 3 mutations killed. Also fixed: Content-Disposition silently dropped for non-ASCII filenames; oversize-upload IT raced Tomcat's max-swallow-size. DeductionAttachmentService has the same content-type + transaction gaps → #300 (its @Transactional was already added in 8aa6e21d). Re-review dispatched (sonnet) 8aa6e21d..a98d0398. Tasks 3+4: fix round 2 dispatched (resume opus) FIX_BASE a98d0398 — includes the TenantAspect mechanism test/fix.
Task 5: fix round 1/5 (4 addressed, 0 open; commit a98d0398) — adversarial re-review (sonnet) "Ready to sign off": prefix guard runs on the normalised Path that is actually opened (no TOCTOU), getRequestURI is undecoded so %2e%2e/%2f never become separators, case-insensitive, Azure mode not proxied by serveAsset, signature sniff and store share one byte[], detected type drives the download, attachment disposition with RFC 5987 names, max-swallow-size is test-only.
Task 5: minor (deferred → final-review triage): the new global IOException handler is broader than needed (could turn an unrelated server-side I/O fault into a 400 elsewhere); no X-Content-Type-Options: nosniff convention exists in the codebase (→ #300).
Task 5: complete (commits d10f0c65, fd132279, a98d0398; review clean after 1 fix round).
Tasks 3+4: fix round 2 done (opus) commits 993afc93 (TenantAspectIT — 9 tests GREEN on unmodified code: the pointcut DOES advise inherited Spring Data methods, because Spring re-resolves a proxied bean's method against the composite of its interfaces; the re-reviewer's out-of-container harness skipped that branch → the mechanism doubt is REFUTED; mutation H kills 7/9), 6d4852b4 (explicit tenant comparison in JournalService.reverse + both recognition lock helpers, failing closed on a null context), cfd889da (shared-payable edge — a genuine defect, red line recorded); backend 1921/0 (200 classes).
Controller: cherry-picked 993afc93 onto the hotfix branch, ran TenantAspectIT 9/9 + CrossTenantReadGuardIT 2/2 on main's code, pushed (PR #301 now b8dd4b30) and corrected the PR caveat. Memory corrected. The P0's cause stands: the missing @Transactional was the entire defect.
Ruling: the remaining class ("public service method, no transaction, tenant-scoped read") gets a mechanical READ-ONLY sweep of all core/service files now (parallel, sonnet); confirmed findings are fixed in one batch on this stack and, where they exist on main, cherry-picked to a second hotfix PR.
Tasks 3+4: re-review of round 2 dispatched (sonnet) a98d0398..cfd889da. Tasks 6+7: BASE cfd889da — dispatched as ONE opus implementer (changeset 88 incl. the ruled extras + import batches).
TENANT-READ SWEEP (sonnet, read-only; report tenant-read-sweep.md): 392 methods → 51 without a transaction → 2 LEAK, 13 EXPOSED-BUT-GUARDED (explicit predicate/comparison present), 36 SAFE; all 24 native-SQL / em.find sites carry explicit tenant checks. LEAKS — both verified by the controller on origin/main source: (1) UserService.getAllUsers() = userRepository.findAll(), no @Transactional, behind GET /api/admin/users which admits TENANT_ADMIN → every landlord's users (PII); (2) OrgSettingsController GET (isAuthenticated) / PUT (SA, TA) use repo.findAll() + rows.get(0), no transaction in the class → read, and OVERWRITE, an arbitrary tenant's penaltyPaymentInstructions (the renter portal's "How to pay" text → fraud-relevant).
Ruling: both go onto the hotfix branch behind PR #301 (each with an IT that is red before the fix; explicit tenant resolution for org settings; @Transactional + explicit tenant scope for non-SUPER_ADMIN on the user list) the MOMENT the single checkout is free (after the Tasks 6+7 implementer returns, before any other backend dispatch), then onto this stack; structural guard (fail-closed TenantAspect check when a tenant context is set and no transaction is active; warn-only first) proposed in the PR. Kunal informed; nothing merged or deployed.
Tasks 3+4: fix round 2/5 — re-review (sonnet) APPROVED: A the "pointcut misses inherited methods" claim REFUTED with HIGH confidence (reproduced both results: matches(findById, realProxyClass) = true, matches(findById, bareInterface) = false — which is exactly why the first reviewer's harness disagreed); B, C, D ADDRESSED; no new breakage.
Tasks 3+4: minor (deferred → rides with the Tasks 15–16 web dispatch): VoucherForm.tsx:263-267 builds payableOwners as a last-write-wins accountId→vendorId map, so when two vendors share a payable account the client can wrongly refuse what the server (after D) accepts — fails safe (over-refusal).
Tasks 3+4: complete (commits b8af18cc, 50528ff7, 0475732a, 60054245, 8aa6e21d, 993afc93, 6d4852b4, cfd889da; review clean after 2 fix rounds).
Tasks 6+7: implemented (opus) commits 649024a2 (changeset 88 incl. leases.external_contract_ref, cheques.imported_status, one-live-journal-per-voucher partial unique index — judged SAFE and shipped, with a forced second post refused by the database), 2bd3155a (import batches, whole-batch reverse via PostingService.reverse, LeaseReverter contract); backend 1964/0 (204 classes, +43); 19 guards mutation-checked. CARRY to T10/T11: implement LeaseReverter to its javadoc contract (cheque journal ids cleared, recognition cancelled); markPosted refuses REVERSED→POSTED (a re-import is a NEW batch — R12's round trip uses a fresh batch); keep ImportBatchServiceNoLeaseModuleTest hand-wired; import_jobs.import_batch_id has no entity field yet (T10 owns ImportJob); journalsPosted is caller-supplied.
Ruling: the checkout is free → the two confirmed production leaks (user list, org settings) are fixed on the hotfix branch behind PR #301 FIRST (one opus implementer, switches the checkout to the hotfix branch and back); the T6+7 review and Tasks 8–9 are dispatched when the checkout is back on the plan 4 branch — cost if wrong: ~40 minutes of plan 4 idle time against two live PII/fraud-relevant leaks.
HOTFIX 2 done (opus, foreground): hotfix branch 15ef2caa (user list scoped + transactional; OrgSettings tenant-explicit, no-context GET empty / PUT 400, PUT can now create — it previously 409'd because landlord_org_id was read-only-mapped); cherry-picked here as e73aaaf4; hotfix-branch full suite 1269/0; all fixes red-first + mutation-checked. Pushed; PR #301 title/body REWRITTEN.
CORRECTION OF THE CONTROLLER'S EARLIER CLAIM: spring.jpa.open-in-view is unset → ON → over HTTP the request-bound EntityManager makes the tenant filter apply even without @Transactional. The user-list HTTP test PASSED before the fix while the service test failed. So the ticket and user-list defects are service-layer (non-HTTP callers), NOT demonstrated live HTTP leaks — my "live in production" statements for those two were wrong and are corrected on the PR, in memory and to Kunal. What was live over HTTP: org settings with no tenant context (read + overwrite of an arbitrary tenant's row). LESSON: prove HTTP exposure through MockMvc before calling it a production leak.
Open follow-ups from the hotfix: OSIV is load-bearing for tenant isolation (decide: pin true with a comment, or turn off + fail-closed aspect); GET /properties/{id}/managers never checks the property's tenant; org_settings has no unique(tenant_id).
Checkout back on feat/accounting-v2-vouchers-cutover (clean). Dispatching the T6+7 review and Tasks 8+9.
Tasks 6+7: review (sonnet) spec ✅ / quality Approved — 0 Critical, 0 Important; the voucher-index safety analysis independently re-derived and holds; append-only respected. Tasks 6+7: minor (deferred): markPosted takes no row lock (safe today via Postgres row locking; fragile if a second write is added); a few non-posting ITs do not end on the trial-balance assertion; changeset comment overstates the quoting need for a block-style key.
Tasks 6+7: complete (commits 649024a2, 2bd3155a; review clean, no fix round).
Task 16 + the batches half of Task 12 (web): dispatched now (resume the opus web implementer; web tree free; typed from Task 7's real Java), carrying two deferred web items: the per-line propertyId/unitId the voucher form drops on save (must-fix: dimensions put an expense on a property's ledger) and the stale payableOwners mirror. The opening-balance/reconciliation pages (Task 15) wait for Tasks 8–9's DTOs.
Task 16 + batches client + voucher-form fixes: implemented (opus, web) commits 1024aeb3 (per-line propertyId/unitId round-trip; shared-payable mirror), e63b0f3c (Import Batches page, cutoverApi.batches, nav entry); tsc clean; vitest 767/767 in 104 files; lint 206; 11 gates mutation-checked (two no-op mutations led to stronger tests). The agent REFUSED to wire buttons to routes the Java does not have (no bulk-post route; no ?cutOver template param) — correct. Its "attachment limits not landed" note is STALE: controller verified a98d0398 is at HEAD (no fileUrl; 10 MB; PDF/PNG/JPEG by signature).
Ruling: the missing HTTP surface (cut-over template with ACCOUNTANT admitted, bulk-post route with a per-lease result, journal drill-through by importBatchId) is owned by Tasks 10–11 — appended to task-10-11-context.md; the web's DRAFT-batch actions are added in the T15 web dispatch once those routes exist.
Review dispatched (sonnet) web 0847859a..e63b0f3c.
Task 16 + batches client + voucher-form fixes: review (sonnet) spec ✅ / quality Approved — 0 Critical, 0 Important; every client gate confirmed line-by-line against the Java; tsc clean; 97/97 in the five relevant vitest files. Task 16: minor (deferred → rides with the Task 15 web dispatch): duplicated todayIso/pad helper (leaseMath.ts:203 already exports it); the batch-reverse date input can be cleared with no confirmDisabled gate while ReverseBatchDTO.date is @NotNull (opaque error instead of a clean refusal).
Task 16 (+ batches half of Task 12): complete (commits 1024aeb3, e63b0f3c; review clean, no fix round).
Tasks 8+9: implemented (opus) commits 19d5845f (opening-balance grid, PACT TB parser, OB journal), e89ab253 (reconciliation + opening-balance API); backend 2035/0 (209 classes; +60); 17 mutations killed, 3 honestly reported as not biting (incl. a measured fact: Hibernate 7 DOES apply an enabled @Filter to a bulk delete). MODIFIED PLAN-1 CODE: PostingService.reverse now exempts docType OB from the period lock (symmetric with post) — reviewer to judge. post() refuses a second post; repost(reason) reverses-and-reposts under the marker lock (both the brief and the ruling honoured) → the web needs a distinct "Replace opening balances" action. derivedDebit/derivedCredit stay 0.00 until Task 11. No real PACT TB export exists in ~/Downloads → issue https://github.com/kunal-ak23/rentaxis/issues/303.
Tasks 8+9: review dispatched (opus) e73aaaf4..e89ab253. Task 10: BASE e89ab253 — dispatching opus implementer. Task 15 (web: opening balances + reconciliation pages, the rest of cutoverApi, two T16 minors): dispatched (resume opus web implementer).
Tasks 8+9: review (opus) spec ✅ with gaps / quality Needs fixes — C1 (Critical) OpeningBalanceService.reverse passes a CALLER-SUPPLIED date to the lock-exempt PostingService.reverse: a later-dated mirror leaves the OB on the books as at D−1 while the marker says "not posted" → the next post DOUBLES the opening balances, still balanced so nothing catches it. I1 grid difference ≠ posted difference when PACT's file has a figure on F-02; I2 no test holds the boundary of the widened reverse exemption (the only lock guard on every document's reversal); I3 matching by code only (ruling said code THEN exact name); I4 five package-private helpers for T10–11 touch repositories with no transaction. PostingService OB exemption: ACCEPT WITH CONDITIONS (keys on the original docType only; pin the reversal date; add boundary tests).
Rulings: C1 → OB reversal always dated live.getEntryDate(), no caller date; booksStartDate cannot change while an OB is live. I1 → a PACT figure on the difference account is ignored in both grid and posting, reported as a problem. I3 → code, then exact name (trimmed, case-insensitive); ambiguous/unmatched are row-numbered problems. I4 → Propagation.MANDATORY (or an explicit active-transaction assertion if the proxy does not intercept package-private). reconcile() unpaged ACCEPTED (chart-sized; web paginates client-side).
Tasks 8+9: fix round 1 message prepared (task-8-9-fix1-message.md) — QUEUED behind Task 10's implementer (same package, one Gradle).
Task 15 (web): implemented (opus) commit 93e48c14; tsc clean; vitest 840/840 in 107 files (+73); lint 206; 10 gates mutation-checked (three no-op mutations forced a better test: float drift is invisible through fmtAmount, so the test asserts the imbalance WARNING). Decisions: the difference account's cell is read-only although setRow accepts a figure that postFresh discards (Ruling: accepted — and the server will refuse it, added to the T8+9 fix round); round2 moved to lib/money.ts. Server gaps → appended to task-8-9-fix1-message.md as additive JSON (computed flag, snapshot totals/balanced, changedSincePosted, 400 on the difference account). Review dispatched (sonnet) web e63b0f3c..93e48c14.
Task 15 (web): review (sonnet) spec ✅ with one Critical gap / quality Needs fixes — CRITICAL: when the second /finance/default-accounts lookup fails, returns no mapping or resolves after the grid, differenceAccountId stays null and the difference account's cell becomes EDITABLE (server accepts, postFresh discards — "the screen lies"); untested. Important: money.ts silently turns numeric strings into 0 (Number.isFinite, no coercion); no beforeunload warning on unsaved OB grid edits although the codebase has the pattern. The date-parameter removal touches only cutover.ts (2 places) + its test.
Ruling: fail CLOSED — use the row's `computed` flag (backend fix round adds it) as the source of truth; while absent, the whole grid is read-only until the difference account is positively identified — cost if wrong: a slower first paint of the grid.
Task 15: fix round 1 dispatched (resume opus web implementer) — also mirrors the queued backend contract changes (no reverse date; computed; snapshot totals/balanced; changedSincePosted; 400 on the difference account), all typed optional.
Task 15: fix round 1 done (opus, web) commit a2f31a84; tsc clean; vitest 865/865 in 107 files (+25); lint 206; 8 gates mutation-checked. Fail-closed grid lock until the difference account is identified (disappears once the backend sends `computed`); toFils parses numeric strings exactly and accepts thousands separators (a European "1.234,50" would mis-parse → deferred minor: the product is AED/English-input today); beforeunload hook extracted from BulkChequeUploadFlow (shared component touched; its 67 tests pass); the OB suite was ORDER-DEPENDENT (clearAllMocks keeps implementations) — fixed with resetAllMocks, stable over five runs. Re-review dispatched (sonnet) web 93e48c14..a2f31a84.
Task 10: implemented (opus) commits 5d681eec (validator), ce5a6fa2 (persist as a DRAFT import batch + HTTP surface); backend 2107/0 (+72); 30 mutations, all killed. R6 SETTLED from the client's real files: no booking-deposit account exists in their chart and in every contract Σ PDC = Σ lines exactly (the security deposit is an ordinary line paid by a cheque in the grid) → no BookingDeposit columns, no exception to the Σ rule. 88 gained cheques.imported_deposited_on/_cleared_on/_bounced_on (+ CHECK) in place — needed so a reversed batch can be re-posted to the same dates. The brief's sample credited a RENT line to rental income (RENT credits ADVANCE_RENT) → template leaves CreditAccount blank; wrong-typed override refused with a cell reference. Unmatched account names are ERRORS. TrackingNumber dropped for EjariNumber (Lease has nowhere to store it). Every property in a workbook must be NEW (consistent with spec D4 wipe-and-reseed) → re-importing the same workbook after a batch reverse trips "already exists"; the path is to correct the draft batch (an "update existing" mode would be a separate task → issue at final review).
Task 10: review dispatched (opus) e89ab253..ce5a6fa2. Tasks 8+9: fix round 1 dispatched (resume opus) FIX_BASE ce5a6fa2. Task 11 follows it (same tree).
Task 15: fix round 1/5 (3 addressed + mirrors + minors, 0 open; commit a2f31a84) — re-review (sonnet) "Ready to ship": no editable window before the difference account is identified; toFils digit-splits strings and both rounding paths agree on every .xx5 boundary probed; opening-balances suite 43/43 across 3 plain + 4 shuffled-seed runs. Task 15: minor (deferred): retry fires a duplicate default-accounts GET; shared Pagination has a pre-existing hardcoded-English / non-mirrored chevron.
Task 15: complete (commits 93e48c14, a2f31a84; review clean after 1 fix round).
Web wiring for the contract import (upload cut-over workbook, template download, job status → batch) dispatched now (resume opus web implementer; typed from Task 10's committed Java). Bulk post wiring waits for Task 11.
Task 10: review (opus) spec ❌ / quality Needs fixes — praised the async tenancy work (task decorator + finally clear + requireTenant + read-only tx, proven by an IT that drives the real executor with colliding names across two tenants) and the single LeaseVat definition. C1 (Critical): two different contracts typed with one ContractNumber silently MERGE into one lease (header read from the first row only; no duplicate rule in sheet or DB; Σ guard passes on the merged key). I1: the downloadable TEMPLATE and two test classes carry a REAL renter's name from the client's PACT exports (privacy). I2: no POI resource limits on an untrusted-upload endpoint widened to ACCOUNTANT / 10 MB. I3: the imported-dates CHECK does not encode which status needs which date. I4: "every property must be new" dead-ends the reverse → correct → re-import loop.
Rulings: C1 → header-consistency + duplicate-ref errors + partial UNIQUE(tenant_id, external_contract_ref) in 88 in place. I1 → scrub every real name/number from code, template, fixtures (denylist test); the spec/plan DOCS also name the two real tenants — controller handles docs separately and plan 5's golden fixtures MUST use anonymised names (added to plan 5 rulings). I2 → explicit tested POI limits at the one place workbooks open. I3 → status↔date table in the CHECK and the validator. I4 → properties stay "must be new" (D4 wipe-and-reseed; import-onto-existing → issue); the loop is closed by a DISCARD-batch operation owned by Task 11; this round records which properties/units/renters the batch created.
Task 10: fix round 1 message prepared (task-10-fix1-message.md) — QUEUED behind the Tasks 8+9 fix round (one Gradle, same tree).
Web contract-import wiring: implemented (opus) commit 3ae0c64f; tsc clean; vitest 894/894 in 108 files (+29); lint 206; 9 gates mutation-checked; no bulk-post route existed at commit time (copy says so). Concerns: canDownloadImportTemplate widened to ACCOUNTANT (right for the cut-over route; name does not say which template); a second polling implementation remains on the properties page (v1 import) → deferred minor; sessionStorage job id is not tenant-scoped (safe 404, misleading copy) → reviewer to judge. Server gaps → Task 11 / T10 fix round: no job progress signal; ImportErrorDTO.row is a primitive int (file-level error = row 0); upload response untyped; chequesCreated reuses the schedules_created column. Review dispatched (sonnet) web a2f31a84..3ae0c64f.
Web contract-import wiring: review (sonnet) spec ✅ / quality Approved — 0 Critical, 1 Important: the sessionStorage job-id key is not scoped by tenant/user (fails SAFE: server 404s, hook clears; message generic). The widened canDownloadImportTemplate gate has exactly ONE caller; the v1 properties-page template button is on canCreateProperties (SA/TA) matching its own route — no leak. Minors: stale comment page.tsx:188-191; hook test cleanup hygiene; dismissing a finished import clears the batch highlight.
Ruling: the Important (scope the storage key by tenant + user; truthful 404 message) and the three minors ride with the LAST web dispatch of this plan (bulk-post + discard wiring after Task 11) — same implementer, same files; it fails safe meanwhile — cost if wrong: a confusing message for a super admin who switches organisation mid-import.
Web contract-import wiring: complete (commit 3ae0c64f; review approved; one Important carried into the final web dispatch).
Tasks 8+9: fix round 1 done (opus) commit 3aab5f13; backend 2133/0 (213 classes; +26); 11 mutations killed. Contract: reverse body loses `date` (ignored if sent); additive computed / totalDebit / totalCredit / balanced / changedSincePosted exactly as the web expects; the computed row now carries the balancing figure (so grid.difference == the posted line); helpers public + MANDATORY + active-transaction assertion; booksStartDate change refused while an OB is live. Concerns: the C1 pin and the books-start guard overlap (pin only bites via a repository-level date move — tested that way); I3 changes matching semantics for already-uploaded snapshots (names now match) → PR note; TenantFiscalSettingsService depends on two cut-over repositories (layering) → reviewer. Re-review dispatched (sonnet) ce5a6fa2..3aab5f13. Task 10: fix round 1 dispatched (resume opus) FIX_BASE 3aab5f13.
Tasks 8+9: fix round 1/5 (5 addressed + additive contract, 0 open; commit 3aab5f13) — re-review (sonnet) "Ship": reversal date taken from the live journal under the marker lock; no supported path can move booksStartDate while an OB is live (first-time and no-op sets still allowed); boundary tests fail under "exempt everything"; name matching is resolved at READ time (not persisted); no circular dependency. Tasks 8+9: minor (deferred): group-vs-leaf same-name ambiguity handled by construction but untested; TOTAL_ROW swallows a code starting "Total…"; the reconciliation "derived" column is really "our books at D−1 minus the live OB" (label).
Tasks 8+9: complete (commits 19d5845f, e89ab253, 3aab5f13; review clean after 1 fix round).
Task 10: fix round 1 done (opus) commits 432afb7b (privacy scrub + denylist test), f28ef734 (C1 header-consistency + duplicate-ref errors + partial UNIQUE; I3 four-way, mode-aware date CHECK; I4 import_batch_entities), 14d03821 (I2 POI limits; ImportErrorDTO.row boxed Integer; typed ImportJobStartedDTO); backend 2163/0 (+30); 25 mutations killed. 88 edited in place again (deploy note). Implementer found its own CHECK leaked on NULL status (Postgres accepts UNKNOWN) → coalesce().
Ruling: both deviations from my literal I3 table are ACCEPTED as better accounting — CLEARED requires deposited_on only for mode PDC (a cash receipt is never banked); BOUNCED permits cleared_on (a bounce after clearing credits the bank, before it credits PDC receivable — dropping the date would move the credit silently). DepositedDate optional, defaulting to the cleared/bounced date through ONE helper (PACT does not export it).
Wider privacy sweep (buildings/company in older tests, a web test, the seed script; the docs naming two real tenants) → issue https://github.com/kunal-ak23/rentaxis/issues/304; Kunal asked about the docs.
Task 10: re-review dispatched (sonnet) 3aab5f13..14d03821. Task 11: BASE 14d03821 — dispatching opus implementer.
Task 10: fix round 1 re-review (sonnet) — C1, I1, I2, I3, contract changes, template/VAT: ADDRESSED (C1 three layers, exact merge scenario refused; git grep for the real names clean; WorkbookGuard is the one door with JVM-global statics set once; CHECK coalesce'd on every branch, both accepted deviations encoded). I4 NOT FULLY ADDRESSED: buildings created by ContractImportPersistService.writeUnits are never linked in import_batch_entities (the report claimed otherwise; the test asserts their absence). Out of scope: AccountImportService.importFromExcel bypasses WorkbookGuard; web still types ImportErrorDTO.row as non-nullable number.
Ruling: the buildings gap is one linkEntity call + a real test in code the Task 11 implementer is building its DISCARD on RIGHT NOW → handed to Task 11 as an early rider commit (with the AccountImportService guard) instead of a second Task 10 round into a busy tree — cost if wrong: none; Task 11's review verifies both. The web `row: number | null` typing rides with the final web dispatch.
Task 10: complete (commits 5d681eec, ce5a6fa2, 432afb7b, f28ef734, 14d03821; review clean after 1 fix round; the I4 buildings residue rides with Task 11).
Task 11: implemented (opus) commits 8c601222 (riders: buildings linked to the batch; chart-of-accounts import through WorkbookGuard), 054b92d3 (bulk post, batch id on every journal), 0b0f2832 (LeaseReverter + reverse pre-conditions), acb662e2 (discard, derived reconciliation column, HTTP surface), 69659826; backend 2209/0 (219 classes; +46); 30 mutations killed. Endpoints: POST …/import-batches/{id}/post → {jobId,batchId} (async); GET …/{id}/post/{jobId} → status + processed/total + per-contract result + derived journalsPosted; POST …/{id}/discard → deleted counts + kept[]; GET /finance/journals?importBatchId=; ImportBatchDTO.discardedAt + DISCARDED; OpeningBalanceRowDTO.derivedDebit/derivedCredit.
Decision by implementer (accepted): a posted-then-REVERSED batch cannot be discarded (immutable journals carry restricting FKs to its leases/units/properties/renters) → recovered by "Post again" (successor batch, tested to identical balances); DRAFT batches discard whole (loop tested end to end). Nightly overdue reminders WILL e-mail renters about imported overdue cheques → product decision, issue https://github.com/kunal-ak23/rentaxis/issues/305. 88 edited in place a third time (deploy note).
Task 11: review dispatched (opus) 14d03821..69659826. Final web wiring (bulk post + progress, discard, Post again, journals drill-through, derived columns, storage-key scoping, row nullable, 3 minors): dispatched (resume opus web implementer).
Task 11: review (opus) spec ✅ / quality Approved with follow-ups — 0 Critical, 4 Important. Hand-derived a full contract (12 journals, six lease-dimension balances) and EVERY account matches; OUTPUT_VAT right. Unlisted deviation: import journals keep their natural source_type (LEASE/CHEQUE/RECOGNITION) and carry only import_batch_id — correct (blockersAgainstRevert and the recognition index depend on it), now documented. I1 discarding a REVERSED batch deletes nothing yet marks it DISCARDED, destroying the "Post again" recovery, with a false refusal text; I2 discard takes no batch row lock (a concurrent discard deletes leases mid-post); I3 on re-post the successor batch row is created in the OUTER transaction while leases commit in INNER ones → an outer rollback strands committed journals with a batch id no row has (no FK); I4 reconciliation IT omits OUTPUT_VAT.
Rulings: I1 → a REVERSED batch can NOT be discarded (400 "Post it again, or leave it reversed"; only DRAFT discards); I2 → discard takes the same batch lock as post (refresh under PESSIMISTIC_WRITE, "try again" on contention); I3 → the successor batch row is committed in its OWN transaction BEFORE any lease posts, and a FK journal_entries.import_batch_id → import_batches (ON DELETE RESTRICT) goes into 88 in place so a journal can never point at a missing batch; I4 → OUTPUT_VAT in the fixture and asserted.
Task 11: fix round 1 dispatched (resume opus) FIX_BASE 69659826.
Final web wiring: implemented (opus) commit d59bd379; tsc clean; vitest 923/923 in 109 files (+29); lint 206; 9 gates mutation-checked. It mirrored the Java AS IT STOOD (REVERSED discardable) — correct then; the Task 11 fix round is changing the rule → a small web follow-up dispatched (REVERSED not discardable; retry only on evidence; bulk-post job id persisted for reload; post disabled while an upload is in flight). PROCESS NOTE: the agent used `git checkout --` as a mutation undo and reverted unstaged real work on two pages; it caught it because the suite went red, re-applied and re-verified — told never to use checkout/restore as an undo again. Server gaps → issues at final review: per-batch failed-contract count; job-status route not batch-scoped (resume); DiscardResult.kept[].reason is English prose; PostJobDTO.status a raw string; chequesCreated reuses schedules_created.
Web follow-up done (opus) commit b30a46c9; vitest 932/932; lint 206; 5 gates mutation-checked from byte copies. Review of the final web unit (d59bd379 + b30a46c9) dispatched (sonnet) web 3ae0c64f..b30a46c9.
Final web unit: review (sonnet) spec ✅ / quality Needs fixes — 0 Critical, 3 Important (retry dialog shows the batch total instead of the failed count; postJob.gone never rendered → a stale bulk-post job 404s silently; upload input not disabled during a bulk post). Everything else verified. Fix round dispatched (resume opus web implementer).
Final web unit: fix round 1 done (opus) commit bda25cd1; vitest 935/935; lint 206; 4 mutations from byte copies. (Both implementers had been killed by an API 529 at 06:20 and resumed at 09:20.) Re-review dispatched (haiku) web b30a46c9..bda25cd1.
Final web unit: fix round 1/5 (3 addressed, 0 open; commit bda25cd1) — re-review (haiku) READY; 53/53 in the page test file; tsc clean.
Final web unit (bulk post / discard / drill-through + all carried items): complete (commits d59bd379, b30a46c9, bda25cd1; review clean after 1 fix round). ALL WEB WORK OF PLAN 4 COMPLETE — Tasks 12–16 done.
Remaining in plan 4: Task 11 fix round (resumed after the API 529) → re-review; then Task 17.
Task 11: fix round 1 done (opus, resumed after the API 529) commit da2bd454; backend 2219/0 (219 classes; +10); 9 mutations killed AFTER the implementer discovered its own harness restored with `git checkout --` (discarded six uncommitted fix files; re-applied, harness fixed to byte snapshots, whole sweep re-run against a committed tree). Contract: ImportBatchDTO.repostOf; discard on REVERSED → 400; reworded 400s; 88 in place a FOURTH time (import_batches.repost_of + FK; fk_je_import_batch ON DELETE RESTRICT). Deferred: successor batch shares import_job_id; edited drafts keep old imported dates after a reverse; a cheque PostingDate ≥ books_start_date falls outside the derived grid; a mid-month books_start_date leaves the straddling month to the ordinary close (runbook line).
Task 11: re-review dispatched (sonnet) backend 69659826..da2bd454. Task 17 started in parallel: rebuild the jar at HEAD, recreate rentaxis_v2 (88 edited four times), bring the stack up, dispatch the walkthrough author.
Task 11: fix round 1/5 (4 addressed + deviation documented, 0 open; commit da2bd454) — re-review (sonnet) Approved: shared lockForRun (find → tenant check → refresh PESSIMISTIC_WRITE timeout 0); successor batch committed via a genuine REQUIRES_NEW TransactionTemplate; fk_je_import_batch real with a schema IT; mid-run-failure ITs in both directions. Task 11: minor (deferred → final fix wave): lockForRun's refusal names the caller's verb, not the lock holder's (a discard blocked by a post says "being discarded"); dead @Autowired fields + unused import in ImportBatchDiscardIT (residue of the checkout mishap).
Task 11: complete (commits 8c601222, 054b92d3, 0b0f2832, acb662e2, 69659826, da2bd454; review clean after 1 fix round). ALL 16 IMPLEMENTATION TASKS OF PLAN 4 COMPLETE. Task 17 (walkthrough) in flight against the live stack (jar da2bd454, fresh DB).
Ruling: the final whole-branch review starts NOW in parallel with the walkthrough (read-only seats; production code frozen except walkthrough-found bugs, which join the single fix wave) — two seats as before: backend (fable) over ea4a205c..da2bd454's backend, web (opus) over the web; each triages the ledger's deferred minors and issue list — cost if wrong: a walkthrough-driven fix lands after the review and gets only a scoped re-review.
Final review (web seat, opus): With fixes — 0 Critical / 3 Important / 16 Minor; tsc clean, vitest 935/935 self-run, i18n parity 81⇄81 + 129⇄129, gate⇄rule table 38 exact of 40. I-1 a DISCARDED batch's badge renders "Reversed" (three-arm ternary over four statuses); I-2 Opening Balances Post/Replace are blocked by ADVISORY `problems` the server accepts (the PACT-difference note every real export emits, the derived-role notes) → the ordinary cut-over cannot be posted from the UI at all; I-3 the OB reversal endpoint has no UI (an OB posted in error can only be replaced, never un-posted). All three block legitimate work; nothing offers what the server refuses.
Ruling (I-2): `problems` gains a severity (ERROR blocks; WARNING informs) on the server side in the backend fix wave — the web gates on ERROR only and lists WARNINGs; until the field lands the web treats every problem as a warning EXCEPT the ones that mirror `postable`'s fatal conditions (unmatched/ambiguous rows) — cost if wrong: one field. Ruling (I-3): add "Reverse opening balances" (ConfirmDialog with reason; only when an OB is live; SA/TA/ACCOUNTANT) next to Replace.
Web fix wave = I-1, I-2, I-3 + M-3 (stale file headers claiming bulk post/discard/re-post "do not exist"), M-4 (28 dead message keys), M-16 (stale role-gate comments). HELD until the walkthrough author finishes (both edit web/), then dispatched together with any walkthrough-found web bugs.

## Final review — backend seat (fable) returned 2026-09-22 — `final-review-backend.md`
Verdict: with fixes. 2C · 4I · 10M. Findings ruled as follows (one backend fix wave, then scoped re-review):
- Ruling R16 (C1): `ImportBatchService.reverse` dates every mirror on its ORIGINAL journal's entry date (same class as the OB C1 fix from T8+9); the caller-supplied `date` leaves the backend contract (`ReverseBatchDTO` keeps `reason` only; unknown JSON fields are tolerated until the web wave drops `date`). — a reversed cut-over must vanish from TB(D−1), otherwise "Post again" doubles it — cost if wrong: none (there is no legitimate use of a later date for a cut-over mirror).
- Ruling R17 (C2): `OpeningBalanceService.postable()` posts the DELTA `PACT(X) − ours(X)` for every non-derived account X (ours = `derivedBalances(asOf)`, which already excludes a live OB), so TB(D−1) = PACT exactly on every typed account and the difference line becomes the true unreconciled gap Σ_R(P − B). Order rule (option a): bulk post, batch reverse and Post again are REFUSED while an OB journal is live — "Reverse the opening balances first; post them again after". One composition IT (import → bulk post → TB upload → OB post → TB(D−1) asserted account by account). Spec §10.3 gets an amendment paragraph on this branch — spec-level correction, ruled here because the spec's exclusion list is not closed under the cut-over's own postings — cost if wrong: an accountant who WANTS gross PACT figures on bank would have to enter a manual journal; acceptable.
- Ruling R18 (I1): `reverse` uses `lockForRun` (NOWAIT, tenant before lock); `lockForWrite` deleted; refusal worded for any holder ("being posted, reversed or discarded right now; try again") — closes the T11 verb minor.
- Ruling R19 (I2): the DataIntegrityViolation catch moves OUTSIDE `ownTx.execute` (also catching UnexpectedRollbackException); IT with a live FK (booking or ticket) proving the row is KEPT and the discard completes.
- Ruling R20 (I3): guard stays; message tells the truth ("recognised by the month-end close on <date>; a cut-over batch cannot be reversed once its contracts have been through a close — correct individual contracts by amendment"). No recognition-period reverse in v2 → issue. Runbook line in the PR.
- Ruling R21 (I4): `TenantFiscalSettingsService.setBooksStartDate` also refuses while any POSTED import batch exists (same shape as the OB guard).
- Minors in the wave: M3 dead code; M4 ordinary `POST /leases/{id}/post` refuses (409) a lease linked to a DRAFT import batch ("belongs to import batch X; post the batch"); M6 comment on `PostingService.reverse` (exemption is inherited); M7 principal instead of X-User-Id in `importCutOver`; `ImportBatchDTO.problems[].severity` (web I-2 needs it). Deferred → issues: M1, M2, M5, M10, I3 product ask, T10 import-onto-existing, T8–9 group-vs-leaf test, T11 edited drafts keep imported dates, hotfix follow-ups (managers endpoint tenant check, org_settings unique). M8 → comment on #304. M9 → web wave (label). Brief: `final-fix-backend-brief.md`.
- Issues filed from the backend seat: #306 recognition-period reverse (I3), #307 IOException handler (M1), #308 attachment blob orphan (M2), #309 post-cut-over deposit undone (M5), #310 successor job id (M10), #311 import-onto-existing (T10), #312 group-vs-leaf test, #313 edited drafts vs imported dates, #314 managers endpoint tenant check, #315 org_settings unique; #304 widened (M8). Backend fix wave dispatched (opus) 2026-09-22 from da2bd454, brief `final-fix-backend-brief.md`.

## Task 17 (walkthrough) — first pass complete 2026-09-22 — `task-17-report.md`
Commits 10374f51, c7c186ae, 9d266480. `11 passed (3.1m)` at default pacing against the da2bd454 jar; 11 takes. Found B1 = C2, B2 = C1 independently (both fixed by the wave mid-run), B3 doc-only. No web bugs. Two web findings: OB footer imbalance warning unreachable; `VoucherForm` file picker during an in-flight save creates a second draft.
- Ruling R22: the recording is NOT final — the fix wave's order rule (2aefd796) changes the scenario order (OB last) and the delta rule changes scenario 05's figures; re-record after both waves against a jar built from the fixed HEAD on a recreated `rentaxis_v2`, per the report's Hand-off (resume the same author). Cost if wrong: one more recording pass.
- Ruling R23 (OB footer): the computed row keeps the balancing figure; the unreachable `ob-difference-warning` becomes an always-shown explanation when the difference line is non-zero ("AED X posts to Opening Balance Difference") — the delta rule makes that line the true unreconciled gap, which the accountant must see, not a warning about columns. Cost if wrong: copy change.
- Ruling R24: `VoucherForm` file input disabled while a save is in flight (`busy` guard) — trivial, closes the second-draft race.
- Azurite `wt4-azurite` (docker) is running for voucher attachments — leave it for the re-record; note it in the walkthrough config's prerequisites.
- Web fix wave dispatched (opus) — brief `final-fix-web-brief.md`; runs in parallel with the backend wave (disjoint trees; web-only `git add` paths).
- 2026-09-22 ~11:50: Kunal's connection dropped while commuting → both fix-wave agents stalled (600 s watchdog). No commits lost. Backend wave: all eight items committed (cbfb558a C1, d59ad4be C2, 2aefd796 order rule, d19b580a I1, eb17d52c I2, 1b879299 I3, dba28b29 I4, 259a77fa M4, af1e8eae minors); its foreground full-suite run was CANCELLED by the daemon at 41 min ("client disconnection detected") — re-run by the controller in the background (log /tmp/p4-final-backend-test.log), agent resumed only to write its report. Web wave: nine items committed (182ad1f5 … 82961319); resumed at M-4 with its uncommitted en/ar/parity-test edits in place. Lesson: a subagent's single foreground call must not exceed the watchdog — long suites go to a background run with polling.

## Backend fix wave returned 2026-09-22 12:05 — `final-fix-backend-report.md` — DONE_WITH_CONCERNS; rulings on its concerns
- Ruling R25 (concern 6 — LOAD-BEARING): `enteredDebit/Credit` MUST stay the PACT figure the accountant entered/uploaded — the web edit inputs are seeded from it (`opening-balances/page.tsx:175`), so returning the delta there would make an untouched Save store the delta as PACT and the next post subtract `ours` again (feedback loop). The row gains `postDebit/postCredit` (= what will post, the delta); totals/difference/`changedSincePosted` and the posted lines derive from the post figures; the web grid shows three columns (PACT entered · on our books · posts) and its footer sums the post figures. Cost if wrong: a redundant column.
- Ruling R26 (concern 2 — I-2 landed on the wrong DTO): the web I-2 finding is about `OpeningBalanceGridDTO.problems` (the OB grid), not the import's `ImportErrorDTO`. The grid's `problems` becomes `[{message, severity}]`: the no-difference-account fault is ERROR, every other entry WARNING. The web already reads both shapes (bare string = ERROR). `ImportErrorDTO.severity` stays (harmless, correct).
- Ruling R27 (concern 1): the four new refusals stay HTTP 400 like the existing OB/books-start guards — the brief's "409" yielded to "same shape as the OB guard"; the web keys on the message. Cost if wrong: none observable.
- Ruling R28 (concern 4): `dryRun` on a lease linked to a DRAFT import batch reports the same refusal as a problem (ok=false) — a dry run that says ok before a post that refuses is a lie.
- Accepted as-is: concern 3 (`import_batch_leases` is the right table), 5 (`SnapshotUploadResult.balanced` describes the file), 7 (I3 date = periodEnd), 8–10. Suite counts come from the controller's background run.
- Backend follow-up dispatched (opus) — brief `final-fix-backend-followup-brief.md`; must not run Gradle until the controller's suite run has written `EXIT=` to `/tmp/p4-final-backend-test.log`. Web agent messaged with R25/R26 as an added wave item.

## Web fix wave returned 2026-09-22 12:12 — `final-fix-web-report.md` — DONE_WITH_CONCERNS
12 commits 182ad1f5 … f11ffc57 (11 items + R25). Vitest 955/955 in 109 files; tsc clean; lint 206 = baseline. Concern 1 (wire names) is settled by the backend follow-up brief (`GridProblemDTO(message, severity)`, `postDebit/postCredit`) — controller cross-checks when it lands. Concern 2 accepted (order rule surfaced, not mirrored: `ORDER_RULE_IS_SERVER_SIDE_ONLY`). Concern 3 fixed by the controller (`Cutover.batchReversed` copy, one commit). Concern 4 → issue #316 (attachment in amend mode lands on the original). Web scoped re-review dispatched (sonnet) on `final-fix-web.diff`.
- Web scoped re-review (sonnet) 2026-09-22 12:22: 'Fix wave accepted' — one follow-up F1 (stale `batch-reverse-date` in the e2e and walkthrough specs, orphaned by 0de13023): e2e fixed by the controller (one-line removal); the walkthrough spec's copy is removed in the re-record (Hand-off item 4). `final-rereview-web.md`.
- Full backend suite (controller, background) on af1e8eae+web: BUILD SUCCESSFUL in 35m 51s — 220 classes, **2232 tests / 0 failures / 0 errors / 0 skipped** (summed from build/test-results XML). Backend follow-up (R25/R26/R28) may now use Gradle.

## Backend follow-up returned 2026-09-22 13:22 — `final-fix-backend-followup-report.md` — DONE
7bf85583 (R25 three figures), 2d48a582 (R26 problem severity), f2836bb3 (R28 dry run). Six affected classes 86/0. Wire names cross-checked against `web/src/lib/api/cutover.ts`: `postDebit`/`postCredit`, `{message, severity}` — match. Concerns: (1) derived rows now show PACT's entered figure with post 0.00 — accepted, informative; (3) `OpeningBalanceService` imports `api.dto.cutover.GridProblemDTO` — layering smell, handed to the re-review (fix only if a fix round runs anyway; else issue); (8) full suite predates the follow-up → controller runs it once more at the final HEAD, after the re-review's fix round if any.
- Backend scoped re-review dispatched (fable) on `final-fix-backend.diff` (da2bd454..f2836bb3, backend + spec). In parallel: jar rebuilt, `rentaxis_v2` recreated, re-record dispatched — if the re-review's fix round changes walkthrough-visible behaviour, the affected scenarios are re-run.
- 12:55 stack cycled for the re-record: jar rebuilt from f2836bb3 (contains GridProblemDTO), rentaxis_v2 dropped/recreated, backend-v2 restarted → UP, changesets 87/88 applied 12:54:59, no server errors; web :3001 unchanged (HMR carried the wave).
- Backend scoped re-review (fable) 2026-09-22 13:05: **"Fix wave accepted."** C2 figures re-derived and match; no fourth door into the order rule; Liquibase untouched; `GridProblemDTO` in the service ruled acceptable. Four minors: N1 dead `asWarning()` + N2 stale `lockForWrite` Javadoc → fixed by the controller (0f2342c9); N3 untested `−ours` branch + N4 discard lease loop without the I2 catch → small sonnet dispatch (`final-fix-backend-minors-report.md`). Then ONE full suite at the final HEAD (background), then push + PR. `final-rereview-backend.md`.
- N3/N4 landed (sonnet): a26ceb7e test for the `−ours` branch, 7d839ae5 I2-style catch on the discard's lease loop; 9/0 + 14/0; both mutation-checked red. Sign note: the re-review wrote "Cr 1,050" for the OUTPUT_VAT `−ours` line; it is Dr 1,050 (ours is a credit; the OB line cancels it; TB(D−1) = 0) — the test asserts the correct sign. Final full suite started 2026-09-22 13:20 at 7d839ae5 (`/tmp/p4-final-backend-test2.log`, background). Walkthrough jar = f2836bb3; HEAD differs only by dead-code/Javadoc, a test and the N4 catch — none walkthrough-visible; PR says so.

## Task 17 re-record complete 2026-09-22 13:22 — `task-17-rerecord-report.md`
dfa326f5 (spec). **Plan 4 `13 passed (2.4m)`**; plan 2 regression `17 passed`; plan 3 regression `12 passed, 1 skipped` (pre-existing skip; flaked once, then twice green → #318); e2e cut-over `10 passed`. 13 takes (12 MB) sent to Kunal. Findings: (1) the order-rule refusal on bulk post is asynchronous — 200 + job FAILED with the sentence (guard runs inside the job under the batch lock) while reverse refuses with 400 synchronously; accepted, no UI route reaches the post case while OB is live (only POSTED batches exist) — PR note; (2) bankSession cookie race fixed in the plan 4 spec → #317 for plans 2/3; (3) plan 3 flake → #318. Task 17: complete.
- Final full backend suite at 7d839ae5 (controller, background): BUILD SUCCESSFUL in 29m 51s — 220 classes, **2237 / 0 / 0 / 0**. Web 955/955, tsc clean, lint 206 = baseline. Walkthrough 13/13 (jar f2836bb3). Plan 4 final review: CLEAN. Ledger snapshot → `docs/superpowers/handoff/2026-09-22-plan4-sdd-ledger.md`; push; PR stacked on #298; plan 5 branch cut from this HEAD.
