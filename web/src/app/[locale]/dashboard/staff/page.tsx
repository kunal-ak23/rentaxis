import StaffManager from "@/components/staff/StaffManager";

/** Operations › Staff (live URL); Settings › Users & staff embeds the same component. */
export default function StaffPage() {
    return <StaffManager />;
}
