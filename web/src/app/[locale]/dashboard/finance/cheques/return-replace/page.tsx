import ReturnReplacePanel from "@/components/collections/ReturnReplacePanel";

/** Moved to Cheque / Cash Collection › Returned / replace; the middleware 308s this route there. Kept for its tests. */
export default function ChequeReturnReplacePage() {
    return <ReturnReplacePanel />;
}
