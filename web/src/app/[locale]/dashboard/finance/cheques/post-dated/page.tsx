import PostDatedPanel from "@/components/collections/PostDatedPanel";

/** Moved to Cheque / Cash Collection › Post-dated; the middleware 308s this route there. Kept for its tests. */
export default function PostDatedChequesPage() {
    return <PostDatedPanel />;
}
