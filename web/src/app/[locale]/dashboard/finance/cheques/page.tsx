import ChequeRegisterPanel from "@/components/collections/ChequeRegisterPanel";

/** Moved to Cheque / Cash Collection › Cheque register; the middleware 308s this route there. Kept for its tests. */
export default function ChequeRegisterPage() {
    return <ChequeRegisterPanel />;
}
