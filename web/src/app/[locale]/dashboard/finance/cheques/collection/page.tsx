import ToDepositPanel from "@/components/collections/ToDepositPanel";

/** Moved to Cheque / Cash Collection › To deposit; the middleware 308s this route there. Kept for its tests. */
export default function ChequeCollectionPage() {
    return <ToDepositPanel />;
}
