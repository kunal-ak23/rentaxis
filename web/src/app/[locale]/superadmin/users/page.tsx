import UsersManager from "@/components/users/UsersManager";

/** Live URL for SUPER_ADMIN (Settings › Administration); Settings › Users & staff embeds the same component. */
export default function SuperAdminUsersPage() {
    return <UsersManager />;
}
