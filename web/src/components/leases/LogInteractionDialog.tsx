"use client";
export default function LogInteractionDialog({ leaseId, onClose, onSuccess }: { leaseId: string; onClose: () => void; onSuccess: () => void }) {
  // STUB — replaced in Task 22
  return null;
}
